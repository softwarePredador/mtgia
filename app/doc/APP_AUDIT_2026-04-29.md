# ManaLoom App Audit - 2026-04-29

## Atualizacao - Card Entry QA PASS WITH RISKS - 2026-05-08

Rodada focada em criacao, busca, insercao, edicao, troca de edicao, remocao e
fichario de cartas, com Scanner/camera/OCR/MLKit fora do escopo.

Resultado app/server focado: **PASS WITH RISKS**. Busca/Card Detail/Deck Detail/
edicao de carta e Binder passaram a deixar identidade de edicao clara com
`SET #collector`, foil/non-foil quando conhecido, nome da colecao, raridade e
data onde o backend fornece. Superficies non-AI de foil/raridade deixaram de usar
icone `auto_awesome`. O app tambem passou a evitar mensagens tecnicas de erro em
busca/edicoes/remocao de carta.

Validacao focada passou em Flutter analyze/testes, Dart analyze/testes offline e
prova live de deck com backend local 8082. Risco remanescente: nao houve prova
runtime em device/simulador nem screenshot review nesta rodada.

Relatorio:
`docs/qa/manaloom_card_entry_qa_2026-05-08.md`.

## Atualizacao - Android internal APK SM A135M PASS WITH RISKS - 2026-05-08

Rodada de validacao do APK interno Android non-scanner no device fisico
`SM A135M` (`R58T300SREH`, Android 14/API 34) contra o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`, com Scanner/camera/OCR/
MLKit 100% ignorados.

Resultado: **PASS WITH RISKS**. O backend `/health` respondeu `healthy`
(`git_sha=01c55faf3dc32cba80756c7198911385d9490723`), `flutter analyze lib
test integration_test --no-version-check` passou sem issues, `flutter test test
--no-version-check` passou com exit code 0, e o APK release foi gerado em
`app/build/app/outputs/flutter-apk/app-release.apk` com SHA-256
`c158e67e733446489df495e0e511df34939f7943154862dba604c7eb1a0fad2e`.

O APK foi instalado com `adb install -r` no `R58T300SREH` e aberto fora de
`flutter run` via `am start -W -n com.mtgia.mtg_app/.MainActivity`. A prova
runtime percorreu Auth register/login, Home, Search/Cards, Colecoes/Set Detail,
Decks, Generate async/save, Deck Detail, Optimize `rebuild_guided`, Validate,
Binder, Marketplace, Trades, Messages, Notifications, Community e Life
Counter/Lotus.

Riscos aceitos: Generate async levou ~55s e retornou fallback deterministico
amigavel; Optimize retornou `422` para deck estruturalmente ruim e a UI mapeou
para reconstrucao guiada sem aplicar mudancas, embora o termo tecnico
`rebuild_guided` ainda apareca na microcopy; logs Android exibiram ruido
`gralloc4`/`OpenGLRenderer` sem crash. Nao houve P0/P1 fora de Scanner, nem
5xx, tela branca, ANR/crash do app ou overflow bloqueante observado.

Evidencias redigidas:
`app/doc/runtime_flow_proofs_2026-05-07_sm_a135m/`.

Relatorio:
`server/doc/ANDROID_INTERNAL_BUILD_VALIDATION_2026-05-07.md`.

## Atualizacao - Android internal APK SM A135M BLOCKED por lockscreen - 2026-05-08

Rodada de validacao do APK interno Android non-scanner no device fisico
`SM A135M` (`R58T300SREH`, Android 14/API 34) contra o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`, com Scanner/camera/OCR/
MLKit 100% ignorados.

Resultado: **BLOCKED** para smoke funcional completo. O backend `/health`
respondeu `healthy`
(`git_sha=74e3176543b7fe9a727567d6ed7cf4503157b70e`), `flutter analyze lib
test integration_test --no-version-check` passou sem issues, `flutter test test
--no-version-check` passou com exit code 0, e o APK release foi gerado em
`app/build/app/outputs/flutter-apk/app-release.apk` com SHA-256
`c158e67e733446489df495e0e511df34939f7943154862dba604c7eb1a0fad2e`.

Melhora em relacao a rodada anterior: o device agora apareceu em ADB e a
instalacao retornou `Success`; o app foi aberto fora de `flutter run` via
`am start -W`, com processo vivo (`com.mtgia.mtg_app`) e sem `FATAL EXCEPTION`/
ANR do pacote no logcat filtrado. Bloqueio remanescente: o telefone permaneceu
em `NotificationShade`/`mDreamingLockscreen=true`, entao Login, Home, Search/
Sets, Decks, Generate async, Deck Detail, Optimize/Validate, Binder,
Marketplace, Trades, Messages/Notifications, Profile/Community e Life Counter/
Lotus ficaram **NOT PROVEN** visualmente.

Relatorio:
`server/doc/ANDROID_INTERNAL_BUILD_VALIDATION_2026-05-07.md`.

## Atualizacao - Android internal APK SM A135M BLOCKED - 2026-05-07

Tentativa de build interno Android non-scanner para decisao final de release no
`SM A135M` (`R58T300SREH`) contra o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`, com Scanner/camera/OCR/
MLKit 100% ignorados.

Resultado: **BLOCKED** para prova instalada fora de `flutter test/flutter run`.
O backend `/health` respondeu `healthy`
(`git_sha=fd5fa91b2528204ae2818fdc7f263e6676334a79`), `flutter analyze lib
test integration_test` passou, `flutter test test` passou com 551 testes, e o
APK release foi gerado em `app/build/app/outputs/flutter-apk/app-release.apk`
com SHA-256
`c158e67e733446489df495e0e511df34939f7943154862dba604c7eb1a0fad2e`.

Bloqueio: `adb devices -l` nao listou Android e `adb -s R58T300SREH` retornou
`device not found` mesmo apos reiniciar ADB. Instalacao, abertura fora de
Flutter, screenshots, logcat filtrado e smoke non-scanner do APK instalado
ficaram **NOT PROVEN** nesta rodada.

Relatorio:
`server/doc/ANDROID_INTERNAL_BUILD_VALIDATION_2026-05-07.md`.

## Atualizacao - iPhone 15 Simulator visual density PASS WITH RISKS - 2026-05-07

Auditoria visual/densidade no `iPhone 15 Simulator`
(`F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, iOS 17.4) contra o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`, com Scanner/camera/OCR/
MLKit 100% ignorados.

Resultado: **PASS WITH RISKS**. O backend `/health` respondeu `healthy`
(`git_sha=cbfea7356c5e84c51f7adce7ec4b7f7eae2a4a60`), contas QA descartaveis
foram autenticadas sem documentar segredos, e foram materializados 57 PNGs em
`app/doc/runtime_flow_proofs_2026-05-07_iphone15_visual_density/` cobrindo
Auth, Home, Search/Cards, Card Detail, Sets/Set Detail, Decks/Deck Detail,
Generate, Optimize, Validate, Binder, Marketplace, Trades, Messages,
Notifications, Profile, Community e Life Counter/Lotus.

Nenhum patch visual de runtime foi aplicado: a rodada nao encontrou P0/P1 visual
deterministico. Riscos remanescentes: `deck_runtime_m2006_test.dart` capturou
`10_complete_validated`, mas falhou depois por assert textual; a captura ampla
de Generate ainda pode marcar preview como nao provado, apesar do harness async
dedicado provar preview/save/detail.

Relatorio:
`docs/qa/manaloom_visual_density_audit_iphone15_2026-05-07.md`.

## Atualizacao - Android SM A135M design second pass PASS WITH RISKS - 2026-05-07

Segunda passada visual/UX no Android fisico `SM A135M` (`R58T300SREH`,
Android 14/API 34) contra o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`, com Scanner/camera/OCR/
MLKit 100% ignorados.

Resultado: **PASS WITH RISKS**. O backend `/health` respondeu `healthy`
(`git_sha=797d69f4409ba39ba7674d77a7993ddad9bf8239`), contas QA descartaveis
foram autenticadas sem documentar segredos, `flutter analyze lib test
integration_test`, `flutter test test`, os harnesses Android non-scanner de
app amplo, Sets/Search, Binder dashboard, Marketplace/Trades/Messages/
Notifications, Deck runtime, Generate async e Life Counter Player State passaram
no device fisico.

Patch visual seguro aplicado na Home: wordmark e headers ficaram resilientes a
largura SM A135M, e os cards secundarios de intencao passaram a usar layout
empilhado para reduzir pressao horizontal sem alterar identidade
Obsidian/Brass/Frost Blue. Harnesses tocados passaram a emitir capturas
`CAPTURE_TAKEN` para Search/Card Detail/Sets, Binder dashboard, Marketplace/
Trades, mensagens, notificacoes e Life Counter/Lotus.

Evidencias: os PNGs da segunda passada ficaram materializados em
`app/doc/runtime_flow_proofs_2026-05-07_sm_a135m_design_second_pass/`, incluindo
capturas individuais e `second_pass_contact_sheet.png`. Risco remanescente:
Optimize `Agressivo` segue dependente do backend publico entregar preview
positivo; falha amigavel foi provada.

Relatorio:
`docs/qa/manaloom_android_design_audit_sm_a135m_2026-05-07.md`.

## Atualizacao - Android SM A135M design audit PASS WITH RISKS - 2026-05-07

Rodada visual/UX no Android fisico `SM A135M` (`R58T300SREH`, Android 14/API 34)
contra o backend publico `https://evolution-cartinhas.8ktevp.easypanel.host`,
com Scanner/camera/OCR/MLKit 100% ignorados.

Resultado: **PASS WITH RISKS**. Autenticacao real com conta QA descartavel,
capturas in-run de Login/Register/Home/Decks/Deck Detail/Generate/Community/
Collection/Profile, runtime PASS para Sets/Search, Binder/Marketplace/Trades/
Messages/Notifications e Life Counter shell. Optimize/Validate fresco ficou
bloqueado antes do fluxo no harness, mas havia prova historica do mesmo
dia/device.

Evidencias locais adicionais ficaram em
`app/doc/runtime_flow_proofs_2026-05-07_sm_a135m_design/`, incluindo PNGs de
Login/Register e screenshots ADB de Sets/Search e Life Counter apos os patches
visuais.

Patches visuais seguros: icone de Colecoes deixou de usar semantica de IA,
tabs da Collection ficaram rolaveis em largura SM A135M, CTAs primarios de
Life Counter/Lotus usam Brass com foreground Obsidian, e CTA `Gerar com IA`
no onboarding usa Frost Blue. Sem mudanca backend/API/DB/AI/scanner/secrets.

Relatorio:
`docs/qa/manaloom_android_design_audit_sm_a135m_2026-05-07.md`.

## Atualizacao - Android SM A135M nao-scanner PASS WITH RISKS - 2026-05-07

Rodada automatica no Android fisico `SM A135M`
(`R58T300SREH`, Android `14`, API `34`) usando o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`. Scanner/camera/OCR/MLKit
scanner ficaram explicitamente **DEFERRED/IGNORED**.

Resultado: **PASS WITH RISKS**. Passaram no device fisico: Sets catalog,
Search/Cards + Colecoes, Collection entrypoints, Binder dashboard,
Marketplace/Trades/Messages/Notifications, Profile/Community, deck
create/import/detail/optimize preview/apply/validate em intensidade `Focado`,
deck generate async/save/detail, Life Counter native surfaces, Lotus DOM runtime
e FCM smoke sem expor token. `flutter analyze lib test integration_test` e
`flutter test test` passaram apos os patches.

Bugs/harnesses corrigidos fora do Scanner:

- Optimize async job failure agora mostra copia amigavel em portugues em vez de
  texto tecnico do executor interno.
- Generate async respeita intervalo minimo de polling/backoff para evitar `429`.
- Harness de deck prova selecao parcial real e preservacao do comandante.
- Harness de Community aceita o titulo real do detalhe publico quando o app usa
  o nome do deck.
- Screenshot nativo Lotus virou evidencia nao-bloqueante; DOM/controles/
  persistencia continuam obrigatorios.

Riscos: optimize `Agressivo` segue **NOT PROVEN** porque o backend publico
retornou job async `failed`; Lotus screenshots PNG ficaram **NOT PROVEN** no
Android fisico, embora `lifeContentFits=true`, `horizontalOverflow=false`,
`40 -> 41 -> 40` e reopen em `41` tenham passado. Sentry DSN nao estava
configurado, entao os smokes registraram `not_configured/null`.

Relatorio:
`app/doc/runtime_flow_handoffs/android_sm_a135m_non_scanner_qa_2026-05-07.md`.

## Atualizacao - QA visual fisico nao-scanner PASS WITH RISKS - 2026-05-07

Rodada automatica no iPhone fisico `Rafa`
(`00008130-001C152922BA001C`, iOS `26.5 23F5043k`) usando o backend publico
`https://evolution-cartinhas.8ktevp.easypanel.host`. Scanner/camera/OCR/MLKit
scanner ficaram explicitamente fora do escopo.

Resultado: **PASS WITH RISKS**. O harness visual amplo via `flutter drive`
passou e salvou screenshots reais de Login, Register, Home, Decks, dialog de
criacao, Deck Detail, Generate, Generate sem preview sincrono, Community,
Collection e Profile em
`app/doc/runtime_flow_proofs_2026-05-06_physical_iphone_non_scanner_visual/app_full_screenshots/`.
Tambem passaram no device fisico os harnesses de Sets, Search/Colecoes,
Collection entrypoints e Binder/Marketplace/Trades/Messages/Notifications.

Bug visual simples corrigido: o dashboard do Binder apresentou
`RenderFlex overflowed by 38 pixels on the bottom` durante filtro por set no
iPhone fisico. A tela agora desfoca o teclado antes de aplicar filtros e troca
a linha dos CTAs vazios por `Wrap`, reduzindo pressao visual em tela estreita.
Validacao local nao-scanner: analyze focado PASS e golden/skin subset PASS.

Riscos restantes: runner fisico wireless instavel para alguns testes
(`Dart VM Service was not discovered`, Flutter Driver extension/timeout Xcode);
Optimize preview/apply/validate fisico e Lotus fisico ficaram **NOT PROVEN**
nesta rodada, embora as telas base e testes locais nao-scanner tenham passado.
Relatorio:
`app/doc/runtime_flow_handoffs/physical_iphone_visual_non_scanner_qa_2026-05-06.md`.

## Atualizacao - iPhone fisico VM Service/Firebase isolation PASS - 2026-05-06

Follow-up solicitado para validar se a tela branca vinha do Firebase iniciar
cedo demais. Ajuste aplicado:

- `AppObservability.bootstrap()` nao aguarda mais Sentry antes do `runApp`; a
  inicializacao Sentry acontece apos o primeiro frame e tem timeout.
- `main.dart` ganhou flags de QA fisico:
  `DISABLE_FIREBASE_STARTUP`, `DISABLE_PUSH_INIT` e
  `DISABLE_FIREBASE_PERFORMANCE_INIT`.
- `ApiClient` respeita as flags e nao cria metricas Firebase Performance quando
  Firebase startup esta desabilitado.

Validacao:

- Backend publico `/health`: `200`, `git_sha=1c89bb0e467fd422d84fa696e57a7f73d07618d3`.
- `flutter analyze lib test integration_test --no-version-check`: PASS.
- `flutter test test --no-version-check`: PASS, `549` testes.
- `flutter run` no iPhone fisico `Rafa` com
  `DISABLE_FIREBASE_STARTUP=true`: app chegou em `/login` e manteve Dart VM
  Service disponivel.
- `flutter test integration_test/sets_catalog_runtime_test.dart` no mesmo
  iPhone fisico com `DISABLE_FIREBASE_STARTUP=true`: PASS,
  `00:15 +1: All tests passed!`.
- Apos o guard final do `ApiClient`, analyze/testes focados seguiram PASS; uma
  repeticao fisica ficou bloqueada antes do app abrir por timeout do Xcode em
  `CONFIGURATION_BUILD_DIR`, nao pelo sintoma anterior de VM Service/Firebase.

Classificacao: **PASS para primeiro harness fisico nao-scanner com Firebase
isolado**. Isso confirma que a queda anterior do VM Service era evitavel ao
isolar startup nativo Firebase. Scanner/camera/OCR e entrega real de FCM seguem
como provas separadas.

## Atualizacao - Firebase FCM fisico BLOCKED por provisioning - 2026-05-06

Verificacao solicitada para Firebase no iPhone fisico `Rafa`
(`00008130-001C152922BA001C`, `iOS 26.5 23F5043k`).

O bundle do Firebase iOS em `GoogleService-Info.plist` bate com o app
(`com.mtgia.mtgApp`). O projeto iOS foi ajustado para declarar Push
Notifications, `aps-environment` por build config e `remote-notification`.
`flutter analyze` focado passou e `flutter build ios --debug --no-codesign`
passou.

Tentativa de smoke FCM no device fisico ficou **BLOCKED antes do app abrir**:
o provisioning atual e uma Apple Personal Team nao suportam a capability Push
Notifications, e o profile nao contem `aps-environment`. Portanto APNs/FCM
fisico nao pode ser provado neste ambiente ate usar uma Apple Developer Team
paga/provisioning com Push habilitado para `com.mtgia.mtgApp` e APNs
key/certificado configurado no Firebase Console. Evidencia sanitizada:
`app/doc/runtime_flow_proofs_2026-05-06_scanner_physical/firebase_physical_push_blocked.log`.

## Atualizacao - iPhone fisico boot branco mitigado - 2026-05-06

Follow-up do bloqueio do Scanner no iPhone fisico `Rafa`
(`00008130-001C152922BA001C`, `iOS 26.5 23F5043k`). A causa operacional mais
provavel da tela branca nao era OCR: o log anterior instalava o app, expunha
Dart VM Service e em seguida registrava `Lost connection to device`, sem prova
de primeira tela renderizada.

Patch aplicado em `app/lib/main.dart`: `runApp` agora acontece antes de
Firebase Push e Firebase Performance; esses servicos inicializam depois do
primeiro frame, com timeout/log/Sentry sanitizado. Resultado fresco no device
fisico com backend LAN `http://192.168.20.167:8082`: app renderizou o fluxo de
auth, roteou para `/login`, Firebase Push excedeu `8s` sem bloquear UI e
Firebase Performance inicializou depois.

Classificacao: **boot fisico mitigado / scanner fisico camera-OCR ainda NOT
PROVEN**. Proximo passo manual: logar no device, abrir Scanner, conceder camera
e rodar a matriz real de cartas. Handoff:
`app/doc/runtime_flow_handoffs/scanner_physical_audit_2026-05-06.md`.

## Atualizacao - Scanner fisico/camera/OCR BLOCKED - 2026-05-06

Rodada fresca com iPhone fisico `Rafa`
(`00008130-001C152922BA001C`, `iOS 26.5 23F5043k`) e backend local real na
porta `8082`. Resultado: **BLOCKED / NOT PROVEN** para fechar o ultimo deferred
de scanner fisico/camera/OCR.

| Marco | Evidencia |
| --- | --- |
| Device fisico | `flutter devices`, `idevice_id` e `xcrun xctrace` descobriram o iPhone fisico `Rafa`. |
| Backend | `PORT=8082 dart run .dart_frog/server.dart`; health `healthy` em `127.0.0.1`, `192.168.20.167` e `192.168.2.46`; backend parado no fim. |
| App no device | `flutter run -d 00008130-001C152922BA001C --debug --publish-port ...` instalou/lancou e expôs Dart VM Service. |
| Scanner controlado | `flutter analyze lib/features/scanner test/features/scanner` PASS; `flutter test test/features/scanner` PASS `+20`. |
| Token backend | `/cards/resolve include_tokens=true`, `/cards?include_tokens=true` e `/cards/printings?dedupe=false` retornaram somente printings token de `Phyrexian Horror`, sem cair em `Phyrexian Censor/Scissor`. |
| Bloqueio | A matriz manual com pelo menos 6 scans reais nao foi executada: sem permissao de camera validada, sem scanner screen fisico, sem frames MLKit, sem OCR real e sem add/retry/manual-search no device. |

Classificacao de release: manter scanner fisico/camera/OCR como
**DEFERRED / NOT PROVEN** ate uma sessao hands-on com token `Phyrexian Horror`,
carta de nome parecido, foil/reflexo, carta antiga, carta escura, carta com
edicoes multiplas e carta normal facil. Handoff:
`app/doc/runtime_flow_handoffs/scanner_physical_audit_2026-05-06.md`.

## Atualizacao - Firebase/Sentry release observability BLOCKED - 2026-05-06

Rodada segura no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`
(`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) preparou o smoke de
Firebase/Sentry para staging/TestFlight sem imprimir secrets. Resultado:
**BLOCKED** para prova TestFlight/internal iOS.

| Marco | Evidencia |
| --- | --- |
| Config Firebase | iOS `GoogleService-Info.plist` e Android `google-services.json` presentes; valores nao repetidos. |
| Config Sentry app | `SENTRY_DSN` local `MISSING`; `SENTRY_ENVIRONMENT`, `SENTRY_RELEASE` e sample rate suportados por dart-define. |
| Harness | `release_observability_smoke_test.dart` inicia UI minima, tenta evento Sentry controlado e inicializa Firebase Performance sem imprimir DSN/payload. |
| Runtime iPhone 15 | PASS; Sentry retornou `not_configured` por DSN ausente; Firebase Performance retornou `initialized` e collection `true`. |
| Build iOS | Archive `Runner.xcarchive` gerado; export IPA/ad-hoc bloqueado por falta de certificado iOS Distribution, provisioning profile e permissao de criar perfil. |
| Build Android | APK release local PASS; assinatura de distribuicao segue risco porque `key.properties` esta ausente e ha fallback debug local. |

Go/no-go: nao afirmar observabilidade de staging/TestFlight ate injetar DSN por
secret store, exportar/uploadar build assinado e conferir evento Sentry +
trace/session Firebase no dashboard. Scanner fisico/camera/OCR permaneceu fora
do escopo. Relatorio:
`server/doc/FIREBASE_SENTRY_RELEASE_OBSERVABILITY_2026-05-06.md`.

## Atualizacao - Lotus visual polish iPhone 15 PASS - 2026-05-06

Rodada fresca no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`
(`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) validou o Life Counter/Lotus
visual polish sem backend, porque o fluxo usa WebView Lotus embutido e stores
locais. Resultado: **PASS**.

| Marco | Evidencia |
| --- | --- |
| Risco investigado | Probe antigo marcava `lifeContentFits=false` e texto visivel vazio. |
| Causa | Falso negativo: Lotus desenha numeros por sprites CSS `.font.char-*`, entao `textContent` fica vazio; o fit antigo media um digito isolado. |
| Harness | `life_counter_lotus_visual_runtime_proof_test.dart` agora usa mensagens `debug_*`, decodifica `.font.char-*`, valida `40 -> 41 -> 40 -> reopen 41`, e exige `lifeContentFits=true`. |
| Runtime iPhone 15 | `flutter test integration_test/life_counter_lotus_visual_runtime_proof_test.dart -d "iPhone 15" --reporter expanded --no-version-check` PASS `00:27 +1`. |
| Visual | Screenshots com 4 jogadores, numeros visiveis, contraste OK, sem snackbar artificial, sem overflow horizontal e sem erro WebView. |
| Evidencia local | `app/doc/runtime_flow_proofs_2026-05-06_lotus_visual_polish_iphone15/` com screenshots inicial/after-plus e log sanitizado. |

Validacao adicional: analyze focado PASS; testes Lotus focados PASS `+21`; suite
deck obrigatoria de pre-commit PASS (`deck_runtime_widget_flow_test` e suite
details/provider/support/optimize `+81`). Sem crash, modal preso, raw 4xx/5xx,
timeout cru, JWT, `DATABASE_URL`, `SENTRY_DSN` ou payload sensivel.

## Atualizacao - Fresh optimize apply runtime PASS - 2026-05-06

Rodada fresca no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) contra backend local real `http://127.0.0.1:8082`. Resultado: **PASS** para o gap de Apply com sugestoes frescas do optimize.

| Marco | Evidencia |
| --- | --- |
| Backend/API preflight | Talrand Commander completo e saudavel; `intensity=focused`, `mode=optimize`, `outcome=optimized`, `swaps=7`, `elapsed_ms=33122`, `timings` e `stage_telemetry` presentes. |
| API apply parcial | Uma troca desmarcada, seis aplicadas; update `200`, validate `200`, total final `100`, comandante `Talrand, Sky Summoner` preservado. |
| Runtime iPhone 15 | `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ... RUNTIME_OPTIMIZE_INTENSITY_LABEL=Focado RUNTIME_OPTIMIZE_REQUIRE_APPLY=true RUNTIME_OPTIMIZE_FORCE_ARCHETYPE=control` PASS `01:31 +1`. |
| UI | Preview fresco exibido, uma sugestao desmarcada, `Aplicar mudanças`, final `10_complete_validated`. |
| Higiene | Sem crash, overflow, timeout cru, modal preso, raw 4xx/5xx, payload bruto, JWT, secrets, `DATABASE_URL`, `SENTRY_DSN` ou prompt completo exposto. |

Mudanca de harness: `integration_test/deck_runtime_m2006_test.dart` ganhou dart-defines opcionais para intensidade, obrigatoriedade de apply e estrategia salva forcada. Defaults preservam a prova aggressive existente; o modo requerido falha se o backend retornar `rebuild_guided` ou safe no-op.

Risco residual do release amplo: scanner fisico/camera/OCR segue **DEFERRED / NOT PROVEN**. O branch `Spellslinger` ainda pode cair em quality rejection seguro; para prova de apply fresco foi usado `control`, confirmado pelo backend como caminho acionavel seguro.

## Atualizacao - Final iPhone release QA after optimize upgrades - 2026-05-06

Rodada fresca de consolidacao no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) contra backend local real `http://127.0.0.1:8082`. Resultado: **PASS WITH RISKS**.

| Area | Resultado |
| --- | --- |
| Backend | `PORT=8082 dart run .dart_frog/server.dart`; `/health` healthy antes dos runtimes; backend encerrado ao final e porta 8082 livre. |
| Sanity app | `flutter analyze lib/features/decks lib/features/cards lib/features/collection lib/features/binder lib/features/trades test/features/decks --no-version-check` PASS; `flutter test test/features/decks --no-version-check` PASS `+153`. |
| Search/Sets | `sets_search_catalog_runtime_test.dart` PASS `00:18 +1`; buscou `Black Lotus`, abriu detalhe, buscou `ECC`, abriu `Lorwyn Eclipsed Commander` e voltou ao catalogo. |
| Deck Generate async | `deck_generate_async_runtime_test.dart` PASS apos ajuste de harness `01:04 +1`; registrou usuario, gerou async com feedback inicial em `459ms`, salvou deck, abriu detalhes e chegou ao branch produto `rebuild_guided` no optimize. |
| Optimize aggressive | `deck_runtime_m2006_test.dart` PASS `03:07 +1`; importou Commander completo, selecionou `Agressivo`, recebeu safe no-op/quality diagnostics amigavel, sem strings cruas `aggressive_candidate_quality`/`quality_gate_rejected` na UI. |
| Binder dashboard | `binder_dashboard_runtime_test.dart` PASS `00:37 +1`; dashboard, wishlist, add/edit/filter/delete de binder item. |
| Marketplace/trades/messages/notifications | `binder_marketplace_trade_runtime_test.dart` PASS `01:45 +2`; venda marketplace, proposta, lifecycle de trade, mensagens, notificacoes e DM/read receipt. |
| Life Counter/Lotus | `life_counter_lotus_visual_runtime_proof_test.dart` PASS `00:28 +1`; carregou Lotus, persistiu +/-, reabriu estado, sem erro WebView e sem overflow horizontal. |
| Validacao pre-commit | `deck_runtime_widget_flow_test.dart` PASS `+1`; suite focada de details/provider/optimize support PASS `+81`; analyze do harness alterado PASS. |

Mudanca de harness: `integration_test/deck_generate_async_runtime_test.dart` passou a aceitar os estados de produto `Criar reconstrução guiada` e `Nenhuma melhoria segura encontrada` depois de abrir optimize. A primeira tentativa falhava por timeout esperando apenas preview/apply, embora o deck gerado ja estivesse salvo e aberto; o ajuste evita tratar uma decisao segura do backend como falha de UI.

Riscos residuais:

- Apply com sugestoes live frescas **NOT PROVEN nesta consolidacao**, porque o backend retornou `rebuild_guided` no deck gerado e safe no-op/quality diagnostics no aggressive Talrand. A evidencia historica de 2026-05-05 continua cobrindo preview selecionavel/apply parcial quando ha swaps aprovados.
- Lotus passou no harness, mas o probe registrou risco visual de fit/texto vazio; manter como polish de UI, nao blocker desta rodada.
- Scanner fisico/camera/OCR permaneceu **DEFERRED / NOT PROVEN** por escopo.

Handoff detalhado: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-05-06.md`.

## Atualizacao - Aggressive no-op diagnostics UI runtime PASS WITH RISKS - 2026-05-06

Rodada fresca no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) contra backend local real `http://127.0.0.1:8082`. Resultado: **PASS WITH RISKS** para a nova UI de diagnostics em `Optimize -> Agressivo` quando o backend retorna safe no-op/quality rejected.

| Marco | Evidencia |
| --- | --- |
| Backend vivo | `/health` healthy em `127.0.0.1:8082`. |
| App checks | `flutter analyze lib/features/decks test/features/decks --no-version-check` PASS; `flutter test test/features/decks --no-version-check` PASS `+153`; analyze incluindo `integration_test/deck_runtime_m2006_test.dart` PASS. |
| Runtime iPhone 15 | `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` PASS `03:26 +1`. |
| Aggressive no-op | UI selecionou `Agressivo`; app enviou `POST /ai/optimize -> 202 (183ms)`; polling terminou em quality rejected seguro. |
| Diagnostics UI | Dialog mostrou copy de produto, nao payload bruto: candidatos analisados `74`, pares avaliados `37`, swaps seguros retornados `7`, principal bloqueio traduzido como limite da intensidade. |
| Sem regressao visual | Sem crash, overflow, modal preso, timeout cru, 4xx/5xx bruto, bucket tecnico ou `aggressive_candidate_quality` exposto ao usuario. |
| Encerramento | Backend temporario 8082 encerrado; porta livre no fim da rodada. |
| Evidencia local | `app/doc/runtime_flow_proofs_2026-05-06_iphone15_simulator/09_quality_rejected_blocker.png` e log `deck_runtime_m2006_test_iphone15_8082.log`. |

Risco residual: a resposta live nao trouxe `low_candidate_coverage=true`, entao a linha de baixa cobertura nao apareceu nesta rodada. O harness agora trata essa linha como opcional e continua exigindo os demais diagnostics amigaveis. Scanner fisico/camera/OCR permaneceu **DEFERRED / NOT PROVEN**.

## Atualizacao - Aggressive no-op diagnostics UI - 2026-05-06

O app agora transforma `optimize_diagnostics.aggressive_candidate_quality` em copy curta e amigavel quando `intensity=aggressive` termina sem swaps seguros ou com quality gate rejeitado. A UI nao exibe payload bruto nem nomes tecnicos de buckets: mostra contadores agregados de candidatos analisados, pares avaliados, swaps seguros retornados e o principal bloqueio traduzido para linguagem de produto.

| Area | Resultado |
| --- | --- |
| Parser/model | `AggressiveCandidateQualityDiagnostics` le diagnosticos opcionais de resposta sync, erro 422, failed job async e payload legacy sem tornar campos obrigatorios. |
| Safe no-op | Mensagem: "A IA encontrou ideias, mas o gate bloqueou as inseguras para preservar seu deck." |
| Low coverage | Quando `low_candidate_coverage=true`, a UI explica que faltaram candidatos seguros suficientes para aquele comandante/bracket. |
| Fallback | Sem diagnostics, o fluxo continua com mensagem amigavel existente, sem raw 4xx/5xx. |
| Backend async | Failed jobs de optimize preservam `quality_error.optimize_diagnostics` agregado para o polling mobile consumir, sem JWT, secrets, prompts completos ou payload bruto. |
| Runtime iPhone 15 | **NOT RUN nesta rodada**; sem backend local/simulador executado apos a mudanca. Scanner fisico/camera/OCR **DEFERRED**. |

Validacao: `flutter analyze lib/features/decks test/features/decks --no-version-check` PASS; `flutter test test/features/decks --no-version-check` PASS `+153`; `cd server && dart analyze routes/ai/optimize/index.dart` PASS.

## Atualizacao - Aggressive Candidate Quality runtime PASS WITH RISKS - 2026-05-06

Rodada fresca no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`) contra backend local real `http://127.0.0.1:8082`. Resultado: **PASS WITH RISKS** para compatibilidade mobile apos o backend consumir `card_function_tags`, `card_role_scores`, `commander_card_synergy` e `optimize_rejection_penalties` no caminho `intensity=aggressive`.

| Marco | Evidencia |
| --- | --- |
| Backend vivo | `/health` healthy em `127.0.0.1:8082`. |
| App checks | `flutter analyze lib/features/decks test/features/decks --no-version-check` PASS; `flutter test test/features/decks --no-version-check` PASS `+147`. |
| Runtime iPhone 15 | `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` PASS `02:58 +1`. |
| Aggressive | UI selecionou `Agressivo`; app enviou `POST /ai/optimize`, backend respondeu `202 (169ms)` e polling seguiu em `/ai/optimize/jobs/:id`. |
| Resultado UX | Quality gate retornou safe no-op; app exibiu estado seguro sem crash, overflow, timeout cru, modal preso ou raw 4xx/5xx. |
| Nao provado nesta rodada | Preview aplicavel, desmarcacao e apply parcial, porque nao houve swaps aprovados pelo gate. A prova anterior de preview/apply parcial segue como evidencia historica do contrato quando ha sugestoes. |

Classificacao de diagnostics: `optimize_diagnostics.aggressive_candidate_quality` deve permanecer fora da UI principal por enquanto. Se virar produto, usar copy derivada e amigavel para baixa cobertura/rejeicoes agregadas, nao os buckets tecnicos crus.

Scanner fisico/camera/OCR permaneceu **DEFERRED / NOT PROVEN**.

## Atualizacao - Optimize Intensity v2 mobile PASS - 2026-05-05

O app agora consome explicitamente o contrato `intensity` de `/ai/optimize` no bottom sheet de otimizacao. A UI exibe modos `Leve`, `Focado`, `Agressivo` e `Rebuild`, mantem fallback legacy para backend sem campo de intensidade, mostra explicacao de escopo/risco por modo e trata `rebuild_guided` como CTA de produto, nao erro cru.

### Atualizacao - aggressive async UX - 2026-05-05

O app passou a tratar `aggressive` como fluxo async quando o backend retorna `202 + job_id`. `DeckProvider.optimizeDeck` mostra progresso inicial "Optimize agressivo em background..." e faz polling por ate 5 minutos reais, calculando o numero de polls a partir de `poll_interval_ms`; isso corrige o timeout efetivo de 60s que aparecia quando o backend reduziu o intervalo para 1s.

Validacao adicional:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/decks test/features/decks --no-version-check` | PASS |
| `cd app && flutter test test/features/decks/screens/deck_details_screen_smoke_test.dart test/features/decks/providers/deck_provider_test.dart test/features/decks/widgets/deck_optimize_flow_support_test.dart --no-version-check` | PASS, `+46` |
| `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` | PASS WITH RISKS, `02:22 +1`; `POST /ai/optimize -> 202 (181ms)`, polling OK, quality gate rejeitou swaps e app mostrou safe no-op. |

Risco: nesta rodada async nao houve apply parcial porque o backend retornou `OPTIMIZE_QUALITY_REJECTED`. Isso e correto para seguranca, mas significa que a evidencia atual do app cobre UX async/progresso e safe no-op; a evidencia de preview parcial/apply/validate permanece na rodada Intensity v2 anterior.

Mudancas app:

| Area | Resultado |
| --- | --- |
| Provider/API | `DeckProvider.optimizeDeck` envia `intensity`; resposta legacy sem `intensity` resolve para `focused`; breadcrumbs sao sanitizados e nao incluem prompt, JWT, SENTRY_DSN, DATABASE_URL ou payload sensivel. |
| UI | Selector de intensidade com copy clara: light 3-5, focused 6-10, aggressive 10-20 com aviso, rebuild como reconstrucao guiada. |
| Preview/apply | Preview de optimize permite selecionar/desmarcar sugestoes; apply usa apenas swaps selecionados e preserva comandante pelos fluxos existentes de apply. |
| Erros | 4xx/5xx/timeouts passam por mensagens amigaveis; `OPTIMIZE_QUALITY_REJECTED` vira estado informativo de "nenhuma melhoria segura"; `rebuild_guided` abre CTA compreensivel. |
| Testes | Selector, fallback legacy, preview selecionavel, partial apply, quality rejection e needs_repair/rebuild copy cobertos em `test/features/decks`. |

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/decks test/features/decks --no-version-check` | PASS, sem issues |
| `cd app && flutter test test/features/decks --no-version-check` | PASS, `00:12 +145` |
| `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` | PASS, `02:41 +1` |

Evidencia iPhone 15 Simulator (`F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, iOS 17.4, backend `http://127.0.0.1:8082`):

| Marco | Evidencia |
| --- | --- |
| Backend vivo | `/health` healthy antes do runtime. |
| Selecao aggressive | Screenshot `08c_optimize_sheet_aggressive`; log sanitizado `intensity: aggressive`, `bracket: 2`, `keep_theme: true`. |
| Backend intensity | `POST /ai/optimize -> 200` em `108490ms`; `mode=optimize`, `outcome_code=optimized`, `optimize_intensity.selected=aggressive`, `returned_swaps=6` apos quality gate reduzir escopo. |
| Preview parcial | Screenshots `09_preview` e `09b_preview_partial_selection`; uma sugestao foi desmarcada antes de aplicar. |
| Apply final | Screenshot `10_complete_validated`; deck manteve `Talrand, Sky Summoner` e estado 100/100 valido. |

Resultado final: **PASS WITH RISKS**. O fluxo app usa `intensity`, preview/apply parcial foi provado em runtime real no iPhone 15 e nao houve crash, overflow, raw error, 4xx/5xx inesperado ou modal preso. Risco residual: latencia live de optimize agressivo ficou alta (~108s) e o quality gate reduziu o retorno para 6 swaps, abaixo do alvo nominal de aggressive (10-20), preservando seguranca.

## Atualizacao - App usando AI Generate async por padrao - 2026-05-05

O app agora envia `async=true` em `POST /ai/generate` por padrao, trata `202 + job_id`, faz polling em `GET /ai/generate/jobs/:id` e consome `result.generated_deck` com o mesmo shape do contrato sync. O caminho sync foi preservado para backend legacy (`200/422` direto) e fallback quando o backend sinaliza que async/polling nao e suportado.

Mudancas app:

| Area | Resultado |
| --- | --- |
| Provider | `DeckProvider.generateDeck` aceita progress callback, cancel token, timeout e poll interval; logs/breadcrumbs sanitizados nao incluem prompt completo. |
| UI Generate | Progresso claro: pedido aceito, tecendo lista, validando legalidade, ajustando mana, pronto para revisar. |
| Cancelamento | `DeckGenerateScreen.dispose()` cancela polling seguro ao sair da tela. |
| Save | Loading dialog de salvar agora fecha pelo root navigator, evitando barreira modal residual apos criar deck gerado. |
| Testes | `flutter analyze lib/features/decks test/features/decks --no-version-check` PASS; `flutter test test/features/decks --no-version-check` PASS `+142`. |

Evidencia iPhone 15 Simulator (`F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, iOS 17.4, backend `http://127.0.0.1:8082`):

| Fluxo | Resultado |
| --- | --- |
| Generate async focado | `POST /ai/generate -> 202` em `802ms`; UI mostrou feedback inicial em `547ms`; polling concluiu com `result_status=200` em `15849ms`; deck salvo, aberto em detalhes e validado. |
| Optimize/apply existente | `integration_test/deck_runtime_m2006_test.dart` PASS `01:27 +1`, screenshot `10_complete_validated`, sem `4xx/5xx`, overflow ou crash. |
| Sanity live server | `TEST_API_BASE_URL=http://127.0.0.1:8082 dart test test/ai_generate_create_optimize_flow_test.dart --tags live -r expanded` PASS `01:45 +2`. |

Risco observado: ao otimizar diretamente um deck recem-gerado no harness focado, `/ai/optimize` retornou `422 needs_repair` e o app acionou `/ai/rebuild` com sucesso, em vez de apresentar sugestoes aplicaveis. O fluxo de preview/apply/validate segue provado pelo runtime existente; o menor proximo ajuste e escolher uma estrategia/arquetipo mais aderente ao deck gerado ou aceitar explicitamente o branch de rebuild no harness focado.

Scanner fisico/camera/OCR permaneceu fora do escopo e `NOT PROVEN`.

## Atualizacao - AI Generate v2 async opt-in backend - 2026-05-05

O app nao foi alterado nesta sprint: `DeckProvider.generateDeck` continua usando o contrato sync de `/ai/generate`, que foi preservado. O backend agora oferece um caminho async opt-in para futura melhoria de UX/progresso sem quebrar o app atual.

Evidencia backend/app-contract:

| Cenario | Resultado |
| --- | --- |
| `/ai/generate` sync frio v2 | `200x10`, p95/p99 `11212ms` |
| `/ai/generate` cache hit v2 | `200x10`, p95/p99 `7ms` |
| `/ai/generate` async accepted v2 | `202x10`, accepted p95/p99 `562ms` |
| `/ai/generate/jobs/:id` | `completedx10`; resultado final tem o mesmo shape do sync em `result` |
| Live generate/create/validate/optimize | PASS: `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test test/ai_generate_create_optimize_flow_test.dart --tags live -r expanded` -> `01:41 +2` |

Leitura para o app:

- Nenhum consumidor Flutter precisa mudar agora.
- Quando a UI passar a usar async, deve tratar `202` como estado de progresso, poll em `poll_url` e consumir `result.generated_deck` como fonte de verdade.
- Scanner/social/binder/sets/life counter nao foram tocados nesta sprint.

## Atualizacao - TestFlight/internal sanity READY WITH RISKS apos AI Generate latency - 2026-05-05

Rodada curta executada em `master` base `40fe6ab`, com backend local real `http://127.0.0.1:8082` e iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`). Resultado: `READY WITH RISKS` para build interno/TestFlight sem scanner fisico. Scanner fisico/camera/OCR permaneceu `DEFERRED / NOT PROVEN`.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test test/ai_generate_create_optimize_flow_test.dart --tags live -r expanded` | PASS apos fix de gate: `01:59 +2` |
| `cd app && flutter analyze lib/features/decks test/features/decks --no-version-check` | PASS sem issues |
| `cd app && flutter test test/features/decks --no-version-check` | PASS: `00:09 +135` |
| `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` | PASS: `01:16 +1`, final `10_complete_validated` |

Performance/risco:

| Endpoint | Statuses | p50 | p95 | p99 | Leitura |
| --- | ---: | ---: | ---: | ---: | --- |
| `POST /ai/generate` pos-patch | `200x5` | `10433ms` | `13005ms` | `13005ms` | Abaixo do alvo sprint `<15000ms`; risco monitorado por dependencia OpenAI/fallback/DB. |
| `POST /ai/generate` cache probe | `200x1` | `3ms` | `3ms` | `3ms` | Cache hit operacional, sem prompt em `cache_key`. |

Observacoes:

- Runtime deck cobriu register/login, generate/create, detail, optimize async, preview, apply e validate contra backend real, sem crash, overflow, timeout/socket ou runtime `4xx/5xx`.
- O primeiro backend sanity encontrou `/ai/optimize` retornando 200 com `validation_score=68`; o gate final foi ajustado para tratar score `<70` como rejeicao/retry e o teste live passou em seguida.
- Sentry/logs: nenhum erro novo foi registrado na rodada; logs locais ficam em `app/doc/runtime_flow_proofs_2026-05-05_iphone15_simulator/` e nao devem ser commitados.
- Backend 8082 foi encerrado ao final da validacao e a porta ficou livre.

## Atualizacao - Internal release/staging READY WITH RISKS sem scanner fisico - 2026-05-04

Rodada final de preparo para release interno/staging executada em `master` no commit `85b4200`, com backend local real `http://127.0.0.1:8082` e iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`). Resultado: `READY WITH RISKS for internal/staging only`. Scanner fisico/camera/OCR permaneceu explicitamente `DEFERRED / NOT PROVEN` e nao foi executado.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd server && dart analyze lib routes bin test && dart test -r expanded` | PASS: `+558` |
| `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test -P live -r expanded` | PASS: `+167 ~3` |
| `cd app && flutter analyze lib test integration_test --no-version-check && flutter test test --no-version-check` | PASS: `+530` |
| `flutter test integration_test/sets_catalog_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:17 +1` |
| `flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:28 +1` |
| `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` | PASS: `01:38 +1`, final `10_complete_validated` |
| `flutter test integration_test/binder_dashboard_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:43 +1` |
| `flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `01:50 +2` |
| `flutter test integration_test/life_counter_lotus_visual_runtime_proof_test.dart -d "iPhone 15" ...8082` | PASS: `00:28 +1` |
| `flutter test integration_test/app_full_non_life_counter_visual_capture_smoke_test.dart -d "iPhone 15" ...8082` | PASS: `01:02 +1` |

Config/env readiness:

- `API_BASE_URL` e `PUBLIC_API_BASE_URL`: configurados por dart-define nos runtimes; obrigatorios em release/profile porque o fallback release do app e vazio.
- Firebase iOS/Android: `PRESENT`, valores nao expostos.
- App Sentry: suportado por dart-define, mas `NOT CONFIGURED` na rodada de runtime.
- Server Sentry: `PRESENT` e inicializado no backend, sem DSN exposto.
- FCM server env: `MISSING` localmente para credenciais de envio; push delivery fica setup de staging se virar requisito.
- Camera permissions iOS/Android: `PRESENT`, mas scanner fisico segue fora do escopo.
- Version/build: `1.0.0+1`; bundle id iOS `com.mtgia.mtgApp`; Android applicationId `com.mtgia.mtg_app`; flavors explicitos `NOT CONFIGURED`.
- Signing/upload: Android `key.properties` local `MISSING`; iOS export/signing local incompleto. Builds internos devem usar CI/local seguro antes de upload.

Performance:

| Endpoint | Statuses | p50 | p95 | p99 | Leitura |
| --- | ---: | ---: | ---: | ---: | --- |
| `POST /ai/generate` | `200x5` | `24293ms` | `44756ms` | `44756ms` | Fora do risco aceito anterior; P1 antes de rollout amplo/producao. |
| `POST /ai/optimize` | `202x5` | `4786ms` | `5029ms` | `5029ms` | Dentro do risco aceito; jobs concluidos. |

Observacoes:

- Runtimes iPhone 15 ficaram limpos para exception Flutter, overflow, timeout/socket e `500` residual.
- Firebase Performance seguiu `NOT PROVEN` na sessao de integration test por indisponibilidade do default app/HTTP metrics.
- Backend temporario `8082` foi encerrado ao final e a porta ficou livre.
- Handoff completo: `server/doc/INTERNAL_RELEASE_STAGING_HANDOFF_2026-05-04.md`.
- Proof folder local ignorado: `app/doc/runtime_flow_proofs_2026-05-04_iphone15_simulator/`.

## Atualizacao - Pre-release QA PASS WITH RISKS sem scanner fisico - 2026-05-04

Rodada pre-release executada em `master` com backend local real `http://127.0.0.1:8082` e iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`). Resultado geral: `PASS WITH RISKS`; scanner fisico/camera/OCR ficou explicitamente `DEFERRED / NOT PROVEN`.

Validacao:

| Comando | Resultado |
| --- | --- |
| `flutter test` focado de decks (`deck_runtime_widget_flow`, smoke/provider/support/optimize) | PASS: `67 passed` |
| `flutter analyze lib/features/cards lib/features/collection test/features/cards test/features/collection` | PASS |
| `flutter test test/features/cards test/features/collection` | PASS: `7 passed` |
| `flutter test integration_test/sets_catalog_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:32 +1` |
| `flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:35 +1` |
| `flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...8082` | PASS: `01:38 +1`, final `10_complete_validated` |
| `flutter test integration_test/binder_dashboard_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `00:59 +1` |
| `flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" ...8082` | PASS: `01:51 +2` |
| `flutter test integration_test/life_counter_lotus_visual_runtime_proof_test.dart -d "iPhone 15" ...8082` | PASS no retry: `00:27 +1`; primeira tentativa falhou por build/DDS concorrente |
| `flutter test integration_test/app_full_non_life_counter_visual_capture_smoke_test.dart -d "iPhone 15" ...8082` | PASS apos patch de labels do harness visual: `01:05 +1` |

Observacoes:

- App/backend core PASS: login/register, Search Cards/Colecoes, Set detail, deck create/detail/import/optimize/apply/validate, Binder dashboard, Marketplace, Trades, Messages, Notifications e Life Counter/Lotus.
- Performance medida em 5 amostras por endpoint: `/ai/generate` p95 `10203ms`, `/ai/optimize` p95 `4825ms`, `/community/marketplace` p95 `629ms`, `/trades` p95 `602ms`, `/trades/:id` p95 `1227ms`, `/binder` p95 `603ms`, `/cards` p95 `1126ms`, `/sets` p95 `702ms`.
- Observabilidade validada por breadcrumbs app `api_slow_request`, backend `http_observability` e `social_notification slow_deferred`; Firebase Performance ficou indisponivel na sessao de integration test por falta de Firebase default app.
- Foi aplicado patch pequeno e seguro no harness `app_full_non_life_counter_visual_capture_smoke_test.dart` para usar os labels atuais `Gerar proposta` e `Preview antes de salvar`.
- Evidencias: `server/doc/RELATORIO_PRE_RELEASE_QA_2026-05-04.md`, `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-05-04.md` e proof folder ignorado `app/doc/runtime_flow_proofs_2026-05-04_iphone15_simulator/`.

## Atualizacao - Final Regression app/backend PASS com scanner fisico NOT PROVEN - 2026-05-04

Rodada final de regressao executada em `master` contra backend local real `http://127.0.0.1:8082` e iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` (`com.apple.CoreSimulator.SimRuntime.iOS-17-4`). Resultado geral: `PASS` para backend, app, AI generate/optimize, decks, binder, trades, sets e Life Counter/Lotus; scanner fisico ficou `NOT PROVEN` porque nao houve execucao com camera/OCR em device fisico.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd server && dart analyze lib routes bin test && dart test -r expanded` | PASS: analyze sem issues; `00:04 +558` |
| `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test -P live -r expanded` | PASS: `02:49 +167 ~3` |
| `cd app && flutter analyze lib test integration_test --no-version-check && flutter test test --no-version-check` | PASS: analyze sem issues; `00:41 +530` |
| `cd app && flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: `/cards`, `/sets`, `/cards?set=ECC` com 200; `00:25 +1` |
| `cd app && flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: `01:24 +1`, screenshot final `10_complete_validated` |
| `cd app && flutter test integration_test/binder_dashboard_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: binder CRUD/stats com 200/201/204; `00:37 +1` |
| `cd app && flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: marketplace/trade/messages/notifications; `01:47 +2` |
| `cd app && flutter test integration_test/life_counter_lotus_visual_runtime_proof_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: Lotus ready/reopen; `00:29 +1` |

Observacoes:

- Backend health respondeu `healthy` em `http://127.0.0.1:8082/health`.
- Nenhum 4xx/5xx, timeout, overflow ou crash permaneceu nos runtimes iPhone 15 aprovados.
- Logs de 401/403/404/500 vistos em `flutter test test` sao mocks esperados de testes unitarios de erro, nao regressao runtime.
- Avisos locais de Xcode sobre pods sem suporte `arm64` para simuladores Apple Silicon iOS 26+ nao bloquearam o iPhone 15 iOS 17.4, mas seguem como risco ambiental.
- Evidencias: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-05-04.md`, `server/doc/RELATORIO_FINAL_REGRESSION_2026-05-04.md` e proof folder local `app/doc/runtime_flow_proofs_2026-05-04_iphone15_simulator/`.

## Atualizacao - Marketplace/Trades Trust Intelligence PASS - 2026-05-04

Sprint Marketplace/Trades Trust Intelligence aplicada em `master` usando somente dados internos do backend. O app mobile continua sem consulta a Scryfall, MTGJSON ou APIs externas em runtime; preco/tendencia/confiança vêm de `/community/marketplace`, `/trades` e `/trades/:id`.

Mudancas principais:

- Marketplace agora exibe preco de referencia interno quando `cards.price` existe, tendencia simples por `price_history` quando ha historico suficiente e estado honesto de dados insuficientes quando falta base.
- Cards do marketplace mostram alerta quando o preco anunciado no binder fica muito acima/abaixo da referencia interna, sem inventar preco quando a base nao existe.
- Marketplace, inbox e detalhe de trade mostram indicadores simples de confianca: trades concluidos, cancelamentos/recusas/disputas, tempo medio de resposta/envio quando calculavel, conta nova, perfil incompleto e historico insuficiente.
- Criacao e detalhe de trade exibem resumo de desequilibrio de valor com diferenca absoluta/percentual e aviso quando ultrapassa o limite configurado, usando apenas `trade_items.agreed_price` e `payment_amount`.
- Timeline do trade ficou mais clara para criada, aceita, enviada, entregue, finalizada, cancelada/recusada/disputada; mensagens do trade agora aparecem como contexto preso ao trade.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd server && dart analyze routes/market routes/trades routes/community routes/users lib test` | PASS |
| `cd server && dart test -r expanded` | PASS |
| `cd app && flutter analyze lib/features/market lib/features/trades lib/features/binder lib/features/profile test/features/market test/features/trades test/features/binder --no-version-check` | PASS |
| `cd app && flutter test test/features/market test/features/trades test/features/binder --no-version-check` | PASS: `00:05 +18` |
| Backend `PORT=8082 dart run .dart_frog/server.dart` + `/health` | PASS: healthy em `http://127.0.0.1:8082` |
| Probes reais de marketplace/trades/trust/price trend | PASS: `price_insight`, `owner.trust`, `sender/receiver.trust`, `value_summary` e mensagens retornaram por endpoints internos |
| `cd app && flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: `01:45 +2` |

Runtime iPhone 15: `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, runtime `com.apple.CoreSimulator.SimRuntime.iOS-17-4`, backend real `http://127.0.0.1:8082`. Evidencia: `app/doc/runtime_flow_handoffs/marketplace_trades_trust_runtime_2026-05-04.md`.

Android fisico opcional ficou `not proven`: nenhum device fisico foi necessario para aceitar a sprint; se aparecer em QA, usar `adb reverse tcp:8082 tcp:8082`.

## Atualizacao - Binder/Fichario Dashboard PASS - 2026-05-04

Sprint Binder/Fichario Dashboard aplicada em `master` para transformar colecao em valor claro sem tocar Life Counter/Lotus, Scanner, meta pipeline, optimize/generate core, FCM, secrets, release build ou assets oficiais de MTG.

Mudancas principais:

- `/binder/stats` agora retorna valor estimado total, total de cartas, cartas unicas, duplicadas, wishlist, faltantes, itens sem preco, cartas usadas em decks, progresso por colecao/set e distribuicoes por raridade, foil/non-foil, condicao e idioma.
- `/binder` passou a aceitar filtros/ordenacao opcionais e compatíveis por set, raridade, idioma, foil/non-foil, faixa de preco, nome, condicao, troca/venda, quantidade, preco e `updated_at`.
- Fichario no app ganhou dashboard compacto e rolavel com resumo, progresso por colecao, wishlist/faltantes e distribuicoes; a lista continua com states amigaveis e sem erro tecnico cru.
- Itens do binder passaram a carregar `deck_count/deck_quantity`, datas e preco de mercado opcional quando o backend envia.
- Novo harness `integration_test/binder_dashboard_runtime_test.dart` cobre Collection -> Fichario -> dashboard -> add/edit/delete -> filtros -> stats atualizados contra backend real.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd server && dart analyze routes/binder routes/cards routes/sets lib test` | PASS |
| `cd server && dart test -r expanded` | PASS: `00:04 +556` |
| `cd app && flutter analyze lib/features/binder lib/features/collection lib/features/cards test/features/binder test/features/collection test/features/cards --no-version-check` | PASS |
| `cd app && flutter test test/features/binder test/features/collection test/features/cards --no-version-check` | PASS: `00:03 +11` |
| Backend `PORT=8082 dart run .dart_frog/server.dart` + `/health` | PASS: healthy em `http://127.0.0.1:8082` |
| Probes reais `/binder`, `/binder/stats`, `/sets`, `/cards?set=ECC` | PASS, contratos retornaram 200/201 e stats ricos |
| `cd app && flutter test integration_test/binder_dashboard_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` | PASS: `00:36 +1` |

Runtime iPhone 15: `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, runtime `com.apple.CoreSimulator.SimRuntime.iOS-17-4`, backend real `http://127.0.0.1:8082`. Evidencia: `app/doc/runtime_flow_handoffs/binder_dashboard_runtime_2026-05-04.md`.

Android fisico opcional ficou `not proven`: `adb devices` nao listou `R58T300SREH` e `adb -s R58T300SREH reverse tcp:8082 tcp:8082` retornou `device not found`.

## Atualizacao - Deck Detail Validate Meta Intelligence PASS - 2026-04-30

Sprint Deck Detail + Validate Deck + Meta Intelligence UI executada em `master` com foco em tornar o pipeline Commander/meta/IA compreensivel para o usuario sem alterar contratos JSON.

Mudancas principais:

- Deck Detail ganhou resumo Commander perto do topo com status de comandante, identidade de cor, contagem `atual/100`, preco quando disponivel e sinal de curva/analise.
- Validacao de deck agora traduz falhas comuns para explicacoes visuais amigaveis: comandante ausente, deck abaixo/acima de 100, fora da identidade de cor, quantidade invalida/singleton, carta banida/nao legal e erro generico de regra, sempre com acao sugerida.
- Optimize/Generate diferenciam proposta revisavel, ajuste leve, rebuild guiado e sinal competitivo/cEDH; o preview continua obrigatorio antes de aplicar.
- Optimize preview consome `meta_reference_context` ja existente no backend e mostra referencias meta usadas, fonte, escopo/bracket, shell/arquetipo, motivo estrategico e aviso de que meta e referencia, nao copia cega.
- O sheet de otimizacao ganhou fallback visivel de ajuste leve quando `/ai/archetypes` retorna sem opcoes, evitando vazio/timeout no iPhone 15.
- Apply/validate passou a interpretar `ok: true`, `valid: true` e `is_valid: true` como sucesso, mantendo compatibilidade com o contrato atual de `POST /decks/:id/validate`.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/decks lib/features/cards test/features/decks test/features/cards --no-version-check` | PASS sem issues |
| `cd app && flutter test test/features/decks test/features/cards --no-version-check` | PASS: `00:17 +137: All tests passed!` |
| `cd server && dart analyze routes/decks routes/ai lib/ai lib/meta test` | PASS sem issues |
| `cd server && dart test -r expanded` | PASS: `00:04 +556: All tests passed!` |
| `cd server && PORT=8082 dart run .dart_frog/server.dart` + `/health` | PASS: backend temporario healthy em `http://127.0.0.1:8082` |
| `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart run bin/run_commander_only_optimization_validation.dart --dry-run` | PASS: 19 candidatos dry-run, sem executar auth/deck/optimize/bulk/validate |
| `cd app && flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" ...` | PASS: `01:13 +1: All tests passed!`, screenshot final `10_complete_validated` |

Evidencias: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-30.md`, `app/doc/runtime_flow_proofs_2026-04-30_deck_meta_validate/` e `server/doc/RELATORIO_COMMANDER_OPTIMIZE_FLOW_AUDIT_2026-04-30.md`.

## Atualizacao - Scanner fisico Android OCR/token PARTIAL PASS - 2026-04-30

Sprint de scanner executada em `master` com backend local real `http://127.0.0.1:8082`, `adb reverse tcp:8082 tcp:8082` e device fisico `SM A135M` / `R58T300SREH` (Android 14). Resultado: `PARTIAL physical PASS / controlled logic PASS`.

Mudancas principais:

- `ScannerGuideGeometry` passou a ser fonte unica do retangulo visual do guia, usado tanto pelo overlay quanto pelo calculo de ROI da camera.
- ROI de OCR ficou mais estrito por overlap real com o guia, reduzindo texto de embalagem/pedido ao redor.
- Ranking/parser penalizam texto externo (`pedido`, `pagamento`, SKU, preco, endereco/cidade/frete) e tratam `Token Artifact Creature ...` como type line, nao como nome.
- Parser deixou de usar `1/1` de texto de regras como collector; normal rules text que menciona criar token segue `isToken=false`.
- Confirmacao live agrupa variacoes OCR proximas (`PuYREXIAN HORROR` vs `Phyrexian Horror`) antes de chamar backend.
- `/cards` passou a expor `collector_number`/`foil` e aceitar `include_tokens=true` para priorizar tokens.
- `/cards/printings` passou a aceitar `dedupe=false`; o scanner usa esse modo para preservar multiplas printings/foil.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/scanner test/features/scanner --no-version-check` | PASS sem issues |
| `cd app && flutter test test/features/scanner --no-version-check` | PASS: `20 passed` |
| `cd server && dart analyze routes/cards routes/cards/resolve test/card_resolution_support_test.dart` | PASS sem issues |
| `cd server && dart test test/card_resolution_support_test.dart` | PASS: `6 passed` |
| `POST /cards/resolve {"name":"Phyrexian Horror","include_tokens":true}` | PASS: `Token Artifact Creature — Phyrexian Horror`, `total_returned=3`, nunca `Phyrexian Censor` |
| `flutter run -d R58T300SREH ...` | PARTIAL PASS: camera/MLKit/OCR real, `Phyrexian Horror` confirmado, uma chamada `/cards/printings` apos confirmacao estavel |
| `flutter test integration_test/scanner_controlled_harness_runtime_test.dart -d "iPhone 15" ...` | PASS: `00:05 +1: All tests passed!` |

Evidencias: `app/doc/runtime_flow_handoffs/scanner_physical_audit_2026-04-30.md`, `app/doc/runtime_flow_handoffs/scanner_runtime_2026-04-29.md` e `app/doc/runtime_flow_proofs_2026-04-30_scanner_physical/`.

Pendencias fisicas: matriz completa com carta normal bem iluminada, multiplas edicoes fisicas, foil fisico, carta parcialmente fora do guia e baixa luz/reflexo. A rodada fisica comprovou camera/OCR/token/backend, mas esses cenarios permanecem para execucao manual assistida.

## Atualizacao - Life Counter/Lotus visual runtime iPhone 15 PASS - 2026-04-30

Sprint Life Counter/Lotus visual proof executada no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, runtime `com.apple.CoreSimulator.SimRuntime.iOS-17-4`, com backend local vivo em `http://127.0.0.1:8081` (`/health` healthy) e app configurado por `API_BASE_URL`/`PUBLIC_API_BASE_URL`.

Novo harness: `app/integration_test/life_counter_lotus_visual_runtime_proof_test.dart`.

Comandos principais:

```bash
cd app && flutter analyze lib/features/home test/features/home integration_test --no-version-check
cd app && flutter test test/features/home --no-version-check
cd app && flutter test integration_test/life_counter_lotus_visual_runtime_proof_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8081 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8081 --reporter expanded --no-version-check
```

Resultados: analyze `PASS`, home tests `PASS`, runtime `PASS` com `00:31 +1: All tests passed!`.

Prova real: `LotusLifeCounterScreen`/WKWebView abriu sem erro, 4 jogadores e controles `+1/-1` apareceram, numero principal apresentou caixa renderizada grande, cor clara e text shadow, sem overflow horizontal; `+1` alterou `40 -> 41`, `-1` alterou `41 -> 40`, estado final `41` persistiu e foi restaurado no reopen. Nenhum crash, timeout, overflow ou erro WebView permaneceu.

Evidencias: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-30.md` e `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_life_counter_lotus/` com log sanitizado e screenshots `life_counter_lotus_runtime_initial.png` / `life_counter_lotus_runtime_after_plus.png`.

P2/P3 restantes: PT-BR/copy dos overlays Lotus, profiling de blur/CSS se houver jank, cobertura visual adicional para 2P/6P/sheets e decisao de produto sobre o grau de linguagem visual propria do Life Counter.

## Atualizacao - P1 visual fora de Trades iPhone 15 PASS - 2026-04-30

Sprint visual P1 executada sobre Home, Decks, Deck Detail, Generate/Optimize/Validate, Binder/Fichario, Marketplace, Search/Cards e Sets/Colecoes. Contratos JSON, rotas backend, meta pipeline, scanner/OCR, Life Counter/Lotus, secrets, assets oficiais de MTG e release builds foram preservados.

Mudancas principais:

- Home re-hierarquizada por intencao: `Jogar agora`, `Construir deck`, `IA de decks`, `Minha colecao`, `Trocas e mercado`.
- Deck Detail ganhou surface de confianca para legalidade/validacao perto do topo, com comandante, contagem e CTA `Validar agora` quando aplicavel.
- Generate com IA agora explica que a IA gera proposta revisavel antes de salvar; CTA virou `Gerar proposta` e save virou `Salvar deck revisado`.
- Optimize preview ganhou bloco `Controle antes de aplicar` com plano, mudancas, cartas depois e terrenos; copy tecnica de debug ficou restrita a debug mode.
- Binder ganhou resumo com total, unicas, duplicadas, troca, venda e valor estimado quando stats existem.
- Marketplace ganhou header de confianca e cards mais auditaveis com quantidade, condicao, idioma, set, preco, owner/localizacao/notas e CTA unico.
- Search/Cards e Sets/Colecoes receberam ajustes de Frost para busca/filtro/suporte, empty states mais orientativos e correcoes de contraste/overflow.
- `DeckMetaChip` passou a truncar labels longos com ellipsis, corrigindo overflow real em Deck Detail estreito.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/home lib/features/decks lib/features/cards lib/features/collection lib/features/binder lib/features/market lib/core test --no-version-check` | PASS sem issues |
| `cd app && flutter test test/features/home test/features/decks test/features/cards test/features/collection test/features/binder test/features/market test/core --no-version-check` | PASS: `00:23 +463: All tests passed!` |
| `flutter devices` | iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, iOS 17.4 |
| `xcrun simctl list devices available \| grep -E "iPhone 15\|Booted"` | iPhone 15 `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` Booted |
| `PORT=8082 dart run .dart_frog/server.dart` + `/health` | PASS: backend temporario healthy em 8082 |
| `flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" ...` | PASS: `00:18 +1: All tests passed!` |

Runtime iPhone 15 em backend real `http://127.0.0.1:8082` provou Search/Cards -> Colecoes -> Set detail com `GET /cards?name=Black+Lotus 200`, `GET /sets 200`, `GET /sets?q=ECC 200`, `GET /cards?set=ECC 200`. Evidencias: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_visual_p1/`; handoff atualizado: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-30.md`.

Resultado final: `PASS` para a sprint visual P1 fora de Trades nos testes focados e no runtime Search/Sets. P2/P3 pendentes: Search global/Meta surface como decisao de produto, screenshots visuais de Home/Deck/IA/Binder/Marketplace, auditoria de aliases legados fora do escopo tocado, Life Counter/Lotus e Scanner.

## Atualizacao - P1 UX trust Social Trading iPhone 15 PASS - 2026-04-30

Runtime reaberto no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, runtime `com.apple.CoreSimulator.SimRuntime.iOS-17-4`, contra backend real `http://127.0.0.1:8082`. Resultado: `PASS` para o harness `integration_test/binder_marketplace_trade_runtime_test.dart` depois de desbloquear o build iOS Simulator e corrigir um crash real do dialog de envio.

Evidencias:

- Handoff: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-30.md`.
- Logs/provas locais: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_social_trading_ux_trust/`.
- Runtime iPhone 15: `iphone15_runtime_validation.log`, resultado `01:44 +2: All tests passed!`.
- Device discovery: `flutter_devices.log` e `simctl_devices.log` confirmam iPhone 15 bootado.
- Health backend: `backend_health.json`.

Fluxos provados em UI real + backend real:

| Fluxo | Status | Evidencia de runtime |
| --- | --- | --- |
| Binder entrypoints create/edit/delete | `PASS` | `POST /binder 201`, `PUT /binder/:id 200`, `DELETE /binder/:id 204` |
| Marketplace search | `PASS` | `GET /community/marketplace 200` |
| Review de proposta | `PASS` | Dialog `Revisar proposta` confirmado antes de `POST /trades` |
| Criacao de trade | `PASS` | `POST /trades 201` |
| Seller aceitar | `PASS` | Dialog `Aceitar trade?`, `PUT /trades/:id/respond 200` |
| Chat de trade | `PASS` | `POST /trades/:id/messages 201`, mensagens recarregadas |
| Seller enviar | `PASS` | Dialog `Confirmar envio`, `PUT /trades/:id/status 200` para `shipped` |
| Buyer confirmar entrega | `PASS` | Dialog `Confirmar entrega?`, `PUT /trades/:id/status 200` para `delivered` |
| Buyer finalizar | `PASS` | Dialog `Finalizar trade?`, `PUT /trades/:id/status 200` para `completed` |
| Notificacoes | `PASS` | Listar, abrir/read e read-all com `200` |
| Mensagens diretas | `PASS` | Conversa, envio, leitura e unread count cobertos |

Mudancas relevantes:

- Build iOS Simulator: `MLImage 1.0.0-beta8` traz slice `arm64` de device iOS; o Runner tentava linkar isso em `iOS-simulator`. `Debug.xcconfig`, `Release.xcconfig` e `Podfile` agora excluem `arm64 i386` apenas para `iphonesimulator*`, forçando `x86_64` no simulator e preservando device iOS fisico.
- Harness: alinhado ao dialog atual de envio (`Confirmar envio`) e tap no `ElevatedButton` correto.
- App runtime: `TradeDetailScreen` passou a manter o `TextEditingController` do envio dentro do ciclo de vida de um dialog stateful privado, evitando assertion/crash por controller usado apos `dispose`.

Sem 4xx/5xx inesperado, timeout, overflow ou crash na rodada final. O Flutter ainda emite aviso de que pods nao suportam `arm64` simulator em Apple Silicon/iOS 26+; nesta prova o runtime iPhone 15 iOS 17.4 executou via `x86_64` e passou.

## Atualizacao - P1 UX trust, erros amigaveis e trades - 2026-04-30

Sprint aplicada no app para reduzir ansiedade do usuario em falhas e aumentar seguranca em acoes criticas de troca. Contratos JSON/endpoints/rotas backend foram preservados; nao houve alteracao em Life Counter/Lotus, Sets pipeline, meta pipeline, optimize/generate core, scanner ou FCM.

Mudancas principais:

- Criado `FriendlyErrorMapper` central em `app/lib/core/utils/friendly_error_mapper.dart` para transformar status code, timeout, erro de rede, `Exception`, `RequestOptions`, `SocketException`/`ClientException` e mensagens tecnicas em copy amigavel.
- Aplicado em Auth/Login/Register/Profile, Deck Generate/Save, Deck Details/Validate/Pricing, Sets/Colecoes, Trades/Create/Detail/Message, Binder e Marketplace, mantendo logs/observabilidade internos via `ApiClient`, `debugPrint` e `AppObservability` onde ja existiam.
- `TradeDetailScreen` agora exige confirmacao contextual para aceitar, recusar, cancelar, marcar como enviado, confirmar entrega, finalizar e disputar, com resumo de trade, itens e valores quando disponiveis.
- `CreateTradeScreen` ganhou review final antes de enviar proposta, listando itens, quantidade, condicao, idioma, valores e aviso quando ha diferenca relevante entre valor pedido e oferta/pagamento.
- Trades tocados migraram usos seguros de `Colors.white` e aliases legados para tokens `AppTheme.brass500`, `brass400`, `frost400`, `textPrimary` e `backgroundAbyss`.
- Cobertura adicionada/ajustada para mapper, Auth timeout, Sets/Set detail 500 amigavel, TradeProvider 409/400 amigavel, review de proposta e confirmacoes em TradeDetail.

Validacao:

| Comando | Resultado |
| --- | --- |
| `cd app && flutter analyze lib/features/auth lib/features/decks lib/features/collection lib/features/trades lib/features/binder lib/features/market lib/core test --no-version-check` | PASS sem issues |
| `cd app && flutter test test/features/auth test/features/decks test/features/collection test/features/trades test/features/binder test/features/market test/core --no-version-check` | PASS: `01:02 +178: All tests passed!` |
| `flutter devices` | iPhone 15 Simulator disponivel: `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, iOS 17.4 |
| `xcrun simctl list devices available \| grep -E "iPhone 15\|Booted"` | iPhone 15 `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` Booted |
| `curl -sS --max-time 5 http://127.0.0.1:8082/health` | tentativa inicial: conexao recusada; follow-up com backend temporario: healthy |
| Follow-up `PORT=8082 dart run .dart_frog/server.dart` + `/health` | PASS: backend temporario healthy em 8082 |
| Follow-up `flutter analyze integration_test/binder_marketplace_trade_runtime_test.dart --no-version-check` | PASS: harness atualizado para os novos dialogs de review/confirmacao/envio |
| Follow-up `flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" ...` | PASS apos fix iOS Simulator MLImage/x86_64 e fix do controller no dialog de envio: `01:44 +2: All tests passed!` |

Runtime `binder_marketplace_trade_runtime_test.dart` no iPhone 15 foi reaberto apos iniciar backend real em `http://127.0.0.1:8082`. O teste passa pelos novos dialogs (`Revisar proposta`, `Aceitar trade?`, `Confirmar envio`, `Confirmar entrega?`, `Finalizar trade?`) e conclui binder/marketplace/trade/chat/notificacoes/mensagens diretas contra backend real. Handoff atualizado: `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-30.md`.

## Atualizacao - Profile e Community Social runtime - 2026-04-30

Sprint executada no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` contra backend real `http://127.0.0.1:8082`. Resultado: `PASS` para Profile proprio edit/reload, perfil publico, busca de usuarios, follow/unfollow, Community Explorar/Seguindo/Usuarios, abertura de deck publico e navegacao/back sem crash.

Evidencias:

- Handoff: `app/doc/runtime_flow_handoffs/profile_community_iphone15_2026-04-30.md`.
- Logs/provas locais: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_profile_community/`.
- Runtime iPhone 15: `profile_community_runtime_pass3.log`, resultado `00:57 +1: All tests passed!`.
- Device discovery: `flutter_devices.log` e `simctl_devices.log` confirmam iPhone 15 bootado.
- Health backend: `backend_health.json`.

Fluxos provados em UI real:

| Fluxo | Status | Evidencia de runtime |
| --- | --- | --- |
| Profile proprio abrir, editar e recarregar | `PASS` | `GET /users/me 200`, `PATCH /users/me 200`, `GET /users/me 200`; campos `display_name`, `location_state`, `location_city`, `trade_notes` persistidos |
| Perfil publico de outro usuario | `PASS` | `GET /community/users/:id 200` |
| Follow/unfollow e contadores/tabs | `PASS` | `POST /users/:id/follow 200`, `GET /users/:id/followers 200`, `DELETE /users/:id/follow 200` |
| Busca de usuarios | `PASS` | `GET /community/users?q=... 200`; resultado aberto por tap |
| Community Explorar | `PASS` | `GET /community/decks?page=1&limit=20 200` |
| Deck publico | `PASS` | `GET /community/decks/:id 200`; detalhe abriu e exibiu carta `Sol Ring` |
| Community Seguindo | `PASS` | `GET /community/decks/following?page=1&limit=20 200` |
| Community Usuarios | `PASS` | Busca na aba `Usuarios` e perfil publico aberto |

Mudancas relevantes: `AuthProvider`, `SocialProvider` e `CommunityProvider` passaram a registrar eventos estruturados/sanitizados para 4xx/5xx, erro de contrato e excecoes de provider. `UserProfileScreen` agora mostra falha de follow/unfollow e erros de seguidores/seguindo com retry; `CommunityScreen` mostra erro do feed Seguindo com retry. Overflows reais encontrados no iPhone 15 em cards/detalhe de decks publicos com usernames longos foram corrigidos com ellipsis.

Pendencias reais: `POST /users/:id/follow` ainda aparece como slow request no runtime (`~2841ms`) e leituras de perfil publico ficam em `~1.7s` por latencia do DB remoto. Nao houve 4xx/5xx inesperado, timeout, overflow ou crash no fluxo final; os 4xx cobertos em testes focados ficaram classificados sem token/email/payload sensivel.

## Atualizacao - P1 performance PUT /trades/:id/respond - 2026-04-30

Sprint executada no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` contra backend real `http://127.0.0.1:8082`. Resultado: `PASS` para reducao mensuravel de `PUT /trades/:id/respond`, sem alterar UX nem contrato JSON.

Evidencias:

- Handoff atualizado: `app/doc/runtime_flow_handoffs/binder_marketplace_trade_iphone15_2026-04-29.md`.
- Logs/provas locais: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_trade_respond_p1/`.
- Runtime iPhone 15: `iphone15_runtime_validation.log`, resultado `01:39 +2: All tests passed!`.
- Device discovery: `device_discovery.log` confirma `iPhone 15 (F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF) (Booted)`.

Metricas p50/p95/p99 em backend real, 5 amostras por action:

| Endpoint/action | Baseline p50/p95/p99 | Depois p50/p95/p99 | Resultado |
| --- | ---: | ---: | --- |
| `PUT /trades/:id/respond` `accept` | `3099/3902/3902ms` | `565/1394/1394ms` | p95 `64.3%` melhor |
| `PUT /trades/:id/respond` `decline` | `3018/3028/3028ms` | `564/591/591ms` | p95 `80.5%` melhor |

Impacto no runtime UI real:

- `PUT /trades/:id/respond`: `200` em `590ms`.
- `POST /trades`: `201` em `1742ms`.
- `PUT /trades/:id/status`: `200` em `602ms`, `608ms` e `593ms`.
- Nenhum overflow, timeout ou 4xx/5xx nao classificado no fluxo Social Trading executado.

Mudanca tecnica relevante: o app nao mudou; a melhoria veio do backend ao reduzir round-trips remotos e retirar notificacao sincrona do caminho critico. `PUT /trades/:id/respond` agora usa CTE atomica para lock, validacao receiver-only/status pending, update e history. Notificacoes `trade_accepted`/`trade_declined` continuam por `NotificationService.createFromActorDeferred` com timeout/log/Sentry sanitizados, preservando estado final e UX.

Validacao:

- `cd server && dart analyze routes/trades routes/notifications lib test && dart test -r expanded`: passou.
- `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test -P live -r expanded`: passou com `02:48 +166 ~3: All tests passed!`.
- `cd app && flutter analyze lib/features/trades lib/features/notifications lib/features/binder lib/features/market integration_test --no-version-check && flutter test test/features/trades test/features/notifications test/features/binder --no-version-check`: passou.
- Runtime iPhone 15 obrigatorio passou contra backend real em 8082.

Pendencia real: FCM/APNS real segue `not_proven` no simulador; leituras sociais de detalhe/mensagens ainda ficam em ~1.1s-1.7s e devem continuar monitoradas.

## Atualizacao - fechamento performance Social Trading P1 - 2026-04-30

Sprint executada no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` contra backend real `http://127.0.0.1:8082`. Resultado: `PASS` para reducao mensuravel de `POST /trades` e `PUT /trades/:id/status`, sem alterar UX nem contrato JSON.

Evidencias:

- Handoff atualizado: `app/doc/runtime_flow_handoffs/binder_marketplace_trade_iphone15_2026-04-29.md`.
- Logs/provas locais: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_social_trading_p1/`.
- Runtime iPhone 15: `iphone15_runtime_validation.log`, resultado `01:43 +2: All tests passed!`.
- Device discovery: `device_discovery.log` confirma `iPhone 15 (F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF) (Booted)`.

Metricas p50/p95/p99 em backend real, 5 amostras:

| Endpoint | Baseline p50/p95/p99 | Depois p50/p95/p99 | Resultado |
| --- | ---: | ---: | --- |
| `POST /trades` | `3976/3991/3991ms` | `1788/1818/1818ms` | p95/p99 `54.4%` melhor |
| `PUT /trades/:id/status` | `2782/2787/2787ms` | `600/621/621ms` | p95/p99 `77.7%` melhor |

Impacto no runtime UI real:

- `POST /trades`: `201` em `1826ms`.
- `PUT /trades/:id/status`: `200` em `636ms`, `647ms` e `635ms`.
- Nenhum overflow, timeout ou 4xx/5xx nao classificado no fluxo Social Trading executado.

Mudanca tecnica relevante: o app nao mudou; a melhoria veio do backend ao reduzir round-trips remotos nas rotas de escrita. `POST /trades` usa validacao batch e CTE para offer/items/history. `PUT /trades/:id/status` usa um statement atomico para lock, validacao, update e historico. Notificacoes continuam deferidas com timeout/log/Sentry e o estado final do trade foi preservado.

Validacao:

- `cd server && dart analyze routes/trades routes/notifications lib test && dart test -r expanded`: passou.
- `cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test -P live -r expanded`: primeira rodada teve falha nao-social em `ai_generate_create_optimize_flow_test.dart` por prompts com `422`; rerun imediato passou com `02:52 +165 ~3: All tests passed!`.
- `cd app && flutter analyze lib/features/trades lib/features/notifications lib/features/binder lib/features/market integration_test --no-version-check && flutter test test/features/trades test/features/notifications test/features/binder --no-version-check`: passou.
- Runtime iPhone 15 obrigatório passou contra backend real em 8082.

Pendencia real adicionada ao backlog: `PUT /trades/:id/respond` ainda ficou na faixa de `~3203ms` no runtime; nao era o alvo deste P1, mas compartilha o mesmo padrao antigo de round-trips e deve ser o proximo candidato de performance Social Trading.

## Atualizacao - staging observability Social Trading - 2026-04-30

Sprint executada no iPhone 15 Simulator `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` contra backend real `http://127.0.0.1:8082` com Sentry staging real no app/backend. Resultado: Sentry/log estruturado `PASS`; FCM externo `not_proven` no simulador por ausencia de APNS token.

Evidencias:

- Backend Sentry smoke: `SENTRY_SMOKE_EVENT_ID=fa3497bfe71248f99d0217b3ba964816`.
- Mobile Sentry smoke: `SENTRY_MOBILE_EVENT_ID=08cc80c92ae446b89e8179e842a368e3`.
- Runtime social trading com Sentry: `binder_marketplace_trade_runtime_sentry_sanitized.log`, resultado `02:12 +2: All tests passed!`.
- FCM smoke: `FCM_PERMISSION status=denied`, `FCM_APNS_TOKEN_PRESENT=false`, `firebase_messaging/apns-token-not-set`; classificacao `not_proven`.
- Logs/provas locais: `app/doc/runtime_flow_proofs_2026-04-30_iphone15_simulator_social_observability/`.

Metricas p50/p95/p99 medidas no backend real:

| Endpoint | p50 | p95 | p99 |
| --- | ---: | ---: | ---: |
| `GET /community/marketplace` | `611ms` | `1485ms` | `1485ms` |
| `POST /trades` | `3979ms` | `4258ms` | `4258ms` |
| `PUT /trades/:id/status` | `2783ms` | `3299ms` | `3299ms` |
| `GET /trades` | `630ms` | `1484ms` | `1484ms` |
| `GET /trades/:id` | `1300ms` | `1346ms` | `1346ms` |
| `POST /trades/:id/messages` | `1227ms` | `1400ms` | `1400ms` |
| `POST /conversations/:id/messages` | `1195ms` | `1341ms` | `1341ms` |

Sanitizacao reforcada sem alterar UX/contrato JSON: Sentry mobile nao recebe email, logs de auth nao imprimem token/email/body, logs push nao imprimem token FCM e o backend redige email/`fcm_token`. O log final sanitizado foi varrido contra padroes obvios de JWT/email/body de auth sem achados.

## Resumo executivo

Auditoria geral executada sobre o app ManaLoom em `2026-04-29 09:38-09:52 -0300`, com inventario de codigo, validacao automatizada ampla/focada e runtime fresco no iPhone 15 Simulator contra backend local real em `http://127.0.0.1:8082`.

O app esta funcional para os fluxos core ja cobertos por harnesses recentes: autenticacao/register no fluxo de deck, Home, Search -> Cartas, Search -> Colecoes, catalogo de colecoes, entrypoints de Colecao/Fichario/Marketplace/Trades/Colecoes e deck Commander create/import/optimize/preview/apply/validate. A auditoria nao implementou features novas.

Principal risco novo/provado: `GET /market/movers?limit=5&min_price=1.0` excedeu o timeout de 15s durante runtime de deck e tambem ficou pendurado por mais de 60s em probe isolado via `curl`. O app captura o erro no `MarketProvider` sem derrubar o fluxo, mas o endpoint e risco de experiencia/performance para Home/Market/Community.

## Evidencia de ambiente e runtime

| Item | Evidencia |
| --- | --- |
| Device primario | `iPhone 15` |
| Simulator id | `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF` |
| Runtime | `com.apple.CoreSimulator.SimRuntime.iOS-17-4` |
| Estado | `Booted` |
| Backend do app | `http://127.0.0.1:8082` |
| Health | `{"status":"healthy","service":"mtgia-server","environment":"development","version":"1.0.0"}` |
| Logs | `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_audit/` |
| Handoff runtime | `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-29.md` |

Resumo do `flutter devices`:

```text
iPhone 15 (mobile) • F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF • ios • com.apple.CoreSimulator.SimRuntime.iOS-17-4 (simulator)
macOS (desktop) • macos • darwin-arm64
Chrome (web) • chrome • web-javascript
Rafa (wireless) • 00008130-001C152922BA001C • ios • iOS 26.5 23F5043k
```

Resumo do `xcrun simctl list devices available | grep -E "iPhone 15|Booted"`:

```text
iPhone 15 Pro (...) (Shutdown)
iPhone 15 Pro Max (...) (Shutdown)
iPhone 15 (F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF) (Booted)
iPhone 15 Plus (...) (Shutdown)
```

## Inventario real

### App

- `app/lib/features`: `auth`, `binder`, `cards`, `collection`, `community`, `decks`, `home`, `market`, `messages`, `notifications`, `profile`, `scanner`, `social`, `trades`.
- `app/lib/core`: `api`, `observability`, `services`, `theme`, `utils`, `widgets`.
- `app/integration_test`: 105+ integration tests, com foco recente em `sets_catalog_runtime_test.dart`, `sets_search_catalog_runtime_test.dart`, `collection_entrypoints_runtime_test.dart`, `deck_runtime_m2006_test.dart`, `scanner_controlled_harness_runtime_test.dart`, `mobile_sentry_smoke_test.dart` e ampla familia `life_counter_*`.
- `app/test/features`: cobertura por `auth`, `binder`, `cards`, `collection`, `decks`, `home`, `market`, `messages`, `notifications`, `scanner`, `trades`.

### Backend relacionado

- `server/routes`: `auth`, `cards`, `sets`, `binder`, `community`, `conversations`, `decks`, `ai`, `import`, `market`, `notifications`, `trades`, `users`, `health`, `ready`, `rules`.
- `server/bin` relevante: `mana_loom_deck_runtime_e2e.dart`, `run_commander_only_optimization_validation.dart`, `sync_cards.dart`, `mtg_data_integrity.dart`, `migrate_*`, `stage/promote/import/expand_external_commander_meta_candidates.dart`, `sentry_smoke.dart`, `test_all_endpoints.py`, `validate_schema.py`.
- Docs/handoffs recentes usados: `runtime_flow_handoffs/README.md`, `deck_runtime_2026-04-27.md`, `deck_runtime_iphone15_simulator_2026-04-28.md`, `release_qa_iphone15_simulator_2026-04-28.md`, `scanner_runtime_2026-04-29.md`, `server/doc/RELATORIO_COMMANDER_ONLY_OPTIMIZATION_VALIDATION_2026-04-21.md`, `server/doc/RELATORIO_META_DECK_INTELLIGENCE_2026-04-24.md`.

## Matriz de modulos

| Modulo | Telas | Providers/Services | Endpoints | Testes existentes | Runtime provado | Pendencias |
| --- | --- | --- | --- | --- | --- | --- |
| Auth | `SplashScreen`, `LoginScreen`, `RegisterScreen` | `AuthProvider`, `ApiClient` token cache | `POST /auth/login`, `POST /auth/register`, `GET /auth/me`, `GET/PATCH /users/me` | `auth/models/user_test.dart`, `auth/screens/auth_screens_test.dart`; server `auth_service_test.dart`, `auth_flow_integration_test.dart` | Fresh iPhone 15 via `deck_runtime_m2006_test.dart` cobre register/autenticacao equivalente; login separado historico/not fully fresh | Provar logout/login separado no iPhone 15; UX de erros de credencial e rate limit |
| Home | `HomeScreen`, onboarding core flow, life counter routes | `DeckProvider`, `MarketProvider`, life counter stores/controllers | `/decks`, `/market/movers`, rotas internas do life counter | `home_screen_test.dart`, 45 testes de home/life counter, muitos integration tests life counter | Fresh iPhone 15 abre Home no fluxo de deck; Life Counter tem suite ampla e golden clone estabilizados; `/market/movers` foi otimizado e cacheado | Manter p95/p99 de `/market/movers` em producao/staging e observar drift visual real do Life Counter |
| Search/Cards | `CardSearchScreen`, `CardDetailScreen` | `CardProvider`, `ScannerCardSearchService` | `GET /cards`, `POST /ai/explain`, `GET /cards/printings`, `POST /cards/resolve` | `cards/screens/card_search_screen_test.dart`, scanner provider/services tests | Fresh iPhone 15 por `sets_search_catalog_runtime_test.dart`: Search -> Cartas -> detalhe por imagem | Explicar carta (`/ai/explain`) nao foi provado no runtime fresco; estados offline sao basicos |
| Sets/Colecoes | `CollectionScreen`, `LatestSetCollectionScreen`, `SetsCatalogScreen`, `SetCardsScreen` | API direta via `ApiClient` nas telas | `GET /sets`, `GET /cards?set=...` | `collection/sets_catalog_screen_test.dart`, server `sets_route_test.dart` | Fresh iPhone 15: `sets_catalog_runtime_test.dart`, `sets_search_catalog_runtime_test.dart`, `collection_entrypoints_runtime_test.dart` | Melhorar cache/paginacao percebida e empty states para sets futuros/parciais |
| Scanner | `CardScannerScreen`, preview/overlay widgets | `ScannerProvider`, `CardRecognitionService`, `ScannerCardSearchService`, `scanner_ocr_parser`, fuzzy matcher | `GET /cards`, `GET /cards/printings`, `POST /cards/resolve` | `scanner_provider_test.dart`, `scanner_card_search_service_test.dart`, `scanner_ocr_parser_test.dart`, `scanner_controlled_harness_runtime_test.dart` | Handoff `scanner_runtime_2026-04-29.md`: parser/provider/backend fallback controlado no iPhone 15; follow-up Android fisico em 2026-04-30 provou camera/MLKit abrindo e corrigiu token fallback para `Phyrexian Horror` | Clean retest do token fisico ainda precisa enquadrar somente a ficha dentro do guia, sem textos de embalagem/pedido ao redor |
| Collection/Binder | `CollectionScreen` tabs, `BinderScreen`, `BinderItemEditor` | `BinderProvider` | `GET/POST /binder`, `PUT/DELETE /binder/:id`, `GET /binder/stats`, `GET /community/binders/:userId` | `binder_provider_test.dart`, `binder/screens/marketplace_screen_overflow_test.dart` | Fresh iPhone 15: `BinderItemEditor` visual criou, editou e removeu `Command Tower`; backend confirmou `201/200/204` | Estados 401/empty/loading seguem como melhoria de cobertura UX, sem lacuna P1 do fluxo principal |
| Marketplace | `MarketplaceScreen`, `MarketScreen` | `BinderProvider`, `MarketProvider` | `GET /community/marketplace`, `GET /market/movers` | `market/models/card_mover_test.dart`, binder marketplace overflow test | Fresh iPhone 15: marketplace sem filtro, busca de item seller real e inicio de proposta; `/market/movers` otimizado | Manter p95/p99; marketplace sem filtro ficou `664ms` no runtime final |
| Trades | `TradeInboxScreen`, `TradeDetailScreen`, `CreateTradeScreen` | `TradeProvider` | `GET/POST /trades`, `GET /trades/:id`, `PUT /respond`, `PUT /status`, `GET/POST /messages` | `trades/screens/create_trade_screen_overflow_test.dart`; server live/error contract cobre rotas | Fresh iPhone 15: criar proposta, seller aceitar/enviar, buyer confirmar entrega/finalizar, chat visual e status `completed` | Troca pura com itens dos dois lados ainda nao foi provada; reduzir latencia residual de escritas |
| Decks | `DeckListScreen`, `DeckDetailsScreen`, add/search/scan routes | `DeckProvider` + support fetch/generation/import/ai/mutation | `/decks`, `/decks/:id`, `/decks/:id/cards*`, `/decks/:id/validate`, `/decks/:id/pricing`, `/decks/:id/export`, `/community/decks/:id` | 21 testes focados de decks; runtime widget flow; server deck/validation tests | Fresh iPhone 15: `deck_runtime_m2006_test.dart` passou com backend real | Nome do test ainda referencia M2006 apesar de rodar no iPhone 15; import textual completo separado nao foi provado neste run |
| Generate | `DeckGenerateScreen` | `DeckProviderSupportGeneration` | `POST /ai/generate`, `POST /cards/resolve/batch`, `GET /cards` | `deck_runtime_widget_flow_test.dart`, deck provider tests, server generate/create tests | Widget runtime provado; iPhone 15 fresh cobre fluxo deck principal com backend real conforme harness atual | Geracao livre com prompts variados e sem mock deve ter matriz de prompts por formato/bracket |
| Optimize | `DeckOptimize` dialogs/sheets dentro de deck details | `DeckProviderSupportAi`, optimize widgets | `POST /ai/optimize`, `GET /ai/optimize/jobs/:id`, `POST /ai/rebuild`, `POST /ai/archetypes` | `deck_optimize_*`, provider tests, server optimization suites | Fresh iPhone 15: optimize -> preview -> apply -> validate passou no deck runtime | Telemetria UX de progresso/timeout e tratamento de `needs_repair` devem continuar no backlog |
| Validate | Deck detail validation UI/provider | `DeckProviderSupportMutation` | `POST /decks/:id/validate` | `deck_validation_test.dart`, app deck tests | Fresh iPhone 15 validou final deck no runtime | Expor melhor motivo de invalidez para usuario final |
| Profile | `ProfileScreen`, `UserProfileScreen` | `AuthProvider`, `SocialProvider`, `BinderProvider` | `GET/PATCH /users/me`, `GET /community/users/:id`, follows/binders | Sem teste dedicado de profile screen encontrado | Entry points e perfil publico historicos; nao provado fresh nesta auditoria | Criar smoke tests de profile/edit avatar/location/trade notes; validar navegacao de notificacao para perfil |
| Messages | `MessageInboxScreen`, `ChatScreen` | `MessageProvider` com polling | `/conversations`, `/conversations/:id/messages`, `/read`, `/unread-count` | `message_provider_test.dart`, `messages/models/message_models_test.dart`; server live/error contract | Fresh iPhone 15: runtime separado criou conversa direta, abriu chat visual, enviou mensagem e confirmou read receipt | Reduzir latencia residual de `POST /conversations/:id/messages`; otimizar polling em background |
| Notifications | `NotificationScreen`, AppBar bell | `NotificationProvider`, `PushNotificationService` | `/notifications`, `/count`, `/read`, `/read-all`, `/users/me/fcm-token` | `notifications/models/notification_models_test.dart`; server live/error contract | Fresh iPhone 15: notificacao de trade, tap/read individual e `Ler todas`/read-all provados na UI | FCM real depende config/device staging; nao foi provado no simulador |
| Community/Social | `CommunityScreen`, `CommunityDeckDetailScreen`, `UserSearchScreen`, `UserProfileScreen` | `CommunityProvider`, `SocialProvider`, `MarketProvider` | `/community/decks`, `/community/users`, `/users/:id/follow*`, `/community/decks/following` | Server error contract/social route coverage; app sem smoke amplo dedicado | Community tab historicamente provada; fresh entrypoint indireto via collection/deck runtime nao cobre todas tabs | Smoke test para Explorar/Seguindo/Usuarios, follow/unfollow/feed e public deck copy |
| Settings | Nao ha modulo `settings` global; ha settings do life counter | Life counter settings stores/adapters | Local storage, sem API principal | Muitos testes `life_counter_*settings*` | Life counter settings tem coverage e os goldens do clone proof foram estabilizados sem atualizar baseline | Decidir se settings global do app entra no roadmap; manter monitoramento de drift visual real |
| Navigation/Routes | `GoRouter` em `main.dart`, `MainScaffold`, shell tabs | Auth redirect, provider cleanup on logout | N/A | Runtime widget flow e integration tests exercitam rotas principais | Fresh iPhone 15 cobre varias rotas protegidas e back/forward em Search/Sets/Collection/Deck | Criar smoke de todas rotas protegidas com auth; nomear rotas para Sentry mais legiveis |
| Providers/API contracts | `ApiClient`, providers por feature | Request id, token cache, Firebase Performance, Sentry capture | Todos acima | `api_client_request_id_test.dart`, provider tests focados | Contratos principais de deck/cards/sets/social/trades/messages provados em runtime focado; 4xx/5xx e slow requests reportaveis no `ApiClient` | Gerar contract map automatizado e CI contra server routes; ampliar UX 404/401 por provider |
| Sentry/logging/telemetry | `AppObservability`, `PerformanceService`, `AppLogger`, `ActivationFunnelService` | Sentry DSN por dart-define; Firebase Performance; request ids | `/users/me/activation-events`, Sentry external, Firebase | `mobile_sentry_smoke_test.dart`, server `observability_test.dart`, `request_trace_test.dart` | Sentry staging app/backend provado por smoke real; breadcrumbs slow request, 4xx/5xx reportaveis e `captureRouteException` auditados nos fluxos sociais | FCM/Firebase Messaging real segue `not_proven` no simulador; padronizar eventos de funil de optimize/generate |
| Offline/loading/error states | `AppStatePanel`, provider flags por modulo | Estados locais em providers/screens | N/A | `app_state_panel_test` citado em manual; testes focados de screens | Loading/error basicos aparecem em flows provados | Nao ha modo offline/cache-first geral; melhorar retry, skeleton e mensagens por modulo |

## Validacao automatizada

| Comando | Resultado | Classificacao |
| --- | --- | --- |
| `cd app && flutter analyze lib test integration_test --no-version-check` | Passou, sem issues | Green |
| `cd app && flutter test test --no-version-check` | Falhou em `life_counter_clone_proof_test.dart` por diffs de golden de 0.06%-0.30% | Teste visual/golden a revisar; nao bloqueou fluxos core |
| `cd app && flutter analyze lib/features/cards lib/features/collection test/features/cards test/features/collection --no-version-check` | Passou | Green |
| `cd app && flutter test test/features/cards test/features/collection --no-version-check` | Passou | Green |
| `cd app && flutter test test/features/decks/screens/deck_runtime_widget_flow_test.dart ... deck_optimize_flow_support_test.dart --no-version-check` | Passou | Green |
| `cd server && dart analyze lib routes bin test` | Passou, sem issues | Green |
| `cd server && dart test` | Falhou em live tests que esperam `http://localhost:8080` sem backend ativo, ex. `ai_archetypes_flow_test.dart` e `decks_crud_test.dart` | Falta de ambiente para testes live; separar de unit suite |

Falhas amplas classificadas:

- App: falha real de regressao visual/test baseline em goldens do life counter, nao crash funcional. Artefatos gerados em `app/test/features/home/failures` foram limpos para nao entrar no commit.
- Server: dependencia externa/ambiente, pois a suite inclui testes que instruem iniciar `cd server && dart_frog dev` em `localhost:8080`. O backend desta auditoria foi iniciado em `8082` por regra do runtime solicitado.

## Runtime iPhone 15 executado

Comandos executados contra backend real `http://127.0.0.1:8082`:

```bash
cd app
flutter test integration_test/sets_catalog_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
flutter test integration_test/collection_entrypoints_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
```

Resultado:

| Test | Resultado | Observacoes |
| --- | --- | --- |
| `sets_catalog_runtime_test.dart` | Passou | Colecoes/catalogo no iPhone 15 |
| `sets_search_catalog_runtime_test.dart` | Passou | Search -> Cartas e Search -> Colecoes |
| `collection_entrypoints_runtime_test.dart` | Passou | Colecao -> Fichario/Marketplace/Trades/Colecoes sem crash |
| `deck_runtime_m2006_test.dart` rodado em iPhone 15 | Passou | register/auth -> Home -> deck Commander -> optimize -> preview/apply/validate; registrou timeout nao fatal de `/market/movers` |

Todos os integration tests exibiram warning conhecido de MLKit/GoogleMLKit sem arm64 para simuladores Apple Silicon iOS 26+, mas o iPhone 15 runtime iOS 17.4 executou e passou.

## O que esta pronto

- Navegacao autenticada principal com `GoRouter`, shell tabs e cleanup de providers em logout.
- Deck core no app: criar/importar/generate, detalhes, adicionar/search/scan routes, optimize, apply e validate.
- Cards search e detalhe por imagem.
- Sets/Colecoes com catalogo, busca e set detail via backend real.
- Collection entrypoints para Fichario/Marketplace/Trades/Colecoes sem crash.
- Scanner controlado acima da camera: OCR parser/provider/backend fallback provado em handoff do mesmo dia.
- Observabilidade base: Sentry opcional por `SENTRY_DSN`, request id no `ApiClient`, Firebase Performance com fallback, provider exception capture.

## Parcialmente pronto

- Binder/Marketplace/Trades/Messages/Notifications/Social possuem providers, rotas e telas, mas precisam de runtime autenticado end-to-end por dominio.
- Home depende de dados de Market/Deck; tolera erro do MarketProvider, mas endpoint lento degrada experiencia.
- Life counter tem grande cobertura, mas a suite ampla esta sensivel a goldens.
- Offline/loading/error states existem por tela/provider, mas nao ha estrategia offline geral.

## Not proven nesta rodada

- Login/logout separado no iPhone 15.
- CRUD completo de binder.
- Marketplace search/list com dados reais e criacao de trade a partir de carta.
- Trade lifecycle completo entre usuarios.
- Messages inbox/chat/polling/read receipt.
- Notifications badge/read/read-all/tap navigation.
- Profile edit/public profile completo.
- Scanner camera/OCR real em hardware fisico.
- FCM/Firebase Messaging real em device/config staging com APNS token e evidencia de entrega.

## Bugs e riscos encontrados

| Severidade | Item | Evidencia | Owner sugerido |
| --- | --- | --- | --- |
| P0/P1 | `GET /market/movers?limit=5&min_price=1.0` lento/hung | Timeout em runtime apos 15s e probe `curl` isolado pendurado >60s | Backend market/performance |
| P1 | Suite app completa falha por goldens life counter | `life_counter_clone_current_set_life.png` diff 0.06%; `high_roll` diff 0.30%; outros clone proof diffs | App home/life counter |
| P1 | Suite server completa mistura unit e live localhost:8080 | `ai_archetypes_flow_test.dart` connection refused; `decks_crud_test.dart` instrui iniciar server | Backend test infra |
| P2 | Nome `deck_runtime_m2006_test.dart` usado como prova iPhone 15 | Test roda em iPhone 15, mas nome historico confunde evidencias | QA/app test naming |

## Melhorias UX

1. Mostrar estado degradado explicito quando Market movers falha, sem ocupar logs do usuario.
2. Melhorar empty/error states de Binder, Trades, Messages e Notifications com CTA de acao.
3. Expor erros de validate/optimize em linguagem mais acionavel.
4. Padronizar feedback de loading em telas com polling.
5. Criar settings global se entrar no roadmap; hoje settings ficam concentradas no life counter.

## Melhorias de performance

1. Otimizar `GET /market/movers` com indice/materializacao/cache/limite defensivo.
2. Evitar carregar movers em telas onde nao e necessario para o fluxo imediato.
3. Revisar timeouts de endpoints de IA vs endpoints leves para degradacao mais rapida.
4. Medir p95/p99 de `/cards`, `/sets`, `/community/marketplace`, `/decks/:id`.

## Melhorias de observabilidade/Sentry

1. Rodar `mobile_sentry_smoke_test.dart` com `SENTRY_DSN` de staging.
2. Nomear rotas GoRouter para breadcrumbs mais legiveis.
3. Adicionar tags por modulo/provider em erros de API.
4. Registrar funil generate -> save -> optimize -> apply -> validate com `ActivationFunnelService`.
5. Criar dashboard para timeouts de `/market/movers` e endpoints de IA.

## Backlog priorizado

### P0

1. Corrigir performance/hang de `GET /market/movers?limit=5&min_price=1.0`.
2. Separar suite server em unit/offline vs live-backend para CI nao falhar por ambiente ausente.

## Atualizacao - Binder/Marketplace/Trades runtime iPhone 15 - 2026-04-29 17:10 -0300

Handoff fresco: `app/doc/runtime_flow_handoffs/binder_marketplace_trade_iphone15_2026-04-29.md`.

Resultado: `flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check` passou com backend real em `8082` (`+2 All tests passed!`).

Matriz curta atualizada:

| Tela/Provider | Endpoint | Teste existente | Runtime provado | Lacuna |
| --- | --- | --- | --- | --- |
| Binder | `GET/POST/PUT/DELETE /binder`, `/binder/stats` | `binder_provider_test.dart`, overflow marketplace | `BinderItemEditor` visual criou/editou/removeu `Command Tower`; backend confirmou `201/200/204` | Sem lacuna P1 |
| Marketplace | `GET /community/marketplace` | overflow marketplace | UI listou marketplace sem filtro, buscou item seller real e iniciou proposta | Latencia final sem filtro `664ms`; manter p95/p99 |
| Create Trade | `POST /trades` | overflow create trade | UI criou venda via Marketplace com `201` | Troca pura com itens dos dois lados nao provada |
| Trade Detail | `GET /trades/:id`, `PUT /respond`, `PUT /status` | server live/error contract | UI seller aceitou/marcou enviado; UI buyer tocou `Confirmar Entrega` e `Finalizar`; backend `completed` | Sem lacuna P1 |
| Trade Messages | `GET/POST /trades/:id/messages` | server live/error contract | Chat visual enviou mensagem, UI buyer reabriu detalhe e viu a mensagem | Sem lacuna P1 |
| Notifications | `/notifications*` | model test + server live | UI buyer abriu notificacoes, tap marcou read individual, `Ler todas` marcou read-all e backend unread `0` | FCM real em device/staging ainda not proven |
| Direct Messages | `/conversations*` | `message_provider_test.dart`, model test + server live | Runtime separado criou conversa, abriu chat visual, enviou mensagem e confirmou read receipt | Sem lacuna P1 |

Correcoes desta rodada: `BinderProvider.removeItem` aceita `204 No Content`; `TradeProvider.sendMessage` atualiza lista de chat de forma imutavel; `TradeDetailScreen` envia chat por `TextInputAction.send`; `MessageProvider.fetchMessages` evita polling sobreposto; `ChatScreen` removeu overflow subpixel de teclado; `NotificationScreen` mostra `Ler todas` quando a lista carregada contem nao lidas.

Dados de QA criados com prefixos `qa_bmt_19ddadb15b4` e `qa_dm_19ddadc9d8f`; trade final `80366433-a69c-4f1e-90d0-03c923c76f5b` ficou `completed`.

Sentry/telemetria: `ApiClient` registra breadcrumbs para slow requests e captura 4xx/5xx reportaveis com metodo, endpoint, status, duracao e request ids, sem payload sensivel. As rotas backend tocadas capturam excecoes via `captureRouteException` e `Log.e` com contexto tecnico sanitizado.

## Atualizacao - performance/observabilidade Social Trading - 2026-04-29 17:37 -0300

Sprint final executada sobre `POST /trades`, `PUT /trades/:id/status`, `POST /trades/:id/messages` e `POST /conversations/:id/messages`, mantendo contrato JSON, status codes, autenticacao, permissoes e UX aprovados.

| Endpoint | Antes frio/quente | Depois frio/quente | Resultado |
| --- | ---: | ---: | --- |
| `POST /trades` | `5324.62ms` / `6167.93ms` | `4123.00ms` / `4941.76ms` | `19.9%` a `22.6%` mais rapido |
| `PUT /trades/:id/status` | `4061.75ms` / `4060.68ms` | `2844.34ms` / `2845.01ms` | ~`30%` mais rapido |
| `POST /trades/:id/messages` | `2440.10ms` / `2443.68ms` | `1222.30ms` / `1228.63ms` | ~`50%` mais rapido |
| `POST /conversations/:id/messages` | `3058.88ms` / `3043.00ms` | `1238.07ms` / `1233.96ms` | ~`59%` mais rapido |

Runtime iPhone 15 fresco: `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_binder_marketplace_trade/binder_marketplace_trade_runtime_social_perf_pass.log`, device `F0B1713F-4B8A-4DB9-825E-C8A4B17A03DF`, backend `http://127.0.0.1:8082`, resultado `01:53 +2: All tests passed!`.

Mudancas backend: notificacoes sociais/trade/direct messages sairam do caminho critico via `NotificationService.createFromActorDeferred`, com timeout de 10s, log estruturado e Sentry em falha; `POST /conversations/:id/messages` reduziu round-trip com CTE; validacoes de `payment_method` e `delivery_method` agora retornam `400` antes de violar constraints do banco. Observabilidade raiz classifica slow request, 4xx e 5xx com endpoint, duracao, request id, user id tecnico e ids seguros, sem token/email/mensagem completa.

Validacao adicionada: `server/test/social_trading_live_test.dart` no preset `live`, cobrindo sucesso, response shape, `400` esperado e notificacoes essenciais. FCM externo segue `not proven` no simulador/config local; logs/captura estruturada foram provados em codigo, teste live e runtime.

### P1

1. Manter monitoramento de drift visual real do Life Counter apos estabilizacao dos goldens.
2. Monitorar p95/p99 persistente de Social Trading; `respond`, `status` e criacao de trade ja foram otimizados, e leituras de detalhe/mensagens ficam como candidato futuro se impactarem UX.
3. Provar Profile edit/public tabs com backend real.
4. Adicionar smoke de Community Explorar/Seguindo/Usuarios + follow/unfollow.
5. Provar FCM real em device/config staging.
6. Fechar matriz física do Scanner: app já abre no iPhone físico com backend
   público e auth público passa, mas câmera/OCR real ainda precisa execução
   manual com cartas reais e logs anexados.

### P2

1. Renomear/copiar `deck_runtime_m2006_test.dart` para nome neutro `deck_runtime_iphone15_test.dart` ou `deck_runtime_commander_test.dart`.
2. Automatizar matriz app provider -> endpoint -> server route.
3. Melhorar copy e empty states em offline/error.
4. Adicionar performance assertions leves em runtime tests para endpoints nao-IA.

## Comandos executados e resultados

```bash
git status --short
flutter devices
xcrun simctl list devices available | grep -E "iPhone 15|Booted"
cd app && flutter analyze lib test integration_test --no-version-check
cd app && flutter test test --no-version-check
cd server && dart analyze lib routes bin test
cd server && dart test
cd app && flutter analyze lib/features/cards lib/features/collection test/features/cards test/features/collection --no-version-check
cd app && flutter test test/features/cards test/features/collection --no-version-check
cd app && flutter test test/features/decks/screens/deck_runtime_widget_flow_test.dart --no-version-check
cd app && flutter test test/features/decks/screens/deck_details_screen_smoke_test.dart test/features/decks/providers/deck_provider_test.dart test/features/decks/providers/deck_provider_support_test.dart test/features/decks/widgets/deck_optimize_flow_support_test.dart --no-version-check
cd server && PORT=8082 dart run .dart_frog/server.dart
curl -sS http://127.0.0.1:8082/health
cd app && flutter test integration_test/sets_catalog_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
cd app && flutter test integration_test/sets_search_catalog_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
cd app && flutter test integration_test/collection_entrypoints_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
cd app && flutter test integration_test/deck_runtime_m2006_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
curl -sS -o /tmp/market_movers_probe.json -w 'http_code=%{http_code} time_total=%{time_total}\n' 'http://127.0.0.1:8082/market/movers?limit=5&min_price=1.0'
cd server && dart analyze routes/trades routes/market routes/binder routes/conversations routes/notifications lib test
cd server && dart test -r expanded
cd server && TEST_API_BASE_URL=http://127.0.0.1:8082 dart test -P live -r expanded
cd app && flutter analyze lib/features/binder lib/features/market lib/features/trades lib/features/messages lib/features/notifications integration_test --no-version-check
cd app && flutter test test/features/binder test/features/trades test/features/messages test/features/notifications --no-version-check
cd app && flutter test integration_test/binder_marketplace_trade_runtime_test.dart -d "iPhone 15" --dart-define=API_BASE_URL=http://127.0.0.1:8082 --dart-define=PUBLIC_API_BASE_URL=http://127.0.0.1:8082 --reporter expanded --no-version-check
```

## Arquivos de evidencia

- `app/doc/runtime_flow_handoffs/deck_runtime_iphone15_simulator_2026-04-29.md`
- `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_audit/sets_catalog_runtime_test.log`
- `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_audit/sets_search_catalog_runtime_test.log`
- `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_audit/collection_entrypoints_runtime_test.log`
- `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_audit/deck_runtime_m2006_test.log`
- `server/doc/APP_BACKEND_CONTRACT_AUDIT_2026-04-29.md`
- `app/doc/runtime_flow_proofs_2026-04-29_iphone15_simulator_binder_marketplace_trade/binder_marketplace_trade_runtime_after_sprint_pass.log`

---

## Addendum 2026-05-07 - Regressao final Android non-scanner

### Veredito de release interno

`GO WITH RISKS`

O app foi validado no Android fisico `SM A135M` (`R58T300SREH`, Android 14 API 34) contra o backend publico `https://evolution-cartinhas.8ktevp.easypanel.host`. O backend respondeu `/health` com `status=healthy`, `environment=production`, `version=1.0.0` e `git_sha=56aed49c36642148abc99a553459ee584967d47d`, alinhado ao `master` local/origin.

Scanner, camera, OCR e MLKit scanner ficaram explicitamente `DEFERRED / IGNORED`; `integration_test/scanner_controlled_harness_runtime_test.dart` nao foi executado e nao participa da decisao.

### Gates executados

```bash
git fetch --prune origin
git pull --ff-only origin master
flutter devices --no-version-check
adb devices -l
curl -sS -i https://evolution-cartinhas.8ktevp.easypanel.host/health

cd app
flutter analyze lib test integration_test --no-version-check
flutter test test --no-version-check
```

Resultados:

- `flutter analyze lib test integration_test --no-version-check`: `PASS`;
- `flutter test test --no-version-check`: `PASS`, `+551`;
- Android runtime non-scanner: 12/12 integration tests `PASS`.

### Runtime Android non-scanner

Todos com:

```bash
flutter test <integration_test_file> \
  -d R58T300SREH \
  --dart-define=API_BASE_URL=https://evolution-cartinhas.8ktevp.easypanel.host \
  --dart-define=PUBLIC_API_BASE_URL=https://evolution-cartinhas.8ktevp.easypanel.host \
  --reporter expanded \
  --no-version-check
```

| Fluxo | Teste | Resultado |
| --- | --- | --- |
| Search/sets catalog | `integration_test/sets_catalog_runtime_test.dart` | `PASS` |
| Search -> Cards/Colecoes | `integration_test/sets_search_catalog_runtime_test.dart` | `PASS` |
| Collection entrypoints | `integration_test/collection_entrypoints_runtime_test.dart` | `PASS` |
| Profile/community | `integration_test/profile_community_runtime_test.dart` | `PASS` |
| Auth/create/detail/optimize/validate deck | `integration_test/deck_runtime_m2006_test.dart` | `PASS` |
| Generate async/save/detail/optimize | `integration_test/deck_generate_async_runtime_test.dart` | `PASS` |
| Binder dashboard | `integration_test/binder_dashboard_runtime_test.dart` | `PASS` |
| Marketplace/trades/messages/notifications | `integration_test/binder_marketplace_trade_runtime_test.dart` | `PASS` |
| Life Counter/Lotus runtime | `integration_test/life_counter_lotus_visual_runtime_proof_test.dart` | `PASS` |
| Lotus card search overlay | `integration_test/life_counter_lotus_card_search_visual_smoke_test.dart` | `PASS` |
| Lotus settings overlay | `integration_test/life_counter_lotus_settings_visual_smoke_test.dart` | `PASS` |
| Life Counter clock visual | `integration_test/life_counter_clock_visual_smoke_test.dart` | `PASS` |

### Riscos aceitos

- Optimize agressivo no backend vivo retornou falha amigavel/safe no-op no fluxo create/detail/optimize/validate; preview positivo com sugestoes aplicaveis, deselect e apply parcial seguem `NOT PROVEN`.
- O fluxo de deck gerado retornou `422 OPTIMIZE_NEEDS_REPAIR` e abriu caminho `rebuild_guided` como acao de produto, sem erro bruto user-facing.
- Latencias acima de 5s foram observadas em `/market/movers?limit=5&min_price=1.0` (5303ms), `POST /ai/archetypes` (7733ms) e conclusao async de AI Generate (15851ms, com feedback inicial em 770ms).
- Screenshot nativo Lotus registrou `LOTUS_SCREENSHOT_NOT_PROVEN`, mas DOM/state/reopen passaram; nao houve crash, tela branca ou overflow bloqueante.

### Evidencias

- Relatorio: `server/doc/RELEASE_INTERNAL_NON_SCANNER_GO_NO_GO_2026-05-07.md`.
- Evidencias locais nao versionadas: `app/doc/runtime_flow_proofs_2026-05-07_android_sm_a135m_non_scanner/`.
