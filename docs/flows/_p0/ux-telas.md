# P0 CORE — UX das telas de deck: imagem de carta, densidade, troca pareada, acessibilidade e prova visual

Medição feita em 2026-09-22 contra o código de `HEAD` = `d15beb05b` (branch `codex/free-beta-release-candidate-2026-07-17`), somente leitura. Nada foi executado (sem flutter/dart/servidor). Toda afirmação traz `arquivo:linha`; caminhos de app são relativos a `app/` quando começam por `lib/`, `test/`, `integration_test/` ou `tool/`.

> **Revisão adversarial (2026-09-22, mesmo HEAD, só leitura).** Este documento foi corrigido no lugar. Toda célula ou frase alterada leva a marca **[rev]**. O único comando além de leitura foi `scripts/manaloom_ui_source_digest.sh`, que só calcula hash num diretório temporário fora do repo e devolveu `8bba809c…`. A seção final, "Verificação adversarial", resume o que caiu, o que subiu e o veredito.

Fonte do aceite: `docs/BREWTACT_MASTER_EXECUTION_BACKLOG_2026-08-12.md:335,336,341,344,347` (registry `docs/generated/TASK_REGISTRY.json`, `source_line` idem). **[rev]** O plano de execução rastreado, `docs/execution/waves/07-beta-release.md:29,30,35,59`, detalha o fechamento de cada uma e é mais exigente que o backlog em PROOF-001 (ver lá).

## Resumo do grupo

| Tarefa | Declarado | Medido | Asserções (provado / sem prova / parcial / ausente) | Arquivos a tocar | Testes a escrever | Migração | Prova viva | Decisão humana | ABERTO hoje |
|---|---|---|---|---:|---:|---|---|---|---|
| `BT-UX-IMG-001` | TODO | **metade** **[rev: era "quase lá"]** | 3 / 2 / 5 / 0 **[rev: +1 asserção, caminho Web]** | 18 **[rev: 16]** | 9 **[rev: 6]** | não | sim | sim (crop/fundo de arte; selo em superfícies decorativas; destino das variantes `artCrop`/`setArt`) | não (29/29 OFF) |
| `BT-UX-FIX-001` | TODO | **metade**, com zero no HEAD e o resto num worktree | 0 / 0 / 6 / 3 | 16 (11 já escritos num worktree) | 7 | não | sim | sim ("trocas" = Optimize ou Trades; arte real na fixture de captura). **[rev]** O cold-repro saiu da lista: já foi resolvido no HEAD | n/a (teste) |
| `BT-UX-SWAP-001` | TODO | **mal começada** **[rev: era "metade"]** | 1 / 1 / 4 / 3 **[rev: o original dizia 1/3/3/2, que já não batia com as próprias linhas (1/2/4/2)]** | 13 **[rev: 9]** | 10 **[rev: 7]** | não | sim | sim (ordem vs `BT-UX-KIT-001`; construir antes de `DCK-P0-02`; **[rev]** o que "impacto do par" significa e quem o calcula) | não (`ai_analyze_optimize_advisory` OFF) |
| `BT-UX-A11Y-001` | BLOCKED_BY_P0 | **mal começada** | 1 / 0 / 6 / 3 | 15, no mínimo **[rev: 12]** | 11, no mínimo **[rev: 8]** | não | sim (TalkBack humano + teclado real) | sim (uso do aparelho físico do dono) | não |
| `BT-UX-PROOF-001` | BLOCKED_BY_P0 | **mal começada** | 1 / 0 / 3 / 3 | 10 **[rev: 7]** | 3 **[rev: 2]** | não | sim (é a própria prova) | sim (quem assina o visual; **[rev]** Android físico ou emulador) | não |

Leitura em uma linha **[rev]**: o contrato de imagem de carta existe como widget e está testado, mas só no caminho nativo. O caminho Web, que é a plataforma principal da beta, não tem teste que rode. O widget ainda oferece duas variantes com corte, e faltam nome sem imagem e prova de ausência de layout shift. O par SAI→ENTRA existe sem nenhuma imagem e sempre empilhado, porque o ramo lado a lado é inalcançável dentro do diálogo de 560 px. O conteúdo por par que o servidor manda (função, motivo, impacto, confiança) é constante para o deck inteiro ou sai de um modelo de frase fixo. As fixtures patológicas existem só num worktree não commitado, cujo bloqueio de commit (o cold-repro) já foi resolvido no HEAD. Acessibilidade e prova dependem de sete superfícies que ainda não existem, e de prova humana que nunca rodou.

### Os três eixos, por tarefa

| Tarefa | IMPLEMENTADO | PROVADO | ABERTO |
|---|---|---|---|
| IMG-001 | núcleo sim (`lib/core/widgets/card_artwork.dart`, `cached_card_image.dart`); conformidade das superfícies parcial; **[rev]** o widget ainda oferece `artCrop`/`setArt` com `BoxFit.cover` | widget sim no caminho nativo (`test/core/widgets/card_artwork_test.dart`, `cached_card_image_test.dart`). **[rev]** O caminho Web (`_ScryfallWebCardImage`) não tem teste que rode. Superfícies e ausência de layout shift também não | não: `decks_private` e demais 28 capabilities `off` em `server/config/release_capabilities.json` |
| FIX-001 | só no worktree `~/.codex/worktrees/manaloom-bt-ux-fix-001` (não commitado) | lá: PASS local declarado na ficha (Deck 8/8, Trade 6/6); aqui: nada no HEAD | n/a |
| SWAP-001 | par sem imagem (`lib/features/decks/widgets/deck_optimize_sheet_widgets.dart:2076-2279`). **[rev]** Sempre empilhado. O conteúdo por par que vem do servidor é constante ou modelo fixo | lógica de seleção pareada sim; conteúdo do par, não | não: `ai_analyze_optimize_advisory` `off` |
| A11Y-001 | infraestrutura (matrizes, harness de guideline, contraste de tokens) | tokens sim; superfícies de deck quase nada; humano zero | não |
| PROOF-001 | verificador de 3 níveis e gate | verificador sim; evidência das superfícies novas não existe; agregado `latest.json` defasado | não |

---

## BT-UX-IMG-001 — contrato visual image-led compartilhado para cartas

**Aceite:** "Aspect ratio `63:88`, `contain`, exact-first, fallback rotulado, nome sem imagem e sem layout shift." Depende de `BT-ART-01`. A onda 07 descreve a entrega como "componente de carta compartilhado" (`docs/execution/waves/07-beta-release.md:29`).

| # | Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
|---|---|---|---|---|---|
| 1 | A razão 63:88 é o contrato das variantes de carta | PRONTO_E_PROVADO | `lib/core/widgets/card_artwork.dart:66` (`mtgCardAspectRatio = 63 / 88`), `:72-93` (gallery/spotlight/recentDeck/fullCard) | `test/core/widgets/card_artwork_test.dart:114-125` afirma `aspectRatio == 63/88` e `fit == contain` nas 4 variantes; `:168-205` afirma, só para `fullCard`, que o `AspectRatio` montado é 63/88 e que `CachedNetworkImage.fit` é `contain` | — **[rev]** O mesmo widget também oferece `artCrop` (16:9) e `setArt` (3:2), ambas com `BoxFit.cover` (`card_artwork.dart:94-103`), e o teste `:127-135` trava as duas como `cover`. Ver #3. |
| 2 | A moldura 63:88 é aplicada nas superfícies de carta | PARCIAL | 41 das 44 chamadas de `CardArtwork(` em `lib/features` passam `constrainAspectRatio: false` e escolhem a própria moldura. Fora de 63:88 (0,716): `lib/features/decks/screens/deck_details_screen.dart:1261-1262` (48×62 = 0,77), `lib/features/decks/widgets/deck_diagnostic_panel.dart:815-816` (48×64 = 0,75), `lib/features/decks/widgets/deck_analysis_tab.dart:2202-2203` (48×64), `lib/features/trades/screens/trade_detail_screen.dart:505-507` (48×64). **[rev]** Com desvio de 2% a 3%: `deck_optimize_sheet_widgets.dart:1281` (pai 45×64) e `deck_import_screen.dart:1162` (pai 82×112). A altura do spotlight da lista vem do `Positioned` pai (`deck_list_screen.dart:1524-1540`) | nenhum | Ajustar as molduras e criar guarda (teste estático ou de geometria) que falhe com moldura de carta fora de 63:88. Como o fit é `contain`, a imagem não distorce: o problema é a letterbox e o selo, que ficam fora da carta. |
| 3 | `contain` (sem corte) em toda imagem de carta | PARCIAL | `card_artwork.dart:74-93` e `lib/core/widgets/cached_card_image.dart:66` usam `contain`. Violação: `lib/features/social/screens/user_profile_screen.dart:696-701` e `:820-825` (`CachedCardImage(... fit: BoxFit.cover)` na carta do comandante), o que contraria também `docs/MANALOOM_CARD_ART_SOURCE_CACHE_AND_RIGHTS_CONTRACT.md` §Decisão 2. **[rev]** O próprio widget compartilhado expõe `CardArtworkVariant.artCrop` e `.setArt` com `BoxFit.cover` (`card_artwork.dart:94-103`); hoje nenhuma chamada as usa (grep), mas nada impede o uso | `test/core/widgets/card_image_source_policy_guard_test.dart:6-35` só barra construtores de rede crus; `:38-45` só barra a **string** `art_crop`. **[rev]** `CardArtworkVariant.artCrop` não contém essa string e passaria na guarda. Nada barra `BoxFit.cover`. O teste `card_artwork_test.dart:127-135` afirma o contrário do contrato de arte | Migrar as duas chamadas para `CardArtwork`. **[rev]** Remover ou proibir `artCrop`/`setArt`, virando o teste `:127-135`, e estender a guarda a `BoxFit.cover` e às variantes com corte. |
| 4 | Exact-first: impressão persistida primeiro, nome só como referência | PRONTO_E_PROVADO **[rev: vale para o modelo e para o caminho nativo; o Web está na #10]** | modelo: `lib/features/decks/models/deck_card_item.dart:121-177` (`printingImageUrl`, `effectiveImageUrl`, `fallbackImageUrl`; DFC usa a face frontal); widget: `cached_card_image.dart:235-239` e `:325-354` (primária, depois fallback) | `test/features/decks/models/deck_card_item_test.dart:122-229` (URL exata vs `/cards/named`, endpoint oracle é referência, DFC); `card_artwork_test.dart:19-65` (a primária falha, o fallback é pedido e o selo vira `reference`); `test/features/decks/widgets/deck_optimize_dialogs_test.dart:870-925` (arte exata no hover, `CMM #380`, sem "Arte de referência") e `:1052-1087` (payload legado cai no nome e mostra "Arte de referência") | — |
| 5 | Fallback rotulado no widget (referência, sem imagem, sem conexão, falha, baixa resolução) | PRONTO_E_PROVADO | `card_artwork.dart:299-369` (rótulo semântico e selo visível) | `card_artwork_test.dart:207-234` (selo `reference` e texto "Referência" a 180 px), `:236-266` (a 92 px, só ícone), `:268-325` (`missing`/`offline`/`error` distintos), `:327-362` (baixa resolução só depois de pronto) | — **[rev]** O texto visível só aparece com largura ≥120 px (`card_artwork.dart:283`). Abaixo disso, o teste `:236-266` afirma que o texto some e fica só o ícone com `Tooltip`. Quase todas as miniaturas de lista têm entre 34 e 92 px, então nas listas o rótulo visível é um ícone (ver #6 e A11Y #9). |
| 6 | Fallback rotulado nas superfícies reais | PARCIAL | 11 chamadas com `showStatusBadge: false` (e 1 condicional: `binder_import_screen.dart:724`). Entre elas estão o herói do deck (`lib/features/decks/widgets/deck_details_overview_tab.dart:276`, com arte de referência sem rótulo visível) e o histórico SAI→ENTRA (`lib/features/decks/widgets/deck_workshop_tab.dart:705`). 13 chamadas diretas a `CachedCardImage` ficam fora do contrato: `battle_coach_screen.dart:1761,1962,2112,2488`, `battle_replays_screen.dart:5376,5497`, `user_profile_screen.dart:696,820,1252,1600`, `community_screen.dart:995,1163,1848`. Abaixo de 120 px o selo é só ícone, e o texto fica no `Tooltip` (`card_artwork.dart:283`, `:418`) | `test/features/decks/widgets/deck_details_overview_tab_test.dart:388` **trava** `showStatusBadge == false` no herói, o que é contraprova | Ligar o selo no herói e no histórico, virar a asserção do teste, migrar as 13 chamadas (as de battle/social estão OFF e podem entrar numa lista de exceção datada) e dar texto ao selo compacto. |
| 7 | O estado "sem conexão" é alcançável | PARCIAL | o estado existe (`card_artwork.dart:24-31`, `:182-191`), mas nenhuma chamada em `lib/features` passa `offline:` (grep vazio) e o app não tem sinal de conectividade (`lib/core/resilience/offline_capability.dart` só mapeia erro; nenhum pacote de conectividade em `lib/` nem no `pubspec.yaml`). Sem rede, a carta cai em "Falha na imagem" | só no widget (`card_artwork_test.dart:268-325`) | Decidir a fonte do sinal offline, ou aceitar "falha" como estado e tirar "offline" do contrato. |
| 8 | Nome visível quando não há imagem | PARCIAL | o fallback compartilhado desenha só o verso com glifo (`cached_card_image.dart:421-511`); o nome existe só na semântica (`card_artwork.dart:299-319`). Há fallback com nome, mas local: `deck_optimize_sheet_widgets.dart:3118-3159`, `battle_coach_screen.dart:1767`. Nas linhas de lista o nome costuma estar ao lado | nenhum no contrato | Slot `fallbackName` (ou equivalente) no `CardArtwork`, mais um teste que afirme o texto visível em `missing` e `error`. |
| 9 | Sem layout shift | PRONTO_SEM_PROVA | a moldura é reservada antes do load por `AspectRatio` (`card_artwork.dart:295-296`) ou pelo `width/height` da chamada. **[rev]** Quando a chamada não dá altura e o pai não limita, o `Stack(fit: expand)` (`:258-260`) falharia com altura infinita, então toda chamada que funciona tem caixa fixa. Loading, erro e placeholder têm o mesmo tamanho (`cached_card_image.dart:362-418`). As 13 chamadas diretas também passam largura e altura fixas | nenhum teste mede o tamanho antes e depois do decode (sem `getSize`/`getRect` nos dois arquivos de teste) | Um teste de geometria: tamanho igual em loading, pronto e erro. |
| 10 **[rev: nova]** | Exact-first e fallback rotulado no caminho **Web** (`_ScryfallWebCardImage`) | PRONTO_SEM_PROVA | no Web, quando a URL primária ou a de fallback é do Scryfall, o widget desvia para outra implementação (`cached_card_image.dart:284-301`), com gate, retry e troca para o fallback próprios (`:604-777`) e estados `primaryReady`/`fallbackReady`/`failed` no `frameBuilder` e no `errorBuilder` (`:779-831`). Isso é código diferente do testado nas #4 e #5 | nenhum teste que rode. `flutter test` roda na VM (`scripts/quality_gate.sh:110-114`, sem `--platform chrome`), então `kIsWeb` é falso: o caminho nunca é montado e os ramos `if (kIsWeb)` de `cached_card_image_test.dart:137,171,203` nunca executam. As capturas Web usam a imagem primária servida em loopback (`scripts/manaloom_authenticated_visual_qa_isolated.sh:528-530`), que carrega de primeira; a troca exata→referência→erro nunca acontece nelas | Teste em `--platform chrome` (ou E2E Web) que force a falha da primária e afirme o selo `reference` e depois `error`, e ligar essa execução no gate. |

**Dependência declarada (`BT-ART-01`):** real só em parte. O conteúdo técnico de `BT-ART-01` já existe em `docs/MANALOOM_CARD_ART_SOURCE_CACHE_AND_RIGHTS_CONTRACT.md` (status `accepted_for_free_beta`, 2026-08-05): impressão primeiro, referência rotulada, `contain`, sem proxy, sem `art_crop`, sem paywall, rate e cache. A guarda desse contrato está em `card_image_source_policy_guard_test.dart`. **[rev]** Essa guarda é mais fraca do que o contrato: ela não barra `CardArtworkVariant.artCrop` nem `BoxFit.cover` (ver #3). O registry marca `BT-ART-01` como TODO, mas o núcleo de `CardArtwork` é de `776b9e25d` (2026-07-23) e os estados rotulados de `e6737c53b` (2026-08-10). IMG-001 pode andar hoje sobre esse contrato. Na ordem do backlog, porém, `BT-ART-01` vem antes (§8 item 10, `:693-694`), e o modelo WIP-1 exige fechar ou redeclarar a dependência. A dependência só pesa de verdade se o redesenho quiser arte como fundo, com crop ou blur, que é exatamente o que a auditoria visual propõe como ganho rápido (`docs/design/visual-audit-2026-09-21/README.md:690-691`). O contrato de arte proíbe isso (§Decisão 2-3). **Não declarada:** o digest de UI está congelado por `BT-UIEV-001`. **[rev]** Qualquer mudança em `app/lib` deixa defasadas as **430** capturas dos 22 manifests correntes em `8bba809c`, e não só 216: são 216 dos packs Web, 160 do p0-matrix Web e 54 do emulador (soma dos `checkpoint_count` dos manifests referenciados por `docs/qa/ui-live/latest.json`).

**O que a auditoria e a spec do kit resolvem aqui:** nada do aceite. A auditoria confirma que em produção a arte é real e que a das capturas é de fixture. Ela aponta que o problema é o espaço (miniaturas de 34 a 92 px; herói regrediu para 78×109 em `e6737c53b`, `deck_details_overview_tab.dart:262-265`), mas os ganhos rápidos dela conflitam com o contrato de arte. A spec do kit (`docs/design/ui-kit-spec.md`) **não define slot de carta**: a "peça com miniatura" `AppChoiceTile` usa miniatura de `100% × 30–66px` (`ui-kit-spec.md:1038`), que só cabe com crop e é incompatível com 63:88 `contain`.

**O que realmente falta [rev]:** conformidade e dois itens do aceite. O widget já faz 63:88, `contain`, exact-first e rótulos, e isso está provado no caminho nativo. Faltam: (a) corrigir as molduras fora de 63:88 e os 2 `BoxFit.cover`; (b) ligar o selo no herói e no histórico, virando o teste que hoje trava o selo desligado; (c) migrar 13 chamadas diretas a `CachedCardImage`; (d) mostrar o nome quando não há imagem; (e) provar a ausência de layout shift e endurecer a guarda (sem `CachedCardImage` direto, sem `cover`, moldura 63:88); **(f)** remover ou proibir `artCrop`/`setArt` no widget; **(g)** provar o caminho Web; **(h)** texto visível no selo compacto, que é onde vive a maioria das superfícies. São cerca de 13 arquivos de `lib/`, 4 de teste (um deles novo, para o Web), 1 doc (contrato de arte) e o script do gate: 18 arquivos e 9 testes novos. Sem migração. Precisa de recaptura das superfícies de deck, e isso só depois de fechar `BT-UIEV-001`. "Quase lá" subestimava: dois dos seis itens do aceite não existem, o rótulo visível abaixo de 120 px é só ícone e a plataforma principal da beta não tem prova.

---

## BT-UX-FIX-001 — fixtures realistas e patológicas de decks/trocas

**Aceite:** "100 cartas reais, DFC, nomes longos, sem imagem, offline, moedas e extrema densidade." Sem dependência declarada.

**Achado principal:** existe trabalho substancial **fora do HEAD**, no worktree `/Users/desenvolvimentomobile/.codex/worktrees/manaloom-bt-ux-fix-001` (branch `codex/bt-ux-fix-001`). A base, `0779595e2`, não é ancestral do HEAD: o merge-base é `406d7dd53`, e o HEAD está 20 commits à frente dele. Esse worktree não foi commitado, está parado desde 2026-08-28 e tem ficha própria (`docs/execution/tasks/BT-UX-FIX-001.md`, só nele) com veredito `IMPLEMENTED_LOCAL_UI_REFLOW_GREEN_BLOCKED_BY_PROJECT_LOGIC_COLD_REPRO`. Arquivos novos: `app/test/support/ux_pathological_product_fixtures.dart` (450 linhas), `..._fixtures_test.dart` (133), `app/test/features/decks/screens/deck_pathological_layout_test.dart` (337), `app/test/features/trades/screens/trade_pathological_layout_test.dart` (329). **[rev]** As correções de reflow somam 466 inserções e 133 remoções em 5 arquivos de produção: `deck_list_screen.dart` (+197), `deck_progress_indicator.dart` (+118/−66), `trade_detail_screen.dart` (+71/−32), `deck_add_cards_menu.dart` (+67/−27) e `deck_details_screen.dart` (+13/−8). As 525 linhas citadas antes incluíam 59 de `docs/execution/CURRENT_QUEUE.md`. O estado TODO do registry está desatualizado.

| # | Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
|---|---|---|---|---|---|
| 1 | 100 cartas reais, versionadas e determinísticas | PARCIAL | HEAD: não há corpus (`app/test/support/` só tem `layout_test_tools.dart` e `list_tile_material_test_support.dart`). A fonte existe: `server/test/fixtures/deck_import_sample.json` (100 linhas, sha256 `19b5724f…`, conferido). Worktree: `ux_pathological_product_fixtures.dart` faz o parse com pin de hash | worktree `ux_pathological_product_fixtures_test.dart`: 100 entradas, 100 nomes únicos, 1 comandante (Atraxa), determinismo, rejeita fonte malformada | Commitar. Os dados não são realistas: `type_line` "Imported card fixture", `mana_cost` vazio, cores vazias, `rarity` "fixture_unknown", sem `oracle_id` nem imagem (worktree `ux_pathological_product_fixtures.dart:54-89`). Por isso curva, cores e funções da aba Análise nunca são exercitadas. A própria ficha põe "provar impressão exata das 100 cartas" fora do escopo. |
| 2 | DFC | PARCIAL | worktree: `doubleFacedCardWithoutArtwork()` (`:290`). A lista de 100 não tem nenhuma DFC (conferido: zero linhas com `//`). HEAD: modelo DFC em `deck_card_item.dart:28-78` | HEAD `deck_card_item_test.dart:191-229` (só modelo); worktree: só o teste do corpus | Nenhum teste de tela consome a DFC (lista de cartas, troca, leitor). |
| 3 | Nomes longos e Unicode | PARCIAL | worktree: `longUnicodeDeckName` e `longUnicodeCardName`, usados nos testes de lista, detalhe e Trade | worktree (PASS declarado na ficha); nada no HEAD | Commitar; usar também no par de troca. |
| 4 | Sem imagem | PARCIAL | worktree: todas as imagens `null`; não há mistura realista de exata, referência e ausente | HEAD: só no widget (`card_artwork_test.dart:268-325`) | Deck com os três estados de arte misturados. |
| 5 | Offline | NAO_ENCONTRADO (em tela) | worktree declara `UxFixtureNetworkState.offline` e a mensagem (`:92`, `:127`, `:446-448`), mas nenhum teste de tela a usa (grep vazio nos dois testes de tela) | — | Teste de tela de deck e troca em estado offline. |
| 6 | Moedas (BRL/USD) | PARCIAL | worktree suporta as duas; os testes de tela só renderizam BRL (sem "USD" nos testes de tela) | worktree: o teste do corpus afirma os valores | Renderizar USD e checar o formato. |
| 7 | Extrema densidade | PARCIAL | worktree: detalhe de 100 cartas rolado até `deck-card-ux-commander-slot-100` em 320×568@200%, 390×844 e 1440×900; lista com 12 decks; Trade com 20+20 itens, 40 mensagens e 12 status. Isso achou overflow real em 5 arquivos de produção, que foram corrigidos | worktree: Deck 8/8 e Trade 6/6 (ficha; não re-executado aqui) | Commitar. **[rev]** O rebase é trivial, não "68 linhas": de `0779595e2` para o HEAD, dos 5 arquivos de produção só `deck_details_screen.dart` mudou (+9/−11, hunks entre `:291` e `:931`), e o worktree mexe em `:1421` e `:1919-1934`, sem sobreposição. As 68 linhas eram a diferença contra o merge-base `406d7dd53`, que não é a base do worktree. Os modelos de Deck e Trade não mudaram. |
| 8 | Fixtures de **trocas** SAI→ENTRA (Optimize) | NAO_ENCONTRADO | nem o HEAD nem o worktree têm payload patológico de `displayRemovals`/`displayAdditions`. O HEAD só tem 2 pares sintéticos (`test/features/decks/widgets/deck_optimize_dialogs_test.dart:748-755`) e os do pack da Oficina. O worktree leu "trocas" como **Trades** (social, capability `trades` OFF). No app, "Trocas" nomeia as duas coisas: Trades sociais (`home_screen.dart:1472`, `collection_screen.dart:68`) e o passo da Oficina (`deck_workshop_tab.dart:321`) | — | Corpus de trocas: 20+ pares com nomes longos, DFC, sem imagem, referência, preço BRL/USD, papel e impacto. |
| 9 | Fixture de captura em runtime realista | NAO_ENCONTRADO | `scripts/manaloom_authenticated_visual_qa_isolated.sh:530` e `:559` fazem `UPDATE cards SET image_url = <webp sintético>` em **todas** as cartas; `:574-575` monta o deck como 1 comandante + 99 cópias de um terreno básico | — | Deck de captura com 100 cartas distintas e estados de arte reais. Aqui há uma decisão sobre de onde vem a arte (ver abaixo). |

**Dependência declarada (nenhuma):** correta para o corpus em si. O trabalho existente estava travado por coisas não declaradas, e **[rev]** uma delas já caiu. (1) A reprodutibilidade fria do `project_logic`, que foi o bloqueio registrado na ficha do worktree, foi endereçada no HEAD por `354983a1e` (2026-09-04, "make project logic cold bootstrap reproducible on macOS", PASS_MACOS_COLD_REPRO) e por `d83e9b1e1` (2026-09-18). Os dois são ancestrais do HEAD. Falta só reexecutar `project_logic --check` a frio antes do commit (não rodado aqui). (2) O congelamento do digest de UI continua valendo: o worktree altera 5 arquivos de `app/lib`, e commitar deixa defasadas as 430 capturas de `BT-UIEV-001`. (3) O rebase é trivial (#7).

**Sobreposição:** as 5 correções de reflow do worktree adiantam `BT-UX-A11Y-001` (reflow) e `BT-UX-DECK-004` ("zero overflow a 320 CSS px e 200% texto"). A fixture de captura é pré-requisito de `BT-UX-PROOF-001`.

**O que a auditoria e o kit resolvem aqui:** só diagnóstico. A auditoria registra que a densidade real nunca foi julgada ("os fixtures têm 1 deck e 1 carta", `README.md:159`) e que a arte das capturas é de fixture. `docs/design/sequencia-e-esforco.md` lista as capturas que faltam (aba Cartas com 100 cartas, aba Análise, lista com muitos decks), o que dá o alvo que a fixture tem de alimentar. O kit declara que não tem peça para "lista longa" (`ui-kit-spec.md:1505`).

**O que realmente falta:** transformar o worktree em commit (aplicar o diff, reexecutar os testes e o `project_logic --check` a frio), depois de `latest.json` fechar. Depois, cobrir o que ele não cobre: o corpus de **trocas de Optimize**, a renderização de DFC, offline e USD, a mistura de estados de arte e dados de carta realistas para a aba Análise. Por fim, trocar a fixture de runtime, que hoje é "99 terrenos + arte sintética em tudo". São cerca de 16 arquivos (11 já escritos) e 7 testes a mais. Decisões: o que é "trocas", e se a fixture de captura pode versionar imagens reais de carta. O contrato de arte diz que o produto "não republica um catálogo de imagens". **[rev]** O emulador desta máquina **voltou a ter rede** em `9a9ba66de` (2026-09-21: o AVD `ManaLoom_API34` estava quebrado e foi apagado com `-wipe-data`; a captura Android saiu com zero falhas de imagem). A fixture Web usa loopback por escolha, e o comentário do script diz que a baseline "must not depend on Scryfall/CDN availability" (`:528-529`). Arte real na captura é, portanto, uma troca entre determinismo e realismo, e não um bloqueio técnico.

---

## BT-UX-SWAP-001 — troca visual pareada `SAI → ENTRA` com imagens integrais

**Aceite:** "Nome, impressão/estado, motivo curto, função e impacto; imagem não substitui texto." Depende de `BT-UX-IMG-001` e `DCK-P0-02`. A onda 07 resume assim: "imagens integrais, impressão/estado, motivo/função/impacto e texto equivalente" (`07-beta-release.md:35`).

| # | Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
|---|---|---|---|---|---|
| 1 | Par SAI → ENTRA como unidade | PRONTO_E_PROVADO (seleção) | `lib/features/decks/widgets/deck_optimize_sheet_widgets.dart:2076-2279` ("SAI" `:2207`, "ENTRA" `:2215`), lista `:564-617`. **[rev]** O ramo lado a lado (`:2222`, largura ≥650) é **inalcançável**: o conteúdo do diálogo é `SizedBox(width: 560)` (`:716-717`) e o par tem 12 px de padding (`:2158`), então a largura máxima é 536 px e o par **sempre empilha**. É a "condição impossível de breakpoint" de SWAP-002 | `deck_optimize_dialogs_test.dart:716-795`: a lista pareada existe, e desmarcar o par tira remoção **e** adição do índice 0. O teste roda a 1000 px e mesmo assim cai no ramo empilhado. `integration_test/deck_workshop_visual_runtime_proof_test.dart:752-755` gera `deck_workshop_03_paired_swaps.png` (digest `8bba809c`) | **[rev] Risco:** o par é montado por índice (`:565-582`) e o payload não tem id de par. O hash de integridade ignora a ordem (`server/lib/ai/optimize_swap_integrity.dart:17-18`, `:64-65`), e as duas listas `*_detailed` são filtradas separadamente (`server/routes/ai/optimize/index.dart:2958`, `:2999`). O servidor tenta manter os pares (`:2115-2122`), mas nada no contrato garante que o índice i de saída e de entrada formem o par que a tela mostra. |
| 2 | Imagens integrais dos dois lados | NAO_ENCONTRADO | a linha `_SelectableSuggestionLineItem` (`:2641-2760`) não tem `CardArtwork`; a arte só aparece sob demanda no hover e no leitor (`:2832-2852`, `:2995`), atrás de um `IconButton` de 48 px com ícone de 20 px (`:2741-2748`). A captura atual confirma: par só com texto | — | Uma carta inteira 63:88 `contain` em cada lado, exact-first e rotulada (depende de IMG-001), e caber no layout empilhado de 536 px. |
| 3 | Nome | PRONTO_SEM_PROVA | `:2692-2700` (`'$displayName$suffix'`); o sufixo sai em inglês ("HIGH 88%"). **[rev]** A confiança do sufixo é a mesma em todos os itens da resposta (`themeProfile.confidence`, `server/routes/ai/optimize/index.dart:2924,2947,2985`) | nenhum teste afirma o nome dentro do par. O runtime só procura "HIGH 93%". **[rev]** `deck_optimize_dialogs_test.dart:561` acha "Mystic Remora" com `findsAtLeastNWidgets(1)`, mas o mesmo nome está nas referências meta desse teste (`:512-514`), então não prova o par | Teste; sufixo em pt-BR ou removido, já que não diferencia item nenhum. |
| 4 | Impressão (set/nº) | NAO_ENCONTRADO no par | só no leitor e no hover: `_RecommendationEditionMetadata` (`:3252-3270`) | no leitor: `deck_optimize_dialogs_test.dart:913` ("CMM #380") | Linha de impressão no par. Hoje ela depende de `loadCard` por carta (até 40 chamadas para 20 pares); o payload do servidor não traz set/nº/imagem por item (`optimize_payload_support.dart:529-553`; os detalhes mesclados só trazem preço e coleção, `optimize_route_recommendation_context_support.dart:450-470`). |
| 5 | Estado (aprovada / fora do plano; exata / referência) | PARCIAL | "Aprovada"/"Fora do plano" em `:2193-2199`; estado da arte ausente (não há arte) | nenhum afirma o texto | Estado da impressão no par. |
| 6 | Motivo curto | PARCIAL | `_pairReason` `:2096-2107`, mostrado em `:2265`; o mesmo motivo repete em cada lado (`:2717-2731`). **[rev]** O motivo que chega do servidor é modelo fixo: "Adicionar X para reforçar {função} no plano {arquétipo}." ou "Remover X para abrir espaço sem perder o plano {arquétipo}." (`server/lib/ai/optimize_payload_support.dart:634-637`). `_pairReason` pega o da entrada primeiro, então o "motivo do par" é o modelo da entrada | nenhum | Motivo real do par (por que X sai para Y entrar), único e curto, sem duplicata. Exige trabalho no servidor. |
| 7 | Função | PARCIAL | o papel é o 1º item de uma sopa de metadados unida por " • " (`:2621-2630`), truncada em 2 linhas. **[rev]** Pela leitura do servidor, as **entradas não têm função**: o builder das adições não recebe `functionalRole` (`index.dart:2941-2952`; só as saídas recebem, `:2972-2994`), então cai no padrão `'utility'` (`optimize_payload_support.dart:486-489`), e o rótulo devolve a palavra crua em inglês (`:694-695`). O app usa `primary_role_label` antes do próprio tradutor (`deck_optimize_sheet_widgets.dart:2592-2596`). Resultado provável: toda ENTRA mostra "utility" (não executado) | nenhum | Função da entrada calculada no servidor, rótulo pt-BR próprio, sem truncar. |
| 8 | Impacto | **NAO_ENCONTRADO** **[rev: era PARCIAL]** | o servidor manda `impact_estimate` por item (`optimize_payload_support.dart:546-552`), e o app não lê (grep vazio em `app/lib`). Mas o campo **não é do item**: `curve` é a variação do CMC médio do deck inteiro (`index.dart:2846-2852` passa o mesmo `preCmc`/`postCmc` para todos); `consistency` depende só de `keepTheme`; `synergy` depende só do tipo ("melhora" para toda adição); `legality` é a constante "mantida". Prioridade e risco dependem só da intensidade (`index.dart:2855-2856`). O "Curva ±x" que aparece em cada linha (`optimize_payload_support.dart:648`) é o mesmo número em todas. O único impacto real é o agregado do plano (`_MetricDiffRow` `:1821`) | nenhum | Calcular o impacto **por par** no servidor (curva, função coberta ou perdida, legalidade), levar ao payload, testar e só então renderizar. Mostrar o campo atual como impacto do par seria exibir placebo. |
| 9 | Imagem não substitui texto | **PARCIAL** **[rev: era PRONTO_SEM_PROVA]** | só é verdade por vacuidade: não há imagem. O rótulo semântico do par é genérico, "Troca N: saída e entrada pareadas" (`:2151-2155`), e anuncia `checked` num contêiner sem ação (a ação está no checkbox) | nenhum | Quando a imagem entrar: texto sempre visível e rótulo semântico "Sai X, entra Y". Não conta como pronto numa entrega cujo núcleo é a imagem. |

**Dependências declaradas:** `BT-UX-IMG-001` é real, porque as imagens do par herdam o contrato. `DCK-P0-02` é parcial. A UI pode ser montada sobre o payload atual (`paired_selection_required` em `optimize_payload_support.dart:592`, dentro de `buildOptimizeDecisionContract` `:556-600`; integridade SHA-256 e autorização HMAC em `server/lib/ai/optimize_swap_integrity.dart:21-25`). O que só nasce com `DCK-P0-02` é o estado do artefato (revisão, expiração, stale). `DeckReviewArtifact` não existe no código (grep vazio) e está a três elos: `BT-DB-001` → `DCK-P0-01` → `DCK-P0-02`, todos TODO. **Não declaradas:** (1) `BT-UX-KIT-001`, que a fila **em árvore de trabalho, sem commit** (`docs/execution/CURRENT_QUEUE.md:55`) põe na posição 2, logo após `BT-UIEV-001`. `docs/design/sequencia-e-esforco.md:46` diz que a troca "é o caso perfeito para peça com miniatura". Fazer SWAP-001 antes do kit é fazê-la duas vezes. (2) Dados do servidor: impressão e imagem por item no payload. **[rev]** (3) Cálculo no servidor, por par, de função da entrada, motivo e impacto (#6, #7, #8). Hoje isso não existe e não é "renderizar um campo". (4) A capability `ai_analyze_optimize_advisory` está `off`, então a superfície é inalcançável na beta.

**Sobreposição:** com `BT-UX-SWAP-002` (mesmo `AlertDialog`, conteúdo de 560 px em `deck_optimize_sheet_widgets.dart:716-717`, e o breakpoint de 650 px que nunca é atingido), `BT-UX-SWAP-003` (motivo e metodologia), `DCK-P1-07` (apply) e o histórico da Oficina, que já desenha SAI→ENTRA com miniaturas de 34×48 e selo desligado (`deck_workshop_tab.dart:624-740`). O ideal é um único componente de par para os dois.

**O que a auditoria e o kit resolvem aqui:** direção, não código. A auditoria confirma que a ausência de arte é do layout e não do fixture (`README.md:846`, `:972`, `:1060`). Os ganhos rápidos dela: miniatura na linha, tile inteiro carregando o estado, tirar os checkboxes, um herói só no rodapé. O redesenho proposto é uma "mesa de trocas em tela cheia… par de cartas com arte lado a lado" (`README.md:1000`). O kit dá `AppTileOverlay` (substituto do `AlertDialog`), `AppHeroTile`/`AppActionBar` (herói único) e a guarda do §9 contra `AlertDialog`. Mas a miniatura do kit (`100% × 30–66px`) conflita com a imagem integral 63:88.

**O que realmente falta [rev]:** o coração da entrega, as imagens integrais, não existe. Impressão, estado da arte e impacto por par também não. Dos atributos textuais, só o nome existe de fato. Função, motivo e confiança existem na tela, mas o que o servidor manda é constante para o deck inteiro ou vem de modelo fixo, e a função das entradas é, pela leitura, sempre "utility". O layout lado a lado nunca aparece. O trabalho tem duas metades: servidor (função da entrada, motivo e impacto por par, impressão e imagem no payload, e de preferência um id de par) e app (componente de par com imagem, compartilhado com o histórico da Oficina, layout que caiba no container real). São cerca de 13 arquivos: 5 de app, 3 de servidor e 5 de teste, incluindo o integration test da Oficina. Mais 10 testes, dos quais 3 a 4 de servidor. Sem migração. Precisa de prova viva. Antes de começar, o dono tem de decidir a ordem em relação a `BT-UX-KIT-001`, se a tela nasce antes de `DCK-P0-02` e o que "impacto do par" deve medir.

---

## BT-UX-A11Y-001 — semântica, contraste, foco, reflow e dados alternativos dos gráficos

**Aceite:** "Contraste WCAG, 48 dp, TalkBack lógico, teclado real e informação nunca só por cor/tooltip." Depende de DECK-001..004 e SWAP-001..003.

| # | Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
|---|---|---|---|---|---|
| 1 | Contraste WCAG dos pares canônicos do tema | PRONTO_E_PROVADO | tokens em `lib/core/theme/app_theme.dart:35-155` | `test/ui/ui_accessibility_matrix_test.dart:74-140`: 6 pares de texto ≥4,5 e 3 de controle ≥3 | — (**[rev]** calculei os tokens do par sobre o fundo real do par, `surfaceElevated` a 52%: todos passam de 5:1, incluindo `brass400` em 12 px. O risco de contraste aqui é baixo; o que falta é a prova na tela, #2.) |
| 2 | Contraste nas superfícies de deck e troca | PARCIAL | `textContrastGuideline` só via `expectManaLoomBaselineAccessibility` (`test/ui/support/manaloom_ui_audit_harness.dart:68-74`), usado em 8 arquivos de teste; em Decks só no Gerador (`test/features/decks/screens/deck_flow_entry_screens_test.dart:499`). A matriz lista para o domínio `decks` só `deck_flow_entry_screens_test.dart` e `deck_list_responsive_test.dart` | idem | Rodar a guideline em Deck Details (visão geral, Cartas, Análise), no diálogo de troca e na Oficina. |
| 3 | Alvos ≥48 dp | PARCIAL | o token existe (`app_theme.dart:218`, `touchTargetMin = 48`) e o botão de prévia tem 48 (`deck_optimize_sheet_widgets.dart:2741-2748`). Mas o **único** controle que alterna o par é um `Checkbox` com `VisualDensity.compact` (`:2175-2180`): no par, a linha é montada sem `onChanged` e com `showCheckbox: false` (`:2136-2144`). O checkbox das listas não pareadas também é compacto (`:2679-2684`). Pela regra do Flutter, compact tira 8 dp: fica 40×40 no Android (32×32 em desktop). O tema fixa só `visualDensity: standard` (`app_theme.dart:226`) e não sobrescreve `materialTapTargetSize` | nenhum `androidTapTargetGuideline` nessas telas | Tile inteiro como alvo (o que o próprio kit propõe) e teste de guideline. Violação provável, não medida. |
| 4 | TalkBack lógico (humano) | NAO_ENCONTRADO | `test/ui/fixtures/ui_accessibility_matrix.json` → `manual_screen_reader.android.status = "pending_physical"`, `completed_surface_ids = []`, travado pelo teste `ui_accessibility_matrix_test.dart:142-150`; `docs/qa/ui-live/latest.json` → `release_checks.android_talkback_human = "pending"` | — | Sessão humana no Android físico, a mesma pendência de `BT-QA-001`. |
| 5 | Semântica (nome, papel, estado) | PARCIAL | a arte tem `Semantics(image, label+estado)` (`card_artwork.dart:249-253`, `:299-319`), provado. `IconButton` sem tooltip é barrado (`ui_accessibility_matrix_test.dart:47-72`). O rótulo do par é genérico (`:2151-2155`) e anuncia `checked` num contêiner sem ação. **Editar e remover carta do deck só existem por swipe** (`Dismissible`, `lib/features/decks/screens/deck_details_screen.dart:1114-1209`, sem nenhuma ação semântica rotulada no arquivo). O diálogo da carta só oferece "Trocar edição", "Explicar com IA", "Ver Detalhes" e "Fechar" (`lib/features/decks/widgets/deck_details_dialogs.dart:398-410`, `:574-599`). **[rev]** Conferido: o único chamador de `removeCardFromDeck` na UI é o swipe (`deck_details_screen.dart:1176`) | parcial (arte e tooltip) | Ação acessível alternativa ao swipe e rótulos que nomeiem as cartas do par. |
| 6 | Teclado real | PARCIAL | widget: `test/ui/ui_keyboard_focus_matrix_test.dart` (tab, shift-tab, enter, space, escape, trap, restauração, foco visível) e `deck_optimize_dialogs_test.dart:923-968` (o foco abre a prévia; Esc mantém o diálogo). Humano: `ui_keyboard_focus_matrix.json` → `manual_web.status = "pending"`; `latest.json` → `web_hardware_keyboard = "pending"`. **[rev]** A sessão Web real já observou `/decks/:id` com 10 verificações aprovadas (`ui_keyboard_focus_matrix.json:24-27`). O status é `pending` por causa das rotas Battle. Essa sessão não pegou o swipe-only, então não exercitou editar/remover. O swipe é inalcançável por teclado | widget sim; humano parcial e em digest antigo | Sessão de teclado Web real nas superfícies novas, cobrindo editar e remover. |
| 7 | Reflow (320 px, 200%) | PARCIAL | HEAD cobre lista e criação de deck a 320×568@200% (`test/features/decks/screens/deck_list_responsive_test.dart:951`, `:1050`), Gerador (`deck_flow_entry_screens_test.dart:467-501`) e mão de exemplo a 320 (`test/features/decks/widgets/sample_hand_widget_test.dart:352`). Deck Details, troca e Análise têm teste só a 360 ou 390 px e sem 200% (`deck_optimize_dialogs_test.dart:642`, `:978`; `deck_details_overview_tab_test.dart:402`). O worktree de FIX-001 cobre detalhe e lista a 320×568@200% (não commitado) | parcial | Commitar FIX-001 e cobrir troca e Análise. |
| 8 | Dados alternativos dos gráficos | NAO_ENCONTRADO (curva) | `BarChart` (`lib/features/decks/widgets/deck_analysis_tab.dart:349-405`) sem valor impresso e sem `Semantics`: o valor só aparece no tooltip (`BarTouchData` `:357-363`). A pizza é donut (`centerSpaceRadius: 40`, `:434-440`) e tem legenda com nome e contagem (`:546-583`) | `test/features/decks/widgets/deck_analysis_tab_test.dart:394` só procura o título "Curva de mana" | Valor impresso, resumo textual e lista de dados acessível (sobrepõe `BT-UX-DECK-003`). |
| 9 | Informação nunca só por cor ou tooltip | PARCIAL | SAI e ENTRA têm texto e ícone (`:2207-2215`), ok. Os valores da curva ficam só no tooltip. O selo de arte abaixo de 120 px é só ícone, com o significado no `Tooltip` (`card_artwork.dart:283`, `:418`) | nenhum específico (a matriz declara `color_redundancy`, mas nada o testa em Decks) | Texto visível no selo compacto e nos valores. |
| 10 | As superfícies-alvo existem | NAO_ENCONTRADO | DECK-001, DECK-003 e SWAP-002 estão BLOCKED_BY_P0; DECK-002, DECK-004, SWAP-001 e SWAP-003 estão TODO (registry) | — | Construir antes de certificar. |

**Dependência declarada:** real para fechar, porque não se certifica acessibilidade de tela que não existe. Mas uma linha de base pode rodar hoje nas telas atuais, e os defeitos acima já valem. A cadeia real é mais longa que a declarada: DECK-001 e DECK-003 dependem de `DCK-P1-04`/`DCK-P1-06` → `DCK-P0-01` → `BT-DB-001`. **Não declaradas:** o TalkBack humano no Android físico e a sessão de teclado Web real. O único aparelho físico é o celular do dono (`R58T300SREH`), e o receipt de `BT-UIEV-001` exige autorização explícita (`docs/qa/execution/2026-09-21/btuiev001-chromedriver-e-recaptura.md:246-249`).

**Sobreposição:** com `BT-QA-001` (TalkBack e teclado humanos), `latest.json.release_checks`, `BT-UX-FIX-001` (reflow), `BT-UX-DECK-003` (gráfico com valor impresso) e a spec do kit.

**O que a auditoria e o kit resolvem aqui:** a spec do kit **pré-responde a parte de desenho** para superfícies futuras feitas nele. Receita de 48 dp com `Padding` + `ConstrainedBox` (`ui-kit-spec.md:362`); aro de foco em latão com contraste 6,60 (`:366`); semântica por papel (`:350-358`); regras de escala 200% (`:368`, `:377`); `disableAnimations`. Também mede 4 reprovações AA das peças (`:1062-1065`) e registra que as próprias peças do kit têm alvos de 44 e 46 dp (`:1497`). Tudo isso é texto: nada está em código, e várias decisões dependem do dono (§11). A auditoria não trata de acessibilidade.

**O que realmente falta:** quase tudo o que é específico do aceite. O contraste dos tokens está provado, e há harness e matrizes. Faltam: (a) guideline de contraste e 48 dp nas telas de deck e troca; (b) corrigir os checkboxes compactos; (c) alternativa acessível ao swipe de editar/remover; (d) dados alternativos da curva; (e) as sessões humanas de TalkBack e teclado. **[rev]** O número é um piso, porque as 7 superfícies ainda não existem e cada uma precisa de pelo menos um teste de guideline, semântica e 200%. São no mínimo 15 arquivos (as telas, os dois diálogos, `card_artwork.dart`, as duas matrizes JSON, os receipts humanos e `latest.json`) e 11 testes (7 por superfície, mais alternativa ao swipe, dados da curva, teclado no fluxo de troca e selo compacto). Sem migração. A prova humana exige a autorização do dono para usar o aparelho físico.

---

## BT-UX-PROOF-001 — evidência visual fresca de todas as superfícies alteradas

**Aceite:** "`PASS_AUTOMATED`, `PASS_RUNTIME`, todas as capturas abertas e `PASS_VISUAL_REVIEWED`." Depende de A11Y-001 e dos 7 UX. **[rev]** A onda 07 é mais dura que o backlog: "automated + runtime + todas as 402 Web e 54 Android **físico** abertas/revisadas no digest final; sem reutilizar stale" (`docs/execution/waves/07-beta-release.md:59`).

| # | Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
|---|---|---|---|---|---|
| 1 | Verificador dos 3 níveis | PRONTO_E_PROVADO | `tool/ui_runtime_evidence.dart:500-794` confere status por nível, digest, hash de manifesto e de PNG, dimensões, os 10 critérios e a cobertura da revisão | `test/tool/ui_runtime_evidence_test.dart:290-364` (monta um `latest.json` com os três níveis e verifica `evidence_levels` e a contagem de capturas), `:148` (rejeita digest velho), `:251` (rejeita checkpoint faltando), `:550` (rejeita crédito físico com captura de emulador), `:664` (rejeita revisão cujos hashes não cobrem a captura) | — |
| 2 | `PASS_AUTOMATED` das superfícies alteradas | NAO_ENCONTRADO | as superfícies não existem; os testes de a11y e reflow de Deck Details e troca também não (ver A11Y) | — | Vem junto com as 7 tarefas e com A11Y. |
| 3 | `PASS_RUNTIME` fresco | PARCIAL | em `8bba809c`: `p0-matrix` (`deck_detail_top`, `deck_detail_below_fold`) e o pack da Oficina (inclui `deck_workshop_03_paired_swaps`). **As abas Cartas e Análise nunca foram capturadas** (lista de checkpoints de `docs/qa/ui-live/current/p0-matrix/web_mobile_390x844.json`). Defasados e fora do agregado: `ux-pack-01-deck-detail-web` (`eef339fc`, 2026-08-03), `card-back-fallback-web` (`591dab35`), `optimization-card-reader-web` (`961ec03e`). **[rev]** O p0-matrix físico (`android_physical_sm_a135m.json`) está em `d517adb6` | — | Pack de Deck Details e troca (herói, diagnóstico, inspector, Cartas, Análise, par com imagens) em 390, 1440, 1920 e Android. |
| 4 | Todas as capturas abertas | PARCIAL | a política diz `reviewer_must_open_every_screenshot`. O verificador exige hash por checkpoint só quando há um manifesto único (`ui_runtime_evidence.dart:760-775`); no agregado confere só contagem e hash de manifesto (`:741-759`) | parcial | É autoatestado. Não existe prova por captura no agregado. |
| 5 | `PASS_VISUAL_REVIEWED` | PARCIAL | `docs/qa/ui-live/latest.json`: PASS de 2026-08-26 no digest `865e6041` (defasado), revisor `agent`/`Codex`, 439 capturas num único parecer. O critério `attractiveness` só exige `"pass"` e uma nota não vazia (`ui_runtime_evidence.dart:729-739`) | idem | Revisão real das novas capturas, com régua de beleza (ver decisão). |
| 6 | Superfícies declaradas na política | NAO_ENCONTRADO | `test/ui/fixtures/ui_live_evidence_policy.json` → `surfaces` tem 10 superfícies, nenhuma de Deck Details (herói, diagnóstico, inspector, abas) nem troca fora do pack da Oficina | — | Declarar a superfície, o script (no molde de `scripts/manaloom_deck_workshop_visual_qa.sh`) e o teste de integração. |
| 7 | Agregado verde no digest corrente | NAO_ENCONTRADO hoje | 22 de 23 manifests em `8bba809c` (430 capturas); `play-vs-ai-web-real` em `865e6041`; `latest.json` intocado de propósito (receipt `btuiev001-chromedriver-e-recaptura.md`, "O que NÃO fechou"; `docs/qa/execution/2026-09-21/PONTO_DE_RETOMADA.md:20-53`) | — | Fechar `BT-UIEV-001` primeiro. |

**Dependência declarada:** real. **Não declaradas:** `BT-UIEV-001` (gate e `latest.json`), `BT-UX-FIX-001` (sem fixture realista as capturas mostram arte sintética e 99 terrenos básicos) e `BT-UX-KIT-001` (se o Épico D migrar para o kit, a prova tem de ser refeita depois). **[rev]** Se valer a onda 07, também o Android físico. A política atual aceita emulador (`app/test/ui/fixtures/ui_live_evidence_policy.json:755`, `emulator_runtime_is_valid_for_current_scope: true`) e o contrato de evidência diz que o emulador valida o runtime Android (`docs/MANALOOM_UI_LIVE_EVIDENCE_CONTRACT.md:282-286`), mas a onda exige "54 Android físico". Com a onda, PROOF depende do mesmo celular do dono que o TalkBack. A própria fila põe PROOF na onda final (`docs/execution/CURRENT_QUEUE.md:117`).

**Sobreposição:** com `BT-UIEV-001` (mesmo gate), `BT-UX-KIT-001` (exige o próprio `PASS_VISUAL_REVIEWED` em 390/834/1440/1920; 834 não é perfil da política atual) e `BT-QA-001`, que depende de PROOF-001 no registry.

**O que a auditoria e o kit resolvem aqui:** a auditoria entrega o **checklist do que falta capturar**. Ela recomenda os checkpoints `deck_detail_cards`, `deck_detail_analysis`, `deck_card_edit_dialog` e outros em `integration_test/app_existing_user_visual_audit_test.dart` (`README.md:163`); a tabela "Capturas que faltam" está em `docs/design/sequencia-e-esforco.md`. Também mostra que beleza hoje é autoatestada: um único parecer cobre 439 capturas (`README.md:63`, `:145`). O kit acrescenta o perfil de 834 px e a comparação de espécime com o protótipo.

**O que realmente falta:** a infraestrutura está pronta e testada. Falta o objeto: não há superfícies alteradas para provar, a política não as declara, as abas Cartas e Análise nunca foram fotografadas e o agregado está defasado. **[rev]** Quando as dependências fecharem: cerca de 10 arquivos (política, script, 1 ou 2 testes de integração, checkpoints do p0-matrix, manifests gerados, revisão, `latest.json` e receipt) e 3 testes (pack novo, checkpoints novos do p0-matrix e verificador contra a política ampliada). Uma rodada de captura em 3 viewports Web mais Android, e revisão de todas as capturas do digest final, não só das alteradas. Decisões: quem assina o visual, e se o Android tem de ser físico.

---

## Achados transversais

1. **ABERTO = 0.** As 29 capabilities estão `off` (`release_capability: off`, `allowed: false`) em `server/config/release_capabilities.json`, inclusive `decks_private` e `ai_analyze_optimize_advisory`. Nenhuma superfície deste grupo é alcançável na beta hoje, independentemente do que for implementado.
2. **Digest de UI congelado [rev].** `BT-UIEV-001` está em 22/23; `latest.json` está em `865e6041` e as capturas em `8bba809c` (conferido agora com `scripts/manaloom_ui_source_digest.sh`). Qualquer edição em `app/lib` deixa defasadas **430** capturas em 22 manifests (216 dos packs Web, 160 do p0-matrix Web, 54 do emulador), e não só as 216 dos packs. As cinco tarefas mexem em `app/lib` ou dependem de captura: nenhuma deve entrar antes de `latest.json` ser reescrito.
3. **Conflito de ordem com o kit.** A fila em árvore de trabalho, sem commit, põe `BT-UX-KIT-001` na posição 2 (`docs/execution/CURRENT_QUEUE.md:55`; a linha do KIT no backlog também só existe na árvore de trabalho). O backlog §8 item 11 põe IMG, FIX e SWAP primeiro (`BREWTACT_MASTER_EXECUTION_BACKLOG_2026-08-12.md:695-698`). O kit redesenha as mesmas telas (`sequencia-e-esforco.md` Ondas 2.1 e 2.3). A decisão é do dono.
4. **Contrato de arte x direção visual.** `docs/MANALOOM_CARD_ART_SOURCE_CACHE_AND_RIGHTS_CONTRACT.md` proíbe crop, cover e blur. A auditoria propõe arte de fundo desfocada como ganho rápido (`README.md:690-691`), e a miniatura do kit (`ui-kit-spec.md:1038`) só cabe com crop. Nenhum dos dois documentos define um slot de carta 63:88. **[rev]** O próprio `CardArtwork` ainda oferece duas variantes com `cover` (`card_artwork.dart:94-103`), travadas por teste, e a guarda do contrato não as pega.
5. **A fixture de captura não é realista.** O script de QA autenticado sobrescreve a imagem de todas as cartas com um webp sintético e monta o deck com 99 terrenos iguais (`scripts/manaloom_authenticated_visual_qa_isolated.sh:530,559,574-575`). Nenhuma captura atual mostra arte real de carta.
6. **Trabalho fora do HEAD.** O worktree `codex/bt-ux-fix-001` (2026-08-28) tem 1.512 linhas de teste e ficha, mais 466 inserções em 5 arquivos de produção, sem commit. O estado declarado TODO está desatualizado. **[rev]** O bloqueio de commit dele (cold-repro) já foi resolvido no HEAD, e o rebase é trivial.
7. **Registry defasado em relação ao backlog.** O registry foi gerado de `sha256 7584fbd9…`. O backlog em árvore de trabalho é `064aec38…` e o do HEAD é `333b6c0b…`: o registry não bate com nenhum dos dois. As 5 linhas deste grupo não mudaram, mas por exemplo `BT-UIEV-001` aparece com `depends_on: BT-SCP-001` no registry e sem dependência no backlog (`:624`).
8. **`docs/design/` inteiro é untracked** e se declara "documentação de apoio, não autoritativa". A spec do kit tem condição de entrada explícita: nada vira código antes de `BT-UIEV-001` verde, worktree próprio, ok do dono e decisões do §11 respondidas.
9. **Prova humana nunca rodou.** TalkBack está `pending_physical` com 0 superfícies, o teclado Web está `pending` (com `/decks/:id` já observado) e a beleza é autoatestada por agente. Isso vale para A11Y, PROOF e `BT-QA-001` ao mesmo tempo.
10. **[rev] A plataforma principal da beta não tem prova de imagem.** Todo `flutter test` roda na VM (`scripts/quality_gate.sh:110-114`). O caminho Web de imagem de carta (`cached_card_image.dart:604-833`), com gate, retry e troca para o fallback, nunca é executado por teste; as capturas Web só exercitam o sucesso da primária em loopback.
11. **[rev] O conteúdo "por item" do Optimize é, em boa parte, do deck inteiro.** Confiança (`index.dart:2947`), prioridade e risco (`:2855-2856`), curva e `impact_estimate` (`:2846-2852`; `optimize_payload_support.dart:546-552`, `:648`) são iguais em todos os itens de uma resposta. O motivo é modelo fixo (`:634-637`) e a função das entradas cai no padrão "utility" (`:486-489`, `:694-695`). Isso não é defeito de UI: é o que torna o aceite de SWAP-001 (motivo, função, impacto por par) trabalho de servidor.
12. **[rev] Dois bloqueios citados na medição original já caíram:** o cold-repro do `project_logic` (`354983a1e`, `d83e9b1e1`) e a falta de rede do emulador (`9a9ba66de`, AVD apagado com `-wipe-data`).

---

## Verificação adversarial

Revisor cético, 2026-09-22, HEAD `d15beb05b`, só leitura. Reexaminei as 44 asserções da medição original e acrescentei 1 (IMG-001 #10). Abri cada teste citado como prova, a implementação de `CardArtwork`/`CachedCardImage` inteira, o diálogo de troca, o builder do payload do Optimize no servidor, o worktree de FIX-001 e os manifests de captura.

### O que caiu

| Tarefa | Asserção | De → para | Por quê |
|---|---|---|---|
| IMG-001 | Status da tarefa | quase lá → **metade** | Dois dos seis itens do aceite não existem: o nome sem imagem e a prova de ausência de layout shift. O rótulo visível abaixo de 120 px é só ícone, e é nessa faixa que vivem quase todas as miniaturas. O widget ainda oferece `artCrop`/`setArt` com `cover`, travadas por teste. O caminho Web não tem prova. Restam 18 arquivos, 9 testes, uma decisão de produto/jurídico e o bloqueio do digest. |
| IMG-001 | #10 Exact-first e fallback rotulado no Web | implícito como provado (#4/#5) → PRONTO_SEM_PROVA | `_ScryfallWebCardImage` (`cached_card_image.dart:604-833`) é outra implementação. `flutter test` roda na VM, `kIsWeb` é falso, e os ramos Web do teste nunca executam. As capturas Web não provocam a troca exata→referência. |
| SWAP-001 | Status da tarefa | metade → **mal começada** | O núcleo da entrega (imagens) não existe. Impressão e impacto por par não existem. Função, motivo e confiança chegam do servidor constantes ou em modelo fixo. O lado a lado nunca renderiza. Contagem revisada: 1/1/4/3. |
| SWAP-001 | #8 Impacto | PARCIAL → NAO_ENCONTRADO | `impact_estimate` e "Curva ±x" são do deck inteiro, iguais em todos os itens (`index.dart:2846-2856`; `optimize_payload_support.dart:546-552`, `:648`). Não há impacto por par em lugar nenhum. |
| SWAP-001 | #9 Imagem não substitui texto | PRONTO_SEM_PROVA → PARCIAL | É verdade só por vacuidade, porque não há imagem. |
| (números) | arquivos/testes | IMG 16/6 → 18/9; SWAP 9/7 → 13/10; A11Y 12/8 → 15/11 (piso); PROOF 7/2 → 10/3 | IMG: variantes com corte, teste Web e selo compacto. SWAP: trabalho de servidor não contado. A11Y: 7 superfícies × pelo menos um teste cada. PROOF: revisão do digest final inteiro e checkpoints novos. |
| (transversal) | capturas invalidadas | 216 → 430 | O digest cobre os 22 manifests correntes, não só os 18 packs Web. |

Também corrigi, sem mudar status: o ramo ≥650 px de SWAP #1 é código morto dentro do diálogo de 560 px; o pareamento é por índice, sem id de par, e com hash de integridade que ignora a ordem; o teste `deck_optimize_dialogs_test.dart:561` não prova o nome no par; e o total de linhas de produção do worktree é 466, não 525.

### O que subiu

| Tarefa | Asserção | Por quê |
|---|---|---|
| FIX-001 | Bloqueio de commit do worktree | O cold-repro do `project_logic`, que é o veredito de bloqueio da ficha, foi endereçado no HEAD por `354983a1e` (2026-09-04) e `d83e9b1e1` (2026-09-18). Sai da lista de decisões humanas; falta só reexecutar a frio. |
| FIX-001 | Rebase do worktree | Trivial: de `0779595e2` para o HEAD, só `deck_details_screen.dart` mudou (+9/−11), sem sobreposição com os hunks do worktree. Os modelos de Deck e Trade não mudaram. |
| FIX-001 | Arte real na captura Android | O emulador tem rede de novo (`9a9ba66de`). O que resta é uma escolha entre determinismo e realismo, não um bloqueio. |
| A11Y-001 | #6 Teclado real (continua PARCIAL) | Há sessão Web real registrada em `/decks/:id`, com 10 verificações (`ui_keyboard_focus_matrix.json:24-27`); ela só não cobriu editar e remover. |

Nenhuma asserção marcada PRONTO_E_PROVADO caiu de estado. Os testes citados afirmam o que a medição disse: 63:88 e `contain` no spec e no widget montado; troca primária→fallback no nativo; selos por estado; seleção pareada; contraste dos tokens; verificador de 3 níveis. Onde afirmam menos do que parecia, restringi o texto (#1 só `fullCard` montado; #5 texto só ≥120 px) e abri a asserção nova do Web, em vez de rebaixar o que está de fato provado.

### Veredito de otimismo

**Otimista demais**, concentrado em duas tarefas. Em IMG-001, "quase lá" venderia uma vitória rápida que ainda pede 18 arquivos, decisão de produto e jurídico e prova na plataforma principal da beta. Em SWAP-001, "metade" contava como existentes atributos que chegam do servidor como constantes ou modelo fixo, e escondia que metade do trabalho é de servidor. No sentido oposto, a medição foi pessimista em FIX-001: dois dos bloqueios citados já tinham caído no HEAD. A11Y-001 e PROOF-001 estavam justos no status e baixos nos números. PROOF ainda ganhou uma dependência que a medição não viu: a onda 07 exige Android físico.
