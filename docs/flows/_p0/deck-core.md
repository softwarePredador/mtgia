# Medição P0 CORE — Núcleo do deck (concorrência, revisão, preview/commit, exclusão, isolamento de sessão)

Data: 2026-09-22. Repo `/Users/desenvolvimentomobile/Documents/rafa/mtg/mtgia`, branch `codex/free-beta-release-candidate-2026-07-17`, HEAD `d15beb05b`. Somente leitura; nada foi executado (sem `dart test`/`flutter`).
Fonte do aceite: `docs/BREWTACT_MASTER_EXECUTION_BACKLOG_2026-08-12.md` l.278-285 e l.294; detalhamento em `docs/execution/waves/02-deck-foundation.md` l.8-17 e 38-46 e, para DCK-P0-00, `docs/execution/waves/01-platform-safety.md` l.22.
Esta versão substitui a medição das 10:48 do mesmo dia. Nenhum arquivo do escopo mudou desde então (sem commits depois de `d15beb05b`; nenhum arquivo de `server/`/`app/lib` do escopo com mtime posterior). Toda afirmação foi reaberta no código; a seção "O que mudou" lista o que caiu, o que subiu e o que é novo.

**Revisão adversarial (2026-09-22, tarde).** Um revisor cético reabriu todas as asserções PRONTO_E_PROVADO e PRONTO_SEM_PROVA e corrigiu este documento no lugar. As linhas alteradas levam a marca **[rev]**. O que caiu, o que subiu e o veredito estão na seção final, "Verificação adversarial". Resultado: de 9 asserções provadas sobram **4**.

**Eixo triplo.** IMPLEMENTADO (o código existe) ≠ PROVADO (há teste que exercita; e, para prova live, receipt) ≠ ABERTO (a capability deixa alcançar). ABERTO é **zero para o grupo inteiro**: `account_registration`, `catalog_private`, `decks_private`, `ai_analyze_optimize_advisory`, `ai_generate_rebuild`, `gallery_public` e `deck_replace_all` estão `off`/`allowed:false` em `server/config/release_capabilities.json` (entradas l.10, 16, 22, 40, 46, 76, 178), e `release_capability_policy_test.dart:14-35` afirma `every(!entry.allowed)`. **[rev]** Uma exceção a "ABERTO = zero": `GET /reports/:id` é control-plane (`server/lib/release_capability_policy.dart:584-585`), não tem middleware de auth (`server/routes/reports/` só contém `[id].dart`) e hoje serve publicamente o snapshot de um deck, inclusive de um deck já apagado (ver F4).

**[rev] Regra de prova usada nesta revisão.** Teste determinístico (unit/widget, roda em qualquer máquina) conta como prova. Teste **live** só conta com receipt ligado ao código atual. `docs/MANALOOM_E2E_RELEASE_CONTRACT.md:219-232` diz que "resultados envelhecem", que receipts ficam "ligados ao SHA", e que relatórios antigos em `docs/qa/` são `historical_evidence` e "nunca são promovidos". É a mesma regra que a linha acima já enunciava ("para prova live, receipt"). A medição original não a aplicava a H1, H2 e C5.

## Tabela-resumo do grupo

Contagem por asserção: **P** = PRONTO_E_PROVADO, **S** = PRONTO_SEM_PROVA, **Pa** = PARCIAL, **N** = NAO_ENCONTRADO.

| ID | Declarado | Medido | P/S/Pa/N | O que existe de verdade | O que realmente falta | Arquivos | Testes | Migração | Decisão humana | Prova viva |
| --- | --- | --- | --- | --- | --- | ---: | ---: | --- | --- | --- |
| DCK-P0-00 | TODO | **metade** | 3/2/2/1 **[rev]** | Portão `deck_replace_all` OFF, classificado por método+path e negado antes do handler e do PG (provado); bulk/set provados em `decks_private`, add unitário e create só por composição **[rev]**; "Colar lista" escondido com a capability OFF (widget test) | Guard "deck vazio não nasce/fica público" em create e PUT (e o live test `decks_crud_test.dart:184-212` que trava o oposto precisa ser invertido); 7 ações do app ainda chamam endpoints de `deck_replace_all` sem checar a capability: remover carta, descrição, estratégia, público, troca de edição, apply de Optimize por troca e import merge. "Renomear" não existe no app **[rev]**. Quando uma dessas ações dispara, a pessoa lê `capability_unavailable` cru (`app/lib/core/utils/friendly_error_mapper.dart:261-322`) **[rev]**. "IA advisory" vale só para Recommendations/Optimize | 9 **[rev]** (≈15 se a decisão for criar PATCH/remoção incremental) | 6 **[rev]** | não | **sim**: com o portão fechado não existe rota para remover carta nem editar metadados | sim |
| DCK-P0-01 | TODO | **mal-começada** | 0/1/4/3 | `FOR UPDATE` em todos os writers; 409 só no apply de Optimize via bulk; rollback de Optimize recusa HEAD por conteúdo; receipt atômico no create e no apply de Optimize | Coluna `revision`, If-Match/expected revision obrigatório em 8 mutators, ledger append-only para toda mutação, idempotência de add/bulk, undo universal (com ponto de entrada na UI **[rev]**), PUT stale 400→409; o único teste do undo está **obsoleto e quebraria hoje** | 25 **[rev]** | 16 **[rev]** | **sim** | sim | sim |
| DCK-P0-02 | TODO | **metade** | 1/1/3/1 **[rev]** | Token HMAC-SHA256 do Optimize (deck_id, deck_signature, mode, bracket, política funcional, removals/additions/swaps, 24h) emitido em `/ai/optimize` e verificado nas duas rotas de apply antes de qualquer DML. **[rev]** "Sem DML" só tem prova comportamental para stale e para violação de piso. Tamper, expired e outro deck contam apenas com teste de ordem no fonte | Owner e revision no payload; hash de input genérico; o mesmo artifact em import-to-deck, import-new, Generate, Rebuild e create; símbolo `DeckReviewArtifact v1` inexistente; live de tamper/expired/outro usuário relendo o deck **[rev]** | 9 | 8 **[rev]** | não (sim se o artifact for single-use) | TTL e uso único | **sim [rev]**: harness PG isolado, porque "sem DML" e "outro usuário" não se provam com unit |
| DCK-P0-03 | BLOCKED_BY_P0 | **mal-começada** | 0/1/2/3 | `/import/to-deck` numa tx só, validação non-strict antes do DELETE (falha preserva, **sem prova**); 409 só se o formato mudou durante a preparação | Preview com diff sem DML, commit por artifact/revision, undo, 409 por revisão, diálogo de diff no app; reclassificar o preview fora de `deck_replace_all` | 10 | 8 | não (usa a de P0-01) | capability do preview | sim (UI) |
| DCK-P0-04 | BLOCKED_BY_P0 | **mal-começada** | 0/0/3/3 | `ai_generate_jobs.request_key/request_fingerprint` com `ON CONFLICT` (provado no store) e `GET /ai/generate/jobs/latest` — tudo apagado fisicamente 30 min após a criação | Entrada bruta persistida, job durável, materialização server-side pelo job, `POST /decks` recusar lista vinda do cliente; hoje o app salva com a lista que monta e com **formato e prompt atuais da tela**, não os da geração | 13 | 8 | **sim** | sim (retenção do prompt; já há prompt gravado em `decks.description`) | sim (Web↔Android) |
| DCK-P0-06 | TODO | **mal-começada** | 0/1/1/5 **[rev]** | `decks.deleted_at` desde a migration 003; comunidade, feed de seguidos, busca de usuários, fichário, simulate, Battle e disponibilidade de coleção filtram `d.deleted_at IS NULL` | DELETE é físico e cascateia 8 tabelas; 26 arquivos de dono ignoram `deleted_at`; **[rev]** o relatório público `GET /reports/:id` sobrevive ao delete e é alcançável hoje; sem lixeira, restore, purge (não há infraestrutura de purge alguma) | 38 | 11 **[rev]** | sim (pequena: índice da lixeira e audit do purge) | janela de purge; destino dos relatórios públicos do deck apagado **[rev]** | sim (UI) |
| DCK-P0-07 | TODO | **mal-começada [rev]** (era "metade") | 0/0/3/3 **[rev]** | `main.dart` limpa todos os providers no logout e na troca de conta; AuthProvider tem geração própria (testada); **Trade, Message, Notification, Binder e Community já descartam resposta tardia por geração, com teste**; rascunhos locais isolados por owner e testados (`deck_entry_draft_store_test.dart:8-38`) **[rev]** | `DeckProvider` sem geração: resposta tardia de A sobrescreve `_selectedDeck`/`_decks` e repovoa o cache depois do logout; `clearAllState` esquece 8 campos (job ativo de Optimize, histórico...); a tela de detalhes não confere o id do deck; zero testes A→B e de forced logout. **Nenhuma asserção do aceite está satisfeita**, mas o trabalho é o menor e mais mecânico do grupo **[rev]** | 6 | 6 **[rev]** | não | não | não |
| DCK-P1-04 | BLOCKED_BY_P0 | **metade** (0 provada) **[rev]** | 0/2/3/2 **[rev]** | `validation_state` com CHECK, triggers que rebaixam para `draft` em mudança de cartas/formato, `/validate` strict. **[rev]** O teste live que prova isso não tem receipt vigente. A UI diz "Validado" só com o estado do servidor em resumo, chip e lista, mas **o painel de diagnóstico diz "Base pronta para testar" sem strict** | Legalidade ausente passa como legal no strict. É convenção em 3 validadores e em 13 filtros de candidatos de 4 arquivos. Os outros 7 casos são ORDER BY de ranking **[rev]**. A sync de legalidades nunca rebaixa deck já validado, e há 6 escritores de `card_legalities` **[rev]**. `cards`/`set`/`replace`/`import-to-deck` não devolvem readiness. Painel de diagnóstico com "pronta" sem strict **[rev]** | 13 **[rev]** (16 alinhando os 3 arquivos restantes de filtro de candidatos) | 8 **[rev]** | **provável [rev]** (trigger em `card_legalities` é o caminho que cobre os 6 escritores) | sim (decks validados podem cair para draft) | **sim [rev]**: receipt do live de validação em harness isolado |

Total do grupo: **[rev]** 54 asserções (53 + G6 nova) — **4 provadas, 8 implementadas sem prova, 21 parciais, 21 inexistentes** (antes: 9/5/18/21). Trabalho: **[rev]** ~123 arquivos tocados (antes ~115), ~71 testes (antes ~63), 3 migrations certas + 1 provável (revision+ledger; payload/retenção/lineage do Generate; índice/audit de purge; trigger de legalidade), todas passando pelo protocolo de migration preservadora de `BT-DB-002`/`BT-DB-003` e disputando o mesmo número `059`. Também: 1 job de purge, 3 superfícies novas no app (lixeira, diff de import, review de Generate materializado), 1 arquivo de política de capabilities isolada para os live tests de deck (não existe gerador, só os de play-vs-ai e de QA visual) e 1 bateria de receipts live sob essa política. Nenhuma tarefa está "já atendida" ou "quase-lá".

---

## O que mudou em relação à medição das 10:48

| Tarefa | Mudança | Por quê (evidência) |
| --- | --- | --- |
| DCK-P0-01 | B7 "undo recusa HEAD novo": PRONTO_E_PROVADO → **PRONTO_SEM_PROVA**; B4 perde a prova do rollback duplicado | O único teste, `server/test/deck_optimization_apply_rollback_live_test.dart`, aplica Optimize via `PUT` com `mutation_context` **sem** `apply_authorization` (helper l.105-140) e espera 400 (l.188), 500 (l.276), 200 (l.292) e 200 (l.370). Desde `35370ed89` (2026-07-31) a rota exige o token HMAC sempre que o contexto é de Optimize (`server/lib/decks/optimization_apply_authorization_support.dart:40-50`; chamada em `server/routes/decks/[id]/index.dart:322-334`; mapeada para 409 em l.580-581). O teste foi tocado pela última vez em `bd2c65a93` (2026-07-29), não está em nenhum script de suíte, e hoje falharia na l.188 antes de chegar às asserções de rollback (l.357, l.396). |
| DCK-P0-00 | A5 (UI) desdobrada: eram 4 ações que batem no portão, são **7**; decisão humana passa a "sim" | Além de remover carta, descrição, estratégia e público (todas `PUT /decks/:id`), também **troca de edição** (`POST /decks/:id/cards/replace`, `deck_provider_support_mutation.dart:381`, chamada em `deck_details_screen.dart:1811`) e **apply de Optimize por troca** (`applyOptimizationWithIds`/`applyOptimization` → `_persistDeckCardsPayload` → `PUT` com lista inteira, `deck_provider.dart:1206-1245,1258-1394`, `deck_provider_support_mutation.dart:288`; a tela só checa `_canUseAnalyzeOptimize`, `deck_details_screen.dart:2530-2555`). Não existe rota alternativa: `decks/[id]/index.dart` só atende GET/PUT/DELETE (l.26-34), `cards/set` recusa quantidade ≤ 0 (`cards/set/index.dart:55-58`) e não há rota de remoção por carta. |
| DCK-P0-00 | Novo: o servidor **não gateia `is_public=true` por `gallery_public`** | `gallery_public` só classifica `/community/decks*` e `/decks/:id/reports` (`release_capability_policy.dart:466-475`); `POST /decks` (`decks/index.dart:335,358-366`) e `PUT` (`decks/[id]/index.dart:173,189-205`) gravam `is_public` livremente. Decks marcados públicos com a galeria fechada ficam visíveis no dia em que ela abrir. |
| DCK-P0-00 | Novo: a esconder "Colar lista" com a capability OFF **está provada** | `app/test/features/decks/screens/deck_details_screen_smoke_test.dart:491-527` ("direct deck deep link hides replace-all surfaces when capability is OFF"): com só `decksPrivate`, "Colar lista" e "Colar lista de cartas" não aparecem e nenhum POST sai. |
| DCK-P0-04 | E6 ganha evidência concreta de violação no cliente | O dropdown de formato só faz `setState` + rascunho (`deck_generate_screen.dart:1121-1128`) e o prompt só agenda rascunho (l.75); nenhum dos dois limpa `_generatedDeck`. Formulário e resultado ficam na mesma tela (l.1009-1038). O salvamento usa `_selectedFormat` e `_promptController` do momento do clique (l.880-887). `generatedDeckSaveBlockingReasons` só olha o resultado (`deck_provider_support_generation.dart:767-796`). Bracket e comandante preferem os valores gerados (l.765-769, 830-836); formato e descrição não. |
| DCK-P0-04 | Novo: Rebuild grava clone **por padrão** | `save_mode` default é `draft_clone` (`server/lib/ai/rebuild_route_request_support.dart:57-64`); o clone é privado (`rebuild_guided_service.dart:500-506`, `is_public=false`) e passa por pisos de mana/funções, mas sem preview nem artifact. Afeta A8 de P0-00 e DCK-P1-08. |
| DCK-P0-07 | Escopo **encolhe**: o padrão já existe em 5 providers, com teste-molde | `_stateGeneration`/gerações por requisição incrementadas no `clearAllState` em Trade (`trade_provider.dart:576-579,1064-1068`), Message (`message_provider.dart:109,473`), Notification (`notification_provider.dart:63,305`), Binder (`binder_provider.dart:748-776,1316-1319`) e Community (`community_provider.dart:247,577`). Testes: `trade_provider_test.dart:366-410` e `notification_models_test.dart:339`. Faltam Deck, Social e Market. A estimativa "12 arquivos no sentido literal" cai para 6. |
| DCK-P0-07 | Novo: a tela de detalhes mostra o deck errado | `deck_details_screen.dart:620` seleciona `p.selectedDeck` sem comparar com `widget.deckId`; resposta tardia de A aparece na tela de B. E `fetchDeckDetails` repovoa `_deckDetailsCache` depois de um `clearAllState` (`deck_provider.dart:259-273`). |
| DCK-P1-04 | H4 deixa de ser "uma linha" | Legalidade ausente = legal em `deck_rules_service.dart:174-175`, `cards/index.dart:109-132` (add), `routes/decks/[id]/analysis/index.dart:287` (comentário explícito "assumimos que é legal"), `ai/optimize_format_legality_support.dart:46-47` (`HAVING COUNT(cl.status) = 0 OR ...`) e em 20 consultas de 7 arquivos de candidatos/lookup (`cl.status IS NULL OR ...`). |
| DCK-P1-04 | Nova asserção H7 (NAO_ENCONTRADO) | A sync de legalidades declara que não mexe em decks (`server/bin/sync_card_legalities_from_scryfall.py:11`) e não há trigger em `card_legalities`; um deck `validated` continua "Validado" depois que uma carta dele é banida. |
| DCK-P0-06 | Detalhes novos | O DELETE físico cascateia 8 tabelas (lista em F2); `server/lib/retention/` só tem `post_game_note_service.dart` — não há purge para nada; `decks_crud_test.dart:437-476` chama-se "should cascade delete deck cards" (intenção de hard delete) e l.477-487 é placeholder `expect(true, isTrue)` pulado para "outro usuário". |
| DCK-P0-01 | Inventário corrigido | 17 arquivos escrevem em `decks`/`deck_cards` (eram 13); `ApiClient` não aceita header por chamada (`api_client.dart:349,421,533`); o padrão de `post_game_notes` tem revisão **opcional** (`post_game_note_service.dart:272,391`), então copiá-lo como está não cumpre "todo mutator usa"; há um terceiro substituto de revisão por hash (`battle_replay_annotation_service.dart:168`). |
| Grupo | Receipts | O único live test deste grupo plugado numa suíte é `deck_optimization_mana_floor_live_test.dart` (`scripts/manaloom_e2e_suite.sh:600`), chamado **sem** `MANALOOM_ISOLATED_RELEASE_CAPABILITIES_FILE`; o teste começa com `POST /auth/register` (l.379), negado no all-off. **[rev]** Mais forte do que "nenhum receipt é posterior à última alteração": esse teste **nunca teve receipt**. Foi criado em `bd2c65a93` (2026-07-29), depois dos receipts de 2026-07-22, e `git grep` só o encontra no script da suíte, no manifesto gerado e em `server/doc/API_CONTRACTS_AND_DATA_MAP.md`. `docs/qa/execution/` não tem nenhum receipt de live test de deck. |

---

## DCK-P0-00 — Conter fluxos perigosos: replace-all OFF, deck novo privado e IA advisory

Aceite (l.278): "Add-only continua; replace direto/API consumer nega; deck vazio não nasce público." Onda 01 (l.22): "UI/deep link/API direta; add-only preservado; negação antes de mutação; deck vazio nunca público".

| Asserção | Estado | Onde está | Teste (o que afirma de verdade) | O que falta |
| --- | --- | --- | --- | --- |
| A1. Capability própria `deck_replace_all`, OFF | PRONTO_E_PROVADO | Chave em `server/lib/release_capability_policy.dart:44`; manifesto `server/config/release_capabilities.json:178` (`implemented_guarded`/`off`/`allowed:false`) | `server/test/release_capability_policy_test.dart:14-35`: chaves do manifesto == `releaseCapabilityKeys` e `every(!entry.allowed)` | — |
| A2. `PUT /decks/:id`, `POST /decks/:id/cards/replace` e `POST /import/to-deck` exigem `deck_replace_all` | PRONTO_E_PROVADO | `release_capability_policy.dart:389-397` | `release_capability_policy_test.dart:195-197` afirma exatamente os três mapeamentos | — |
| A3. Add-only (add, bulk, set) continua em `decks_private` | **PRONTO_SEM_PROVA [rev]** (era PRONTO_E_PROVADO) | Prefixo `/decks/` em `release_capability_policy.dart:537-541`; o app adiciona por `POST /decks/:id/cards` (`deck_provider_support_mutation.dart:75`), `bulk` (l.95) e `set` (l.144) | **[rev]** O mapa de `release_capability_policy_test.dart:187-225` afirma só `GET /decks` (l.192), `bulk` (l.193) e `set` (l.194). Não afirma `POST /decks/:id/cards`, que é o add do app, nem `POST /decks`, o create. O teste "every route handler is classified" (l.309-341) só exige `capability != null`. Se o add fosse reclassificado para `deck_replace_all`, a suíte continuaria verde. O teste afirma menos que a asserção | 2 linhas no mapa do teste (`'POST /decks/deck/cards': 'decks_private'`, `'POST /decks': 'decks_private'`). Ressalva mantida: `/import/to-deck` em modo merge (`replace_all:false`, default em `routes/import/to-deck/index.dart:39`) é add-only semanticamente e cai no portão (classificação só por path) |
| A4. API direta negada antes do handler e antes do PG | PRONTO_E_PROVADO (por composição) | `server/routes/_middleware.dart:105-144`: decisão antes de observabilidade (l.148) e de `_db.connect()` (l.151), sem ramo por capability | `release_capability_policy_test.dart:371-391` (ordem no fonte: decisão < observabilidade < PG; é teste de string e sozinho não provaria); l.393-438 (`battle_batch` e `account_registration` → 404 `capability_unavailable` com o middleware real e contexto cujo `read` lança, sem chamar o handler). A composição com o mapeamento provado em A2 cobre `deck_replace_all`. Não há caso explícito para `PUT /decks/x` **[rev: linhas corrigidas]** | 1 caso `PUT /decks/x` → 404 `capability: deck_replace_all` |
| A5. UI não oferece nem dispara replace-all com a capability OFF | PARCIAL | "Colar lista" gateado: `app/lib/features/decks/screens/deck_details_screen.dart:420-422,1654-1661`. **Sem gate** (vão receber 404 do middleware): remover carta (swipe l.1176 → `deck_provider.dart:165-186` reconstrói a lista e chama `PUT`, `deck_provider_support_mutation.dart:121-133`); descrição (`mutation.dart:345-357`) e estratégia (l.359-372), ambas `PUT`; público (`deck_provider_support_import.dart:212-220`, gate só por `gallery_public`, `deck_details_screen.dart:768,1538-1539`); troca de edição (`cards/replace`, `mutation.dart:376-390`, tela l.1800-1815); apply de Optimize por troca (`PUT` com lista inteira, `mutation.dart:277-292`, `deck_provider.dart:1230,1377`, tela l.2530-2555 gateada só por `ai_analyze_optimize_advisory`); import merge (gateado junto com "Colar lista"). **[rev]** "Renomear" não é ação do app: nenhum `PUT` do app envia `name` (`git grep "\.put("` em `app/lib`). **[rev]** O 404 dessas ações chega à pessoa como o texto cru `capability_unavailable` (`app/lib/core/utils/friendly_error_mapper.dart:261-322`; achado A5 de `docs/flows/deck_lifecycle.md:290`) | `deck_details_screen_smoke_test.dart:491-527` prova só o "Colar lista" escondido (e só olha `postCalls`, não `PUT`) | Gatear ou redirecionar as 6 ações restantes, mais a mensagem de produto no mapper; o problema real é que **não há rota alternativa** (ver "O que realmente falta") |
| A6. Deck novo nasce privado por padrão | PRONTO_SEM_PROVA | `server/routes/decks/index.dart:335` (`?? false`); coluna `DEFAULT FALSE` (`database_setup.sql:315`); `/import` (`routes/import/index.dart:297`), cópia da comunidade (`routes/community/decks/[id]/index.dart:403`) e Rebuild (`lib/ai/rebuild_guided_service.dart:500-506`, `false` literal) nascem privados; app envia `canPublishNow && isPublic` (`deck_list_screen.dart:434-438,458`) | nenhum teste afirma o default `false` | 1 contract/live test |
| A7. Deck vazio nunca nasce nem fica público | NAO_ENCONTRADO — e há teste travando o oposto | Create aceita `{is_public:true, cards:[]}` (`decks/index.dart:335-336,358-366`); PUT grava `is_public` sem olhar cartas nem `validation_state` (`decks/[id]/index.dart:173,189-205`); servidor não exige `gallery_public` para gravar `is_public` (só a leitura da galeria é gateada, `release_capability_policy.dart:466-475`); o app, com `gallery_public` ON, cria público sem comandante = deck vazio público | `server/test/decks_crud_test.dart:184-212` **afirma** 200/201 para `POST /decks {is_public:true, cards:[]}` e `GET` com `is_public: true` | Guard no create e no PUT (e opcionalmente exigir `gallery_public` no handler); inverter esse teste; app mapear o 4xx em `togglePublicRequest` |
| A8. IA advisory (entrega, não aceite mínimo): nada da IA entra no deck sem autorização | PARCIAL | Recommendations marcadas `unvalidated_ai_text` e chaves do fallback prevalecem (`server/lib/deck_recommendations_advisory_support.dart:1-45`); apply de Optimize exige HMAC (`optimization_apply_authorization_support.dart:40-50`). **Contra-exemplos:** Generate grava via `POST /decks` com lista montada no app (`app/lib/features/decks/screens/deck_generate_screen.dart:839-887`; o servidor aceita qualquer `cards`, `decks/index.dart:336-337`); Rebuild grava clone com cartas da IA por padrão, sem preview/artifact (`rebuild_route_request_support.dart:57-64`; `rebuild_guided_service.dart:453-536`) | `server/test/deck_recommendations_advisory_support_test.dart:6,46,84,137,169`; `optimize_apply_authorization_test.dart:781,840`. Nada cobre Generate→`POST /decks` nem Rebuild | Fecha com DCK-P0-04 (materialização por job) e DCK-P1-08 (Rebuild preview-first) |

**O que realmente falta.** O portão está de pé e provado; o que falta é (1) o guard semântico "deck vazio não é público", com o live test invertido, e (2) resolver uma contradição de produto que a contenção criou: com `deck_replace_all` OFF e `decks_private` ON, o editor de decks **adiciona mas não remove carta, não edita descrição/estratégia, não alterna público, não troca edição e não aplica Optimize por troca**. Todas essas ações só existem via `PUT /decks/:id` ou `cards/replace`. **[rev]** Renomear o servidor aceita no `PUT`, mas o app nunca ofereceu. "Add-only continua" é literalmente verdade na API, mas o produto resultante não é um editor utilizável. Resolver isso exige decisão: reclassificar `PUT` só de metadados (hoje impossível, a política não vê o corpo — seria preciso um `PATCH /decks/:id` novo em `decks_private`), criar remoção incremental (antecipa DCK-P1-03) ou esconder as ações. Nota de estado: metade desta tarefa foi entregue pelo baseline `b2d3fc04f` (2026-08-13) e o registro continua `TODO`.

- Dependência declarada `BT-SCP-001` (`IN_PROGRESS_CONTAINED`): o que esta tarefa usa dela — manifesto default-deny e negação antes do PG — já existe e está provado; a tarefa pode andar agora. Dependência não declarada: a decisão acima e, na prática, DCK-P1-03.
- Sobreposição: A7 é a mesma entrega de DCK-P1-01 ("deck vazio não aparece público"); A5 é o problema de DCK-P1-03 ("sem reconstruir lista inteira no cliente" — `removeCardFromDeck` faz exatamente isso); A8 é fechada por DCK-P0-04 e DCK-P1-08; o apply de Optimize via `PUT` é o "segundo apply" de DCK-P1-07.
- Trabalho: `decks/index.dart`, `decks/[id]/index.dart` (guards), `decks_crud_test.dart` (inverter), `release_capability_policy_test.dart` (caso PUT + **[rev]** linhas de add e create), 1 contract test novo, `deck_provider_support_import.dart`, `deck_details_screen.dart` (gatear 6 ações), `deck_details_screen_smoke_test.dart`, **[rev]** `friendly_error_mapper.dart` (+ teste) → **[rev] 9 arquivos, 6 testes** no caminho "esconder as ações". Se a decisão for criar `PATCH /decks/:id` de metadados e remoção incremental em `decks_private`, somam-se 2 rotas, a classificação na política, as requisições do app e os testes de rota: **≈15 arquivos, ≈10 testes**. Sem migração, decisão humana sim, prova viva sim (receipt live com política isolada mostrando a negação e o guard).

---

## DCK-P0-01 — Revisão otimista + ledger imutável + undo universal

Aceite (l.279): "Todo mutator usa expected revision/If-Match; uma de duas concorrentes vence; outra 409; retry não duplica; receipt atômico evita segunda validação; undo recusa HEAD novo."

Inventário de escrita em `decks`/`deck_cards` (17 arquivos). Mutators de conteúdo de deck existente (precisam de expected revision): `routes/decks/[id]/index.dart` (PUT; DELETE via `lib/battle/interactive_battle_deck_lifecycle.dart`), `[id]/cards/index.dart`, `[id]/cards/bulk`, `[id]/cards/set`, `[id]/cards/replace`, `[id]/optimizations/[eventId]/rollback`, `routes/import/to-deck` — 8. Criadores (revisão 1 + evento no ledger): `routes/decks/index.dart`, `routes/import/index.dart`, `routes/community/decks/[id]/index.dart` (cópia), `lib/ai/rebuild_guided_service.dart` — 4. Estado de validação: `lib/deck_validation_route_support.dart`. Caches derivados (não devem mudar revisão): `[id]/ai-analysis` (l.234), `[id]/pricing` (l.217). Moderação/privacidade: `lib/social_safety_service.dart:852,988`, `lib/user_data_privacy_service.dart`.

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| B1. `revision` persistida por deck | NAO_ENCONTRADO | `decks` não tem a coluna (`server/database_setup.sql:309-338`); `revision` só existe em `post_game_notes` (`database_setup.sql:2320`; `server/bin/migrate.dart:1396`). Há três substitutos por hash de conteúdo: `deck_snapshot_hash` no GET (`decks/[id]/index.dart:826`, `lib/deck_snapshot_contract.dart:17`), `buildDeckSignature` do Optimize (`lib/decks/deck_optimization_history_service.dart:368`) e `subject_deck_revision` das anotações de replay (`lib/battle/battle_replay_annotation_service.dart:168`). **[rev]** Há ainda um falso token de versão: `deck_version_at` no GET é `DateTime.now()` (`decks/[id]/index.dart:831`), inútil como If-Match e perigoso se alguém o reaproveitar | — | Migration `revision BIGINT NOT NULL DEFAULT 1` + bump em todo mutator; decidir o que fazer com os três hashes e remover/renomear `deck_version_at` |
| B2. Todo mutator exige expected revision / If-Match | NAO_ENCONTRADO | Único If-Match do servidor: `routes/decks/[id]/post-game-notes/[noteId].dart:53-69` (notas, e **opcional**: `null` pula a checagem, `post_game_note_service.dart:272,391`). Em deck, só o apply de Optimize exige `expected_deck_signature` (`decks/[id]/index.dart:237-251`; `cards/bulk/index.dart:142-160`). App: `ApiClient.post/put/delete` não recebem headers (`app/lib/core/api/api_client.dart:349,421,533`); `DeckDetails` tem `deckSnapshotHash` (`models/deck_details.dart:128`) mas nenhuma mutação o envia | — (só o caso Optimize, ver B3) | Parser de If-Match obrigatório (428 sem header) nos 8 mutators; `ApiClient` com header; modelos com `revision` |
| B3. Duas escritas concorrentes: uma vence, outra 409 | PARCIAL | Todos os writers serializam com `FOR UPDATE` (`decks/[id]/index.dart:135-139,219`; `cards/index.dart:55`; `set:72`; `replace:54`; `bulk:105,127`; `import/to-deck/index.dart:221,241,263`; `rollback:31,53`; `lib/deck_validation_route_support.dart:9`; `interactive_battle_deck_lifecycle.dart:28`) — serializar não é detectar: a segunda escrita entra depois e vence. 409 tipado só no apply de Optimize via bulk (`bulk:153-159` → l.364-366). Via `PUT` o mesmo stale devolve **400** (`decks/[id]/index.dart:247-250` lança `DeckRequestException` → `badRequest` em l.586-588) — e é o `PUT` que o app usa para Optimize por troca | `optimize_apply_authorization_test.dart:885-896` (fonte de 5 writers contém `FOR UPDATE`); `deck_optimization_mana_floor_live_test.dart:582-590` (bulk stale → 409 `optimization_preview_stale`; exige política isolada) | 409 tipado por `revision` em todos; corrigir o 400 do PUT |
| B4. Retry não duplica | PARCIAL | Idempotência só em jobs de IA (`lib/ai_generate_job.dart:55-115`) e em Battle (`routes/ai/battle/**`, header `Idempotency-Key`). `POST /decks/:id/cards` soma (`cards/index.dart:284`), `bulk` soma (`lib/deck_cards_bulk_support.dart:49`) — em formato singleton o retry vira erro de regra, em básicos/60 cartas duplica; `set` e `PUT` são idempotentes por natureza; rollback duplicado → 409 `optimization_already_rolled_back` (`rollback/index.dart:66-82`) | O teste do rollback duplicado (`deck_optimization_apply_rollback_live_test.dart:352-361`) está no arquivo obsoleto (ver B7) | Idempotency key (ou o próprio If-Match) em add/bulk; receipt reproduzível no retry |
| B5. Receipt atômico evita segunda validação | PARCIAL | Create persiste readiness na mesma tx (`decks/index.dart:468-542`, `exposeDeckValidationState` l.542); apply de Optimize devolve validação embutida (`bulk:348-361`); `/import` idem. `cards`, `set`, `replace` e `import/to-deck` não devolvem `deck_state` (grep = 0); o trigger rebaixa para `draft` (`database_setup.sql:414-445`) e o app chama `/validate` depois quando não há validação embutida (`deck_provider_support_mutation.dart:242-245`) | `deck_validation_state_live_test.dart:138-176` (create → `draft`; `/validate` → `validated`) | Revalidar strict e persistir estado na mesma tx dos mutators manuais, devolvendo receipt {owner, deck, rev anterior/nova, operação, hash, resultado} |
| B6. Ledger imutável de mudanças | PARCIAL | Só `deck_optimization_events` (`migrate.dart:1023-1058`): sem UPDATE em lugar nenhum; DELETE só na exclusão de conta (`lib/user_data_privacy_service.dart:696`); mas `ON DELETE CASCADE` do deck (`migrate.dart:1029`) apaga o histórico junto com o deck | `deck_optimization_history_service_test.dart`; `deck_optimization_history_route_contract_test.dart` | Tabela append-only para toda mutação (manual, import, generate, delete/restore) que sobreviva ao soft-delete |
| B7. Undo recusa HEAD novo | PRONTO_SEM_PROVA (só Optimize) — **rebaixado** | `rollback/index.dart:104-133`: compara assinatura atual com `after_snapshot` → 409 `optimization_rollback_conflict`. A comparação é por conteúdo: um HEAD novo com o mesmo conteúdo (mudou e desfez) passa | O teste (`deck_optimization_apply_rollback_live_test.dart:386-400`) está **obsoleto**: aplica via `PUT` sem `apply_authorization` e espera 400/500/200 (l.188, 276, 292, 370); com o HMAC obrigatório desde `35370ed89` (2026-07-31) a rota devolve 409 `optimization_apply_not_authorized` já na l.188. Última alteração do teste: `bd2c65a93` (2026-07-29); não está em nenhum script | Reescrever o teste com `authorizedMutationContext` (padrão de `deck_optimization_mana_floor_live_test.dart:123-214`) |
| B8. Undo universal | NAO_ENCONTRADO | Só existe o rollback de Optimize | — | Undo sobre o ledger de B6, criando nova revisão, com a recusa de B7 por `revision` |

**O que realmente falta.** A fundação transacional da Onda 02 não existe: nenhuma revisão, nenhum If-Match em deck, nenhum ledger fora do Optimize, nenhum undo fora do Optimize — e a única prova comportamental do undo está quebrada desde 31/07. O que pode ser promovido a padrão: o lock `FOR UPDATE` já presente em todo writer, o par "assinatura esperada → 409" do bulk, o `revision`/409 de `post_game_notes` (tornando-o obrigatório) e a recusa do rollback. Duas armadilhas de desenho: `cards/set` (`cards/set/index.dart:183-211`) e `import/to-deck` (l.296-324) fazem `DELETE` de todas as cartas e reinserem mesmo quando a mudança é pequena — o ledger precisa registrar o diff calculado, não a DML.

- Dependência declarada `BT-DB-001` (TODO): **real** — a onda diz que a migration só nasce depois do baseline (`waves/02-deck-foundation.md:32-34`). **[rev]** E não basta. `BT-DB-001` é só a auditoria do schema contra o baseline 058. A DDL em si é `BT-DB-002`, "Próxima migration preservadora — `059` somente se ainda for o próximo número", com preflight e postcheck. Depois vem `BT-DB-003`, upgrade/rollback por restore (backlog l.601-603). Dependência real não declarada: `BT-DB-002`/`BT-DB-003`. As migrations de P0-01, P0-04, P0-06 e P1-04 disputam o mesmo `059` e precisam ser consolidadas ou sequenciadas. Pode andar antes: parser de If-Match, header no `ApiClient`, correção 400→409 do `PUT`, reescrita do teste obsoleto.
- Sobreposição: DCK-P1-07 (os dois applies de Optimize no ledger único), DCK-P1-03 (edição com revision), DCK-P2-01 (timeline); B5 é a mesma entrega de H6 de DCK-P1-04.
- Trabalho: 2 de schema (`migrate.dart`, `database_setup.sql`), 2 libs novas (helper de revisão, serviço de ledger), 13 writers, `health_readiness_support.dart` (checagem de schema) e, no app, `api_client.dart`, `models/deck.dart`, `models/deck_details.dart`, `deck_provider_support_mutation.dart`, `deck_provider_support_import.dart`, `deck_provider.dart` (409 amigável) e, **[rev]** para o undo universal ter por onde ser acionado, `deck_details_screen.dart` e o widget de histórico/desfazer → **[rev] ~25 arquivos, ~16 testes** (helper; contract dos 8 mutators; live de concorrência com duas conexões; live de retry; ledger atômico; undo universal; reescrita do rollback; provider 409; **[rev]** widget do desfazer). Migração sim. Decisão humana: If-Match obrigatório já no primeiro corte ou com transição; o que "undo universal" significa para o histórico anterior à migration. Prova viva sim.

---

## DCK-P0-02 — `DeckReviewArtifact v1` único para preview/commit

Aceite (l.280): "Qualquer mudança semântica invalida; outro usuário/tamper/stale/expired falha." Onda 02 (l.10): "owner, deck, revision, input/constraints hash, HMAC e expiração; tamper, outro usuário, stale e expired falham sem DML".

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| C1. Artifact HMAC emitido no preview e verificado no commit | PRONTO_E_PROVADO (só Optimize; mantido na revisão, com ressalva) | Emissão `server/lib/ai/optimize_swap_integrity.dart:234-311` (HMAC-SHA256, versão `v2`, 24h em l.25) via `attachOptimizeApplyAuthorizationToResponse` (l.425-456), chamado em `routes/ai/optimize/index.dart:779,1075` e `lib/ai/optimize_route_internal.dart:607`; verificação l.500-600 (tempo constante, versão, binding, expiração, delta); uso em `decks/[id]/index.dart:322-334` e `cards/bulk/index.dart:169-179` | `server/test/optimize_apply_authorization_test.dart:343-400` (tamper → `invalid_signature`; +25h → `expired_token`; bracket → `bracket_binding_mismatch`; outro `deck_id` → `deck_binding_mismatch`); l.402 (delta real vs autorizado, pela mesma `validateOptimizationApplyAuthorization` que as duas rotas chamam); l.535 (condition/comandante adulterados); l.781 (sem secret → fail-closed). **[rev] Ressalva:** o round-trip criptográfico é unitário de verdade. Já a *emissão pela rota* `/ai/optimize` só tem teste de ordem no fonte (`optimize_route_resilience_support_test.dart:166-181`). Nenhum teste chama `/ai/optimize` e confere `apply_authorization` na resposta, e o live `mana_floor` assina o token localmente (`deck_optimization_mana_floor_live_test.dart:123-214`) | Generalizar para import, Generate, Rebuild e create; 1 teste de rota que confira o token emitido |
| C2. Liga owner (outro usuário falha) | PARCIAL | Payload sem `user_id` (l.284-297). Hoje "outro usuário" falha por composição: a rota só acha o deck com `user_id = @userId` (`decks/[id]/index.dart:135-139`) e o token é preso ao `deck_id` | Nenhum teste cruzado; o único "outro usuário" de deck é o placeholder pulado `decks_crud_test.dart:477-487` (`expect(true, isTrue)`) | `owner_id` no payload + teste live com dois usuários |
| C3. Liga revision (stale falha) | PARCIAL | Liga `deck_signature` (hash do conteúdo, l.287; verificação l.556-557 → `deck_binding_mismatch`); não distingue "mesmo conteúdo, outra revisão" | Nenhum caso unitário com `deckSignature` divergente (todas as chamadas usam `'signature-1'`); stale só provado pela pré-checagem da rota em live (`deck_optimization_mana_floor_live_test.dart:582-590`), que roda antes do HMAC | 1 caso unitário; `revision` quando P0-01 existir |
| C4. Hash de input/constraints (qualquer mudança semântica invalida) | PARCIAL | Liga `mode`, `bracket`, `functional_role_policy`, `removals`, `additions`, `swaps` (l.284-297). Não liga prompt/constraints de Generate nem a lista do import. `expectedBracket` é opcional na verificação (`optimize_swap_integrity.dart:508,585`) e as rotas não o passam; o bracket é cobrado pelo `context_bracket` (`optimization_apply_authorization_support.dart:82-95`) | `optimize_apply_authorization_test.dart:307-341` (cartas/quantidades fora do preview), l.464 (bracket do contexto ≠ assinado) | Hash canônico dos inputs por fluxo |
| C5. Falha sem DML | **PRONTO_SEM_PROVA [rev]** (era PRONTO_E_PROVADO) | Verificação antes do `DELETE FROM deck_cards`: `decks/[id]/index.dart:322-334`, com o DELETE em l.387; `bulk:169-179`, com o DELETE em l.230. Tudo dentro de `runTx`. O `UPDATE` de metadados do PUT (l.183-205, SQL em l.193-194) roda antes, mas na mesma transação | **[rev] O teste afirma menos que a asserção.** O aceite da onda é "tamper, outro usuário, stale e expired falham sem DML". O live `deck_optimization_mana_floor_live_test.dart:573-616` e 655-673 relê o deck só depois de: assinatura ausente (`optimization_signature_required`), **stale** (`optimization_preview_stale`, pré-checagem anterior ao HMAC) e violações de piso com token *válido*. Nenhum caso de tamper, expired, outro deck ou outro usuário passa pela rota. Para esses, só existem o unit da função (l.343-400) e a ordem no fonte (l.840-883), que é teste de string. Além disso, o live **nunca teve receipt** e, do jeito que a suíte o chama (`manaloom_e2e_suite.sh:600`, sem política isolada), para no `POST /auth/register` | 1 live (harness isolado) com token adulterado e expirado relendo cartas, `validation_state` e eventos; receipt |
| C6. Um único artifact/versão para todos os previews | NAO_ENCONTRADO | `git grep DeckReviewArtifact` só acha a onda; import-new tem assinatura só no cliente e sem comandante (`app/lib/features/decks/screens/deck_import_screen.dart:129-134`); `/import/validate` não recebe `deck_id` (`routes/import/validate/index.dart:20-49`) | — | Módulo `lib/decks/deck_review_artifact.dart` v1 |

**O que realmente falta.** O núcleo criptográfico existe e é bem testado no Optimize; falta promovê-lo a artifact de revisão de deck: incluir owner e revision, aceitar hash genérico de input, e emitir/verificar o mesmo artifact em import-to-deck, import-new, Generate, Rebuild e create-from-review. Observação: o token é reutilizável por 24h enquanto o conteúdo do deck voltar ao mesmo estado (não há nonce nem registro de uso); se "replay pós-commit" entrar no aceite, vira tabela de artifacts usados.

- Dependência declarada `DCK-P0-01`: real só para o campo `revision`; owner, hash de input e generalização podem andar já sobre `deck_signature`.
- Sobreposição: DCK-P1-02 (assinatura completa de import-new), DCK-P1-07, DCK-P1-08; os commits de P0-03 e P0-04 consomem este artifact.
- Trabalho: lib nova, `optimize_swap_integrity.dart`, `optimization_apply_authorization_support.dart`, `decks/[id]/index.dart`, `cards/bulk/index.dart`, `routes/ai/optimize/index.dart`, `optimize_route_internal.dart`, rota de preview de import, provider do app → **~9 arquivos, [rev] ~8 testes** (owner, revision, input-hash, assinatura divergente, expirado/tamper na v1 nova, cross-user live, replay, **[rev]** live de tamper/expired relendo o deck). **[rev] Premissa de escopo:** esses 9 arquivos cobrem o módulo v1 e a migração do Optimize. Ligar o artifact a import-to-deck, Generate, import-new e Rebuild fica contado em P0-03, P0-04, DCK-P1-02 e DCK-P1-08. Se P0-02 tiver de entregar sozinho o "único para todos os previews", somam-se `routes/import/index.dart`, `routes/import/validate/index.dart`, `routes/ai/generate/index.dart`, `routes/ai/rebuild/index.dart` + `lib/ai/rebuild_guided_service.dart`, `routes/decks/index.dart` e as telas de import/Generate: ≈17 arquivos. Migração não (sim se uso único). Decisão humana: TTL (hoje 24h, a onda não fixa) e uso único. **[rev] Prova viva sim:** harness PG isolado. A própria coluna "o que falta" de C2 e C5 pede live, e "sem DML" não se prova com unit.

---

## DCK-P0-03 — Importar em deck existente em duas fases, diff completo e undo

Aceite (l.281): "Zero delete/insert antes de confirmar; falha preserva original; stale retorna 409." Onda 02 (l.13): "parse/resolve/diff/validate sem delete; confirmação usa artifact/revision; erro preserva original; stale 409; undo restaura exatamente".

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| D1. Fase 1 (parse/resolve/diff/validate) sem DML | PARCIAL | `/import/validate` faz parse/resolve sem deck (`routes/import/validate/index.dart:20-70`); `/import/to-deck` resolve e grava na mesma chamada (`routes/import/to-deck/index.dart:24-339`) | `import_parser_test.dart`, `import_list_service_test.dart` (parse) | Preview com `deck_id` devolvendo diff + artifact, sem DML |
| D2. Commit só com confirmação | NAO_ENCONTRADO | O POST executa `DELETE FROM deck_cards` + INSERT (`to-deck/index.dart:296-324`) — **inclusive no modo merge** | — | Commit que exige o artifact de P0-02 |
| D3. Diff completo apresentado | NAO_ENCONTRADO | Resposta só traz contagens, `not_found_lines`, `warnings`, comandante (`lib/import_to_deck_merge_support.dart:71-93`); `app/lib/features/decks/widgets/deck_import_list_dialog.dart` sem preview/diff | `import_to_deck_merge_support_test.dart` (shape) | Builder de diff + diálogo no app (linguagem visual do contador, não formulário) |
| D4. Falha preserva original | PRONTO_SEM_PROVA | `validateAndThrow(strict:false)` (l.290-294) antes do `DELETE` (l.296-299), tudo em `pool.runTx` (l.213-325) | `deck_crud_atomicity_live_test.dart:181-189` relê o deck após um 400 por `list: 123` — falha de forma do corpo, antes do banco; `import_to_deck_flow_test.dart:258-296` (estouro de 100 cartas, falha dentro da tx) não relê o deck e faz `return` silencioso se as cartas de fixture não existirem (l.269-273) | 1 live: falha dentro da tx + releitura do deck e do `validation_state`; remover o `return` silencioso |
| D5. Stale retorna 409 | PARCIAL | 409 `import_deck_changed` só se o formato mudou entre a preparação e o lock (`to-deck/index.dart:213-231,340-348`); mudança de cartas não é detectada | `deck_manual_mutation_route_contract_test.dart:68-81` só afirma que a string `'error_code': 'import_deck_changed'` existe no fonte e a ordem do lock | 409 por revision/artifact + teste comportamental |
| D6. Undo restaura exatamente | NAO_ENCONTRADO | Sem ledger de import | — | Evento no ledger de P0-01 + undo |

**O que realmente falta.** Quase todo o "duas fases": hoje o import é atômico (bom, mas não provado para falha dentro da tx) e de um tiro só. Detalhe que pesa no desenho: a rota inteira está atrás de `deck_replace_all` (`release_capability_policy.dart:394-396`), então o preview precisa de classificação própria para ser alcançável com o portão fechado.

- Dependências declaradas `DCK-P0-01` e `DCK-P0-02`: reais (commit por artifact/revision, undo pelo ledger). Não declaradas: decisão de capability do preview e a decisão de produto de P0-00.
- Sobreposição: P0-02 (artifact), P0-01 (undo), DCK-P1-02 (assinatura do import-new, que hoje é client-only e ignora o comandante).
- Trabalho: rota de preview, builder de diff, `to-deck/index.dart`, evento do ledger, `release_capability_policy.dart` + teste, `app/lib/core/config/release_capabilities.dart`, `deck_import_list_dialog.dart`, `deck_provider_support_import.dart`, `deck_provider.dart` → **~10 arquivos, ~8 testes** (preview sem DML, diff completo, commit ok, stale 409, tamper, undo exato, falha dentro da tx com releitura, widget do diálogo). Sem migração própria. Decisão humana: capability do preview (e se o merge add-only sai de `deck_replace_all`). Prova viva sim (UI de diff).

---

## DCK-P0-04 — Generate persiste request/fingerprint e materializa server-side pelo job

Aceite (l.282): "Resultado A nunca salva como controles B; cliente não injeta lista; cross-device reidrata a entrada original."

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| E1. Job persiste `request_key` + fingerprint, idempotente | PARCIAL | `server/lib/ai_generate_job.dart:55-115` (`ON CONFLICT (user_id, request_key) DO NOTHING`; fingerprint diferente → `AiJobIdempotencyConflict`); rota `routes/ai/generate/index.dart:1384-1411` (fingerprint = `cacheKey`, 409 `ai_job_idempotency_conflict`); schema `database_setup.sql:1771-1801`. **Persistência efêmera:** `_jobTtl = 30 min` (l.12) e `_cleanup` faz `DELETE FROM ai_generate_jobs` (l.345-353), disparado em `get`/`latest` (l.119,169) fora do runtime isolado (l.355-363) | `ai_job_lifecycle_live_test.dart:119-148` chama o store direto no pool (reuso e conflito); o 409 HTTP não tem teste | Retenção durável (ou tabela de pedidos separada da fila) + teste da rota |
| E2. Entrada original persistida (prompt, comandante, bracket, constraints) | NAO_ENCONTRADO | A tabela só guarda `cache_key`, `format`, `request_key`, `request_fingerprint`, `result`; o fingerprint é hash (`generate/index.dart:1384-1391`). O `result` ecoa `prompt` (l.969, 2149, 2233) só ao completar e some em 30 min. O app já grava o prompt bruto em `decks.description` ao salvar (`deck_generate_screen.dart:883`) | — | Coluna/tabela `request_payload` gravada no create, com retenção decidida |
| E3. Cross-device reidrata a entrada original | PARCIAL | `GET /ai/generate/jobs/latest` (`routes/ai/generate/jobs/[id].dart:26-40`); rascunho **local** por owner (`app/lib/features/decks/services/deck_entry_draft_store.dart:11-43`) | — | Depende de E1 + E2 + app ler o payload do servidor |
| E4. Materialização server-side pelo job | NAO_ENCONTRADO | Nenhum `INSERT INTO decks` no fluxo de Generate (criadores de deck: só `decks/index.dart:358`, `import/index.dart:297`, cópia da comunidade l.403 e Rebuild l.500); o app monta a lista e chama `createDeck` → `POST /decks` (`deck_generate_screen.dart:839-887`; `deck_provider_support_mutation.dart:42`) | — | Rota de materialização que lê o `result` persistido; app para de enviar `cards` |
| E5. Cliente não injeta lista | NAO_ENCONTRADO | `POST /decks` aceita qualquer `cards` (`decks/index.dart:336-337`) | `deck_provider_test.dart:686-710` só verifica no cliente bracket devolvido ≠ pedido | Depende de E4 (rejeitar `cards` quando vier `source_job_id`) |
| E6. Resultado A nunca salva como controles B | PARCIAL | Cliente: bracket e comandante preferem os gerados (`deck_generate_screen.dart:765-769,830-836`); bloqueios de mock/`can_save=false` (`deck_provider_support_generation.dart:724-796`). **Violação atual:** trocar o formato (l.1121-1128) ou editar o prompt (l.75) não invalida o resultado, e o salvamento usa `_selectedFormat` e `_promptController` atuais (l.880-887). Servidor: nada liga o deck ao job | `deck_provider_test.dart:665-710,763-900` (bloqueios no cliente) | Invalidar o resultado ao mudar entrada; vínculo job→deck com fingerprint no servidor |

**O que realmente falta.** A metade "persiste" é menor do que parece: o job inteiro some em 30 minutos, então não dá para materializar depois nem reidratar em outro aparelho. A entrada bruta não é gravada. A metade "materializa" é zero. E o próprio aceite central ("A nunca salva como controles B") é violável hoje pela tela, trocando o formato depois de gerar.

- Dependência declarada `DCK-P0-02`: real para o artifact do commit. Não declaradas: `BT-DB-001` (migration), `BT-PRIV-003` (retenção de prompts, l.267 do backlog) e o redesenho da retenção do job. E2 e a retenção podem começar sem P0-02; a correção de E6 no cliente pode ir agora.
- Sobreposição: DCK-P1-10 (drafts server-side), DCK-P1-05 (review de Generate), DCK-P1-08 (Rebuild grava clone por padrão, mesma lacuna de artifact), A8 de P0-00.
- Trabalho: 2 de schema (payload, retenção, `source_generate_job_id` ou evento no ledger), `ai_generate_job.dart`, `routes/ai/generate/index.dart`, rota nova de materialização, `decks/index.dart`, `release_capability_policy.dart` + teste, e no app `deck_generate_screen.dart`, `deck_provider_support_generation.dart`, `deck_provider.dart`, `deck_entry_draft_store.dart` → **~13 arquivos, ~8 testes** (payload gravado, sobrevive ao cleanup, `latest` reidrata, materialize = `result`, `cards` injetado rejeitado, retry do materialize não duplica, 409 HTTP de idempotência, troca de formato invalida o resultado). Migração sim. Decisão humana: retenção do prompt bruto (e o que fazer com o prompt já gravado em `decks.description`), janela de retenção do job. Serviço externo: provedor de IA só para a prova viva. Prova viva sim (Web↔Android).

---

## DCK-P0-06 — Soft-delete, lixeira, restore e purge posterior governado

Aceite (l.284): "DELETE não apaga cards imediatamente; some de todas as superfícies; restore íntegro e privado." Onda 02 (l.11): "purge tem janela, audit e restore de backup".

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| F1. Coluna de soft-delete | PRONTO_SEM_PROVA | Migration `003 add_deck_deleted_at` (`server/bin/migrate.dart:82-93`); `database_setup.sql:337`; índice parcial `idx_decks_user_validation_state ... WHERE deleted_at IS NULL` (l.395-397) | nenhum teste de deck toca `deleted_at` | — (inerte há 55 migrations) |
| F2. DELETE marca em vez de apagar; cartas ficam | NAO_ENCONTRADO | `lib/battle/interactive_battle_deck_lifecycle.dart:55-63`: `DELETE FROM decks`. CASCADE apaga 8 tabelas: `deck_cards` (`database_setup.sql:402`), `deck_matchups` (475-476), `battle_replay_annotations` (616), `deck_weakness_reports` (1653), `ai_optimize_jobs` (1742), `post_game_notes` (2307), `deck_comments` (2429), `deck_optimization_events` (`migrate.dart:1029`); outras ~10 ficam com `deck_id` nulo (ex.: l.487-501, 789-790, 1102-1103) | `decks_crud_test.dart:388-406` e `deck_crud_atomicity_live_test.dart:191-200` afirmam 204 e depois 404 (compatíveis com soft-delete); `decks_crud_test.dart:437-476` se chama "should cascade delete deck cards" mas não afirma nada sobre as cartas | `UPDATE decks SET deleted_at = now()` mantendo o guard de Battle ativa (l.34-53); renomear/inverter o teste de cascade |
| F3. Some de todas as superfícies do dono | NAO_ENCONTRADO | 26 arquivos leem/escrevem `decks` sem `deleted_at` (lista abaixo), entre eles `GET /decks` (`decks/index.dart`) e `GET /decks/:id` | — | Filtro em cada um (ou view `active_decks`) |
| F4. Some das superfícies públicas/sociais | **PARCIAL [rev]** (era PRONTO_SEM_PROVA) | Filtram `d.deleted_at IS NULL`: `lib/community_engagement_service.dart:19,113`; `routes/community/decks/index.dart:34`; `routes/community/decks/[id]/index.dart:55,352`; `routes/community/users/[id].dart:46,153`; `routes/binder/index.dart:116`; `routes/binder/[id]/index.dart:339`; `routes/ai/simulate/index.dart:489`; `lib/collection_availability_contract.dart:29`; `lib/battle/battle_preflight_service.dart:272,326`; e, **[rev]** não citados antes, `lib/community_following_feed_service.dart:21` e `routes/community/users/index.dart:116`. **[rev] Superfície pública que escapa:** o relatório compartilhável. `GET /reports/:id` (`routes/reports/[id].dart:7-24`) é control-plane (`release_capability_policy.dart:584-585`) e não tem middleware de auth. `ShareableReportService.getPublicReport` (`lib/reports/shareable_report_service.dart:84-100`) devolve o snapshot completo do deck, com as cartas, sem olhar `decks`. Relatórios nascem `is_public = TRUE` e sem expiração (l.44-66), e `shared_deck_reports.deck_id` é `ON DELETE SET NULL` (`server/bin/migrate.dart:927`): o snapshot público **sobrevive até ao hard delete de hoje** e segue alcançável com todas as capabilities OFF. Criar relatório novo exige `gallery_public` (OFF), então o risco é o estoque criado antes de 2026-08-13 | nenhum teste com deck soft-deletado | Ao apagar (soft ou hard), despublicar ou apagar os `shared_deck_reports` do deck, ou fazer `getPublicReport` exigir deck vivo; 1 live cobrindo comunidade e relatório de deck apagado |
| F5. Lixeira (listar) e restore | NAO_ENCONTRADO | Nenhuma rota em `server/routes/decks/**`; nenhuma tela no app; `deleteDeck` do app remove da lista sem desfazer (`deck_provider.dart:530-560`) | — | 2 rotas + tela |
| F6. Restore íntegro e privado | NAO_ENCONTRADO | — | — | Restore com `is_public=false`, revisão preservada (P0-01), evento no ledger |
| F7. Purge governado (janela, audit, backup) | NAO_ENCONTRADO | Não há purge em `server/lib`, `server/bin` nem `server/routes` (grep vazio); `server/lib/retention/` só contém `post_game_note_service.dart` | — | Job de purge com janela, audit e receipt |

Arquivos que leem/escrevem `decks` sem `deleted_at` (critério: `(FROM|JOIN|UPDATE) decks` em `server/routes` e `server/lib` sem nenhuma ocorrência de `deleted_at` no arquivo) — **26**: `lib/ai/optimize_request_support.dart`, `lib/battle/battle_replay_annotation_service.dart`, `lib/battle/battle_replay_read_service.dart`, `lib/battle/interactive_battle_deck_lifecycle.dart`, `lib/deck_validation_route_support.dart`, `lib/reports/shareable_report_service.dart`, `routes/ai/{archetypes,rebuild,simulate-matchup,weakness-analysis}/index.dart`, `routes/decks/[id]/{ai-analysis,analysis,cards,cards/bulk,cards/replace,cards/set,export,optimizations,optimizations/[eventId]/rollback,pricing,recommendations,simulate}/index.dart`, `routes/decks/[id]/index.dart`, `routes/decks/index.dart`, `routes/import/index.dart`, `routes/import/to-deck/index.dart`.

**O que realmente falta.** A coluna existe e as superfícies de comunidade e de Battle a respeitam. **[rev]** O relatório compartilhável público não: ele guarda o próprio snapshot e sobrevive ao delete. O dono também não: o DELETE é físico, leva junto oito tabelas (inclusive notas pós-jogo, comentários e o histórico de Optimize) e nenhuma rota de dono filtra. "Restore íntegro" hoje é impossível até por desenho, porque tudo que pende do deck é apagado. Não existe nenhuma infraestrutura de purge no servidor para copiar.

- Dependência declarada `BT-DB-001`: parcialmente real — trocar o DELETE e filtrar não exige DDL (a coluna existe); índice da lixeira, audit do purge e integridade de revisão exigem. Não declaradas: `DCK-P0-01` ("restore preserva revisão", onda l.11) e `BT-PRIV-002` (exclusão de conta precisa enxergar decks na lixeira).
- Sobreposição: DCK-P1-01/DCK-P1-09 (restore privado), BT-PRIV-002, DCK-P2-01.
- Trabalho: lifecycle do delete, 26 filtros (um deles, `shareable_report_service.dart`, também no `getPublicReport` **[rev]**), 2 rotas, 1 lib de purge + 1 agendamento, 2 de schema, `user_data_privacy_service.dart`, app (`deck_provider.dart`, `deck_provider_support_fetch.dart`, tela de lixeira, entrada na `deck_list_screen.dart`) → **~38 arquivos, [rev] ~11 testes** (delete preserva cartas e dependentes; lista/detalhe/mutators 404 para deletado; comunidade omite; **[rev]** relatório público de deck apagado some; restore privado e íntegro; restore de deck alheio 404; purge respeita janela; Battle ativa ainda bloqueia; ledger sobrevive; widget da lixeira; renomear o teste de cascade). Migração sim (pequena). Decisão humana: janela de purge, se deck na lixeira conta em limites/aprendizado/export e **[rev]** se restaurar republica os relatórios. Prova viva sim (UI). **[rev]** O aceite da onda inclui "restore de backup" no purge (`waves/02-deck-foundation.md:11`). Isso é ensaio de restore com a infra de backup do grupo de infraestrutura, fora destes 38 arquivos.

---

## DCK-P0-07 — Isolar sessão/conta e ordenar respostas assíncronas no app

Aceite (l.285): "Logout troca epoch e limpa jobs/history/caches; resposta tardia de deck/usuário A nunca sobrescreve B; testes determinísticos A→B e login A→logout→login B." Onda 02 (l.12): "restart e forced logout cobertos".

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| G1. Logout troca epoch | PARCIAL | `AuthProvider._authGeneration` (`app/lib/features/auth/providers/auth_provider.dart:36`), incrementado em login/register/logout/expire (l.85,160,251,419,448) e checado nos fluxos do próprio AuthProvider. Providers irmãos têm geração própria incrementada no `clearAllState` (Trade `trade_provider.dart:1064-1068`, Message `message_provider.dart:473`, Notification `notification_provider.dart:305`, Binder `binder_provider.dart:1316-1319`, Community `community_provider.dart:577`). `DeckProvider`, `SocialProvider` e `MarketProvider` não têm nenhuma | `auth_provider_log_sanitization_test.dart:290` (refresh tardio não repovoa usuário após logout); `auth_provider_security_test.dart:135-162` (expire limpa sessão) | Geração no `DeckProvider` (e Social/Market) |
| G2. Logout limpa jobs/history/caches | PARCIAL | `main.dart:953-1005` chama `_clearAllProvidersState` (l.1159-1169) no logout e na troca de conta. `DeckProvider.clearAllState` (`deck_provider.dart:1611-1625`) limpa lista, selecionado, detalhes e análise, mas **não** `_listStatusCode`, `_lastAppliedOptimizationEventId`, `_activeOptimizeJobId`, `_activeOptimizeDeckId`, `_activeOptimizeRequestKey`, `_optimizationHistory`, `_optimizationHistoryLoading`, `_optimizationHistoryErrors` (campos l.57-75; `clearAllCache`, l.1426-1436, limpa o histórico mas não é chamado no logout) | `release_capability_surface_contract_test.dart:179-205` só afirma, no fonte do `main.dart`, o reset de capabilities; nada testa o `DeckProvider` | Completar `clearAllState` |
| G3. Resposta tardia de deck A nunca sobrescreve deck B | NAO_ENCONTRADO | `fetchDeckDetails` (`deck_provider.dart:226-291`): após o `await` (l.259) só checa `_isDisposed` (l.260) e grava `_selectedDeck` e o cache (l.261-273); a tela seleciona `p.selectedDeck` sem comparar com `widget.deckId` (`deck_details_screen.dart:620`) | `deck_provider_test.dart:190-209` cobre só o descarte após `dispose` | Geração por requisição de detalhe + checagem de id na tela |
| G4. Resposta tardia de usuário A nunca sobrescreve B | NAO_ENCONTRADO (no DeckProvider) | `fetchDecks` (l.391-444) grava `_decks` após o `await` sem geração; `ApiClient.setToken` é estático (`api_client.dart:54-57`), então requisição disparada sob A chega normalmente após o login de B; 26 pontos `await` de rede no provider. Rascunhos locais **são** isolados por owner (`deck_entry_draft_store.dart:11-43`) | Padrão pronto e testado nos irmãos: `trade_provider_test.dart:366-410`, `notification_models_test.dart:339` | Aplicar o mesmo padrão |
| G5. Testes determinísticos A→B e login A→logout→login B | NAO_ENCONTRADO | — | Moldes: `trade_provider_test.dart:366-410` (Completer por endpoint + `clearAllState`); `_DelayedGetApiClient` em `deck_provider_test.dart:80-85` | ~5 testes |
| G6. **[rev] nova** — "restart e forced logout cobertos" (onda 02 l.12, que a medição não decompôs) | PARCIAL | Restart: o estado do `DeckProvider` é só memória (`deck_provider.dart:66-78`, sem `SharedPreferences`), e o único estado de deck persistido, os rascunhos, é chaveado por owner (`deck_entry_draft_store.dart:11-43`). Forced logout: 401 → `AuthStatus.unauthenticated` → o mesmo `_clearAllProvidersState` do logout (`main.dart:986-1005`), portanto herda G2, G3 e G4. O loop de polling do Optimize só é cancelado pelo token de cancelamento da tela, não pelo `clearAllState` (`deck_provider.dart:823-834`) | Restart **provado** para rascunhos: `app/test/features/decks/services/deck_entry_draft_store_test.dart:8-38` (rascunho de A não aparece para B e some ao limpar). Forced logout: `auth_provider_security_test.dart:135-162` cobre só o AuthProvider. Nada exercita o DeckProvider | 1 teste: 401 no meio de um `fetchDeckDetails`/polling de Optimize, depois login de B, e nada de A aparece |

**O que realmente falta.** A limpeza central, o epoch do AuthProvider e o padrão "geração incrementada no `clearAllState` + checagem pós-`await`", com testes, já existem em cinco providers. Falta aplicá-lo ao `DeckProvider` (26 pontos assíncronos), completar o `clearAllState` e fazer a tela de detalhes conferir o id. Se "usuário" for lido de forma ampla, Social e Market também ficam sem proteção. **[rev] Sobre o rótulo:** nenhuma das asserções do aceite está satisfeita no `DeckProvider`. Há 3 parciais e 3 inexistentes. DCK-P0-03, com mais código existente (1 pronta + 2 parciais), está em "mal-começada". Pelo mesmo critério, esta tarefa é **mal-começada**, não "metade". O que a distingue é o custo: é a menor e mais mecânica do grupo, com molde e testes-molde prontos.

- Dependência declarada: nenhuma — correto; pode começar agora.
- Sobreposição: DCK-P1-13 (cache owner+deck+revision) reutiliza a mesma geração.
- Trabalho: `deck_provider.dart`, `deck_details_screen.dart`, `social_provider.dart`, `market_provider.dart`, `deck_provider_test.dart` + 1 teste novo de sessão → **6 arquivos, [rev] 6 testes** (detalhe A→B mesmo usuário; `fetchDecks` tardio após `clearAllState`; análise tardia; job/histórico de Optimize limpos no logout; login A→logout→login B; **[rev]** forced logout com requisição e polling em voo). Sem migração, sem decisão humana, sem prova viva (o aceite pede testes determinísticos).

---

## DCK-P1-04 — Uma única verdade de validação/readiness estrita por revisão

Aceite (l.294): "UI nunca diz 'pronto' sem strict atual; ausência de legalidade não vira legal." Onda 02 (l.14): "backend e app resolvem a mesma legalidade/estrutura; ausência de dado não vira legal; swap 1→1 invalida cache/readiness".

| Asserção | Estado | Onde está | Teste | O que falta |
| --- | --- | --- | --- | --- |
| H1. Estado de validação persistido e consistente | **PRONTO_SEM_PROVA [rev]** (era PRONTO_E_PROVADO) | `database_setup.sql:316-318,348-397` (colunas, CHECK de estado e de payload); `lib/deck_validation_state_support.dart`; `lib/deck_readiness_contract.dart`; o create persiste o estado na mesma tx (`routes/decks/index.dart:511-542`) | **[rev] Determinístico:** só o vocabulário e a exposição (`server/test/deck_validation_state_support_test.dart:6-63`). CHECK e trigger aparecem só em testes de string sobre o SQL (`data_model_migration_test.dart:477-516,734-749`) e sobre o fonte das rotas (`deck_validation_state_route_contract_test.dart:7-39`). **Live:** `deck_validation_state_live_test.dart:113-235` cobre exatamente a asserção (novo `unknown`; create `draft`/`incomplete_deck_size`; `/validate` → `validated`; rename mantém; **60→59 Plains → `draft`**, e **[rev]** não é "troca de carta"; strict falho → 400 com os dois motivos; troca de formato → `draft`). Mas o último receipt é de 2026-07-22 (`docs/qa/MANALOOM_SPRINT4_CORE_EVIDENCE_2026-07-22.md:236-248`). Pelo contrato E2E (l.219-232), esse relatório é `historical_evidence`, que nunca é promovido. As rotas exercitadas mudaram em 6 commits depois dele (`bd2c65a93`, `58c161bc4`, `35370ed89`, `8264ffb27`, `b2d3fc04f`, `f6f791098`). Hoje o teste só roda com política isolada que ligue `account_registration`, `catalog_private` (usa `GET /cards`), `decks_private` e `deck_replace_all` (usa `PUT`), e o repo não tem gerador dessa política para testes de deck | Nenhum teste novo: escrever a política isolada, rodar pelo `manaloom_server_contract_e2e_isolated.sh` e registrar o receipt em `docs/qa/execution/` |
| H2. Mudança de cartas/formato invalida automaticamente | **PRONTO_SEM_PROVA [rev]** (era PRONTO_E_PROVADO) | Triggers `manaloom_deck_cards_require_review` (`database_setup.sql:414-447`, `AFTER INSERT OR DELETE OR UPDATE OF deck_id, card_id, quantity, is_commander`, que cobre troca 1→1 por `UPDATE card_id`) e `manaloom_deck_format_require_review` (l.450-469); também em `migrate.dart:1784-1826`. O app ressincroniza pela assinatura id+quantidade (`deck_details_screen.dart:988-994`, `_deckValidationSignature` l.1988-2001), então a troca 1→1 também invalida o cache do app | **[rev]** Mesmo live de H1, com a mesma falta de receipt vigente. Nenhum teste determinístico exercita o trigger. A troca 1→1 (`card_id` diferente, mesma contagem), que é o caso da onda, não é exercitada: o live só muda quantidade e formato | Receipt (como em H1); 1 caso de troca 1→1 no live; ligar a `revision` quando P0-01 existir |
| H3. UI nunca diz "pronto" sem strict do servidor | **PARCIAL [rev]** (era PRONTO_E_PROVADO) | Obedecem ao servidor: `Deck.isValidated` (`app/lib/features/decks/models/deck.dart:33-34`); lista (`deck_list_screen.dart:1305-1314`, "Validado" só com `isValidated`); visão geral (`deck_details_overview_tab.dart:79-98`); a tela ressincroniza o resultado pelo conteúdo do deck (`deck_details_screen.dart:188-197,988-994`). **[rev] Violação:** o painel de diagnóstico da visão geral (`app/lib/features/decks/widgets/deck_diagnostic_panel.dart:256-281`, renderizado em `deck_details_overview_tab.dart:318-325` sempre que `onShowOptimizationOptions` existe, ou seja, com `ai_analyze_optimize_advisory` ON, `deck_details_screen.dart:772-774`) diz **"Base pronta para testar"** e "Deck jogável, mas com ajustes importantes" a partir do `readiness` do `/analysis`. Esse readiness não é strict e trata legalidade ausente como legal (`server/routes/decks/[id]/analysis/index.dart:285-287,801-808`). O painel não olha `validation_state` | Provam as superfícies obedientes: `app/test/features/decks/widgets/deck_analysis_tab_test.dart:451-494` (100 cartas em `draft` mostra "Revisar" e nunca "Pronto"/"100/100"); l.496-520 (só "Validado" com estado do servidor); `deck_details_overview_tab_test.dart:432-460` (hero mostra "Rascunho"). **[rev] Teste que trava o oposto:** `deck_diagnostic_panel_test.dart:1012-1176` renderiza "Base pronta para testar" para `makeHealthyCommanderDeck()` (l.33-60), que não define `validationState` e por isso fica `unknown` (`deck.dart:58`) | Painel condicionado a `deck.isValidated` (ou texto que não afirme prontidão); ajustar o teste de l.1012-1176; widget test da lista |
| H4. Ausência de legalidade não vira legal | NAO_ENCONTRADO (o código faz o oposto) | `lib/deck_rules_service.dart:174-175` (`if (status == null) continue;`, também em strict — `strict` só afeta tamanho/comandante, l.62,212,230,345); `_loadLegalities` devolve só linhas existentes (l.456-472); add unitário só bloqueia se houver linha (`routes/decks/[id]/cards/index.dart:109-132`); análise assume legal (`routes/decks/[id]/analysis/index.dart:285-287`); filtro do Optimize deixa passar sem linha (`lib/ai/optimize_format_legality_support.dart:46-47`, `HAVING COUNT(cl.status) = 0 OR ...`). **[rev] Contagem corrigida:** eram "20 consultas `cl.status IS NULL OR` em 7 arquivos". São **12 filtros que admitem carta sem linha** em 3 arquivos (`optimize_filler_loader_support.dart` ×9, `optimize_runtime_support.dart` ×2, `optimize_swap_candidate_support.dart` ×1), mais o `HAVING` acima: 13 em 4 arquivos. Os outros 7 casos (`import_card_lookup_service.dart:307,397,501`, `deck_card_name_resolution_support.dart:174,270`, `commander_reference_helpers.dart:129,172`) são `ORDER BY CASE ... WHEN cl.status IS NULL THEN 2`: ranking de printing que põe legal antes de desconhecido, e não admissão | `deck_rules_service_test.dart` não tem caso de legalidade ausente | Strict com `legality_unknown`; decidir se os seletores de candidatos acompanham (4 arquivos, não 7) |
| H5. Mesma verdade entre backend e app | PARCIAL | App não recomputa legalidade, mas a lista rotula por contagem ("A validar", "Excede limite", "Incompleto", `deck_list_screen.dart:1305-1314`) e a tela monta um resultado local a partir do persistido (`deck_details_screen.dart:2003-2020`) | — | Rotular só a partir de `deck_state`/`review_reasons` |
| H6. Mutação manual devolve readiness na mesma resposta | PARCIAL | Create e Optimize devolvem; `cards`, `set`, `replace`, `import/to-deck` não | — | Ver B5 (P0-01) |
| H7. "Strict atual": validado cai quando a regra/legalidade muda | NAO_ENCONTRADO | A sync declara não alterar decks (`server/bin/sync_card_legalities_from_scryfall.py:11`); nenhum trigger em `card_legalities`; os triggers só olham cartas/formato do deck. **[rev]** A sync citada não é o único escritor. `card_legalities` também é escrita por `server/bin/sync_cards.dart:847`, `server/routes/cards/resolve/index.dart:678` (em runtime), `server/bin/sync_cards_full_fast.py`, `server/bin/seed_legalities.dart` e `server/bin/seed_legalities_optimized.dart`. Rebaixar "na sync" deixaria 5 caminhos de fora | — | **[rev]** Trigger em `card_legalities` que marque `draft` os decks `validated` afetados (migration, cobre os 6 escritores) ou versionar a legalidade no estado validado |

**O que realmente falta.** A verdade única de estado existe no banco. **[rev]** Mas hoje não está provada: o live que a exercita tem só receipt histórico de 2026-07-22, e as rotas mudaram 6 vezes desde então. A UI obedece ao servidor em resumo, chip e lista, provados por widget tests, e **fura no painel de diagnóstico**, que diz "Base pronta para testar" para deck nunca validado, com um widget test travando isso. O que contraria o aceite de frente é a regra de legalidade: ausente = legal em três validadores e em 13 filtros de candidatos, e um deck validado não cai quando uma carta dele é banida.

- Dependência declarada `DCK-P0-01`: real só para "por revisão"; H4 e H7 podem ir agora. **[rev]** Se H7 for por trigger, entra na fila de migration de `BT-DB-002`.
- Sobreposição: DCK-P1-06 (legalidade/estrutura/evidência) e DCK-P1-12 usam a mesma razão; H6 = B5. **[rev]** A correção do painel de H3 é a mesma de DCK-P1-06 ("nenhum score/heurística aparece como regra").
- Trabalho mínimo: `deck_rules_service.dart`, `cards/index.dart`, `analysis/index.dart`, `optimize_format_legality_support.dart`, `deck_readiness_contract.dart`, `deck_list_screen.dart`, `deck_rules_service_test.dart`, **[rev]** `deck_diagnostic_panel.dart` + `deck_diagnostic_panel_test.dart`, `migrate.dart` + `database_setup.sql` (trigger de H7, no lugar de mexer só no `.py`), 1 arquivo de política isolada e o live test → **[rev] ~13 arquivos** (16 se alinhar os 3 arquivos restantes de filtro de candidatos), **[rev] 8 testes** (unit strict com legalidade ausente, contract da razão, live revalidando deck com carta sem linha, trigger rebaixa validado quando a legalidade muda, widget da lista, painel não diz "pronta" sem `validated`, troca 1→1 no live, receipt do live de H1/H2). **[rev] Migração provável** (trigger). **[rev] Prova viva sim** (harness PG isolado + receipt). Decisão humana: tornar legalidade ausente bloqueante pode rebaixar decks hoje `validated` e esvaziar pools de Optimize/Generate — precisa de contagem no banco e aceite do dono.

---

## Achados transversais

1. **Não existe revisão de deck em lugar nenhum**; existem três substitutos por hash de conteúdo (`deck_snapshot_hash` no GET, `buildDeckSignature` no Optimize, `subject_deck_revision` nas anotações de replay). O padrão `revision`/409 de `post_game_notes` pode ser copiado, mas lá a revisão é opcional.
2. **Todos os writers serializam com `FOR UPDATE`**; concorrência hoje é last-writer-wins, não conflito detectado. `cards/set` e `import/to-deck` (inclusive o merge) apagam e reinserem a lista inteira.
3. **A contenção criou um editor que só adiciona.** Com `decks_private` ON e `deck_replace_all` OFF não há como remover carta, editar descrição/estratégia, alternar público, trocar edição ou aplicar Optimize por troca, e não existe rota alternativa. **[rev]** Renomear não entra na lista: o app não tem essa ação. É a decisão de produto que destrava P0-00 e antecipa DCK-P1-03.
4. **Inconsistência 400/409 no stale do apply de Optimize**: via `PUT` 400 (`decks/[id]/index.dart:247-250,586-588`), via bulk 409 (`cards/bulk/index.dart:153-159,364-366`) — e o app usa o `PUT` para o modo troca.
5. **O eixo PROVADO do grupo está congelado desde julho.** Todo teste comportamental com banco exige `RUN_INTEGRATION_TESTS=1` e política isolada que ligue `account_registration`, `decks_private`, **[rev]** `catalog_private` (quem busca carta por `GET /cards`) e, para quem usa `PUT`, `deck_replace_all`. Os receipts são de 2026-07-22 (`docs/qa/MANALOOM_SPRINT4_CORE_EVIDENCE_2026-07-22.md`, `..._SPRINT6_DECKBUILDER_EVIDENCE_2026-07-22.md`), antes do portão (`b2d3fc04f`, 2026-08-13) e do HMAC obrigatório (`35370ed89`, 2026-07-31). **[rev]** Pelo contrato E2E (`docs/MANALOOM_E2E_RELEASE_CONTRACT.md:219-232`), esses relatórios são `historical_evidence` e não valem como prova do código atual. O `mana_floor` **nunca teve receipt**. Só ele está numa suíte (`scripts/manaloom_e2e_suite.sh:600`), chamada sem política isolada. O runner isolado (`scripts/manaloom_server_contract_e2e_isolated.sh:658-659`) aceita a política por variável, mas não existe gerador dela para os testes de deck (só `manaloom_play_vs_ai_e2e.sh:198` e `manaloom_authenticated_visual_qa_isolated.sh:24` geram as suas). E `deck_optimization_apply_rollback_live_test.dart` **quebraria hoje**. **[rev] Consequência:** H1, H2 e C5, que a medição contava como provadas, dependem só desses live tests.
6. **Legalidade ausente = legal é convenção do servidor inteiro** (3 validadores + **[rev]** 13 filtros de candidatos em 4 arquivos; os outros 7 `cl.status IS NULL` são ranking, não admissão), e a sync de legalidades não rebaixa decks validados. **[rev]** São 6 escritores de `card_legalities`, então a correção natural é um trigger (migration).
7. **`decks.deleted_at` existe desde a migration 003** e é respeitado por comunidade, fichário, simulate e Battle, mas o DELETE é físico e cascateia 8 tabelas; não existe infraestrutura de purge.
8. **`ai_generate_jobs` é fila de 30 minutos, não registro**: tudo que P0-04 e DCK-P1-10 planejam "em cima do job" precisa antes de retenção durável.
9. **"IA advisory" vale para 2 de 4 fluxos** (Recommendations, Optimize); Generate grava pela lista do cliente e Rebuild grava clone por padrão sem artifact.
10. **Dois live tests afirmam o contrário do aceite ou estão obsoletos**: `decks_crud_test.dart:184-212` (deck vazio público) e `deck_optimization_apply_rollback_live_test.dart` (apply sem token). Fechar as tarefas exige reescrevê-los, não só somar testes.
11. **O padrão de isolamento de sessão já existe no app** (5 providers, com testes); P0-07 é aplicação mecânica ao `DeckProvider`. **[rev]** Mecânica e pequena, mas ainda por fazer: nenhuma asserção do aceite está satisfeita.
12. **`BT-DB-001` só bloqueia de verdade** P0-01 (revision/ledger), P0-04 (payload/retenção) e a parte de purge/audit de P0-06. P0-07, P0-00, P0-02 (stateless), H4 de P1-04 e o soft-delete+filtros de P0-06 podem andar sem DDL. **[rev]** H7 também pode andar sem DDL se for feito nos 6 escritores. Pelo caminho robusto, o trigger, precisa de DDL. **[rev]** A cadeia real de DDL é `BT-DB-001` → `BT-DB-002` (migration preservadora `059`, com preflight/postcheck) → `BT-DB-003` (upgrade/rollback por restore). Nenhuma tarefa do grupo declara `BT-DB-002`, e as 3 ou 4 migrations do grupo disputam o mesmo número.
13. **Ordem sugerida pela medição**: P0-07 (só app, com molde) e H4/H7 de P1-04 (após a decisão de rebaixamento) fecham rápido; P0-00 fecha com 2 guards, 1 teste invertido e a decisão do editor; reescrever o live test de rollback já, independente de P0-01; P0-01 é a raiz de P0-02/P0-03/P1-04-por-revisão e espera BT-DB-001; P0-06 pode começar pelo soft-delete+filtros; P0-04 precisa das decisões de privacidade e de retenção antes da migration, mas a invalidação do resultado ao trocar formato/prompt pode ir agora. **[rev]** Acrescentar ao começo da fila, porque é barato e devolve o eixo PROVADO: (a) escrever a política isolada de capabilities para os live tests de deck e re-rodar `deck_validation_state_live_test`, `deck_crud_atomicity_live_test`, `import_to_deck_flow_test` e `mana_floor` com receipt em `docs/qa/execution/`; (b) acrescentar ao mapa de `release_capability_policy_test.dart` as linhas de add e create.
14. **[rev] Relatório compartilhável é uma superfície pública viva fora da matriz de capabilities.** `GET /reports/:id` é control-plane (`release_capability_policy.dart:584-585`), sem auth, e serve o snapshot completo do deck (`shareable_report_service.dart:84-100`), que nasce público e sem expiração (l.44-66) e sobrevive ao delete (`migrate.dart:927`, `ON DELETE SET NULL`). Afeta P0-06 (F4) e interessa ao grupo de privacidade.
15. **[rev] Uma superfície de IA/heurística afirma prontidão.** O painel de diagnóstico diz "Base pronta para testar" com base no `readiness` não strict do `/analysis` (`deck_diagnostic_panel.dart:256-281`), e um widget test trava isso para deck `unknown` (`deck_diagnostic_panel_test.dart:1012-1176`). É o terceiro teste do grupo que afirma o contrário do aceite, ao lado de `decks_crud_test.dart:184-212` e do rollback obsoleto.
16. **[rev] P0-04 depende de uma P1.** `BT-PRIV-003` (retenção de prompts), da qual a medição já dizia depender, é **P1** no registry (`TASK_REGISTRY.json`, source_line 267) e depende de `BT-PRIV-001`/`BT-PRIV-002`. Uma P0 CORE esperando uma P1 precisa ser repriorizada, ou a decisão de retenção precisa ser tomada fora dela.

---

## Verificação adversarial (2026-09-22, tarde)

**Escopo.** Reabri as 9 asserções PRONTO_E_PROVADO e as 5 PRONTO_SEM_PROVA: li cada teste citado de ponta a ponta e cada `arquivo:linha` do código. Também conferi com leitura de código 38 das 39 PARCIAL/NAO_ENCONTRADO (a exceção é F6, que decorre de F5), num total de 52 das 53 asserções originais, e decompus de novo o aceite da onda 02 (`docs/execution/waves/02-deck-foundation.md:9-14`) contra as asserções, o que revelou uma lacuna (G6). Nada foi executado: sem `dart test`, `flutter`, build ou servidor. Mesma árvore da medição: HEAD `d15beb05b`; as únicas mudanças não commitadas em `server/` e `app/lib` são do marketplace, fora do escopo.

**Critério aplicado.** Teste determinístico conta como prova. Teste live só conta com receipt ligado ao código atual. A própria medição define assim ("para prova live, receipt", l.7 original), e o contrato E2E exige o mesmo (`docs/MANALOOM_E2E_RELEASE_CONTRACT.md:219-232`: relatórios antigos em `docs/qa/` são `historical_evidence` e "nunca são promovidos"). Teste de string ou de ordem no fonte não prova.

### O que caiu (6 asserções)

| Tarefa | Asserção | De → Para | Por quê |
| --- | --- | --- | --- |
| DCK-P0-00 | A3. Add-only (add, bulk, set) continua em `decks_private` | PRONTO_E_PROVADO → PRONTO_SEM_PROVA | O mapa `release_capability_policy_test.dart:187-225` afirma `bulk` e `set`, mas **não** `POST /decks/:id/cards`, que é o add do app (`deck_provider_support_mutation.dart:75`), nem `POST /decks`. O teste "every route handler is classified" (l.309-341) só exige `capability != null`. Reclassificar o add para `deck_replace_all` passaria na suíte. O código está certo (`release_capability_policy.dart:537-541`). Faltam 2 linhas no teste. |
| DCK-P0-02 | C5. Falha sem DML | PRONTO_E_PROVADO → PRONTO_SEM_PROVA | O único teste comportamental (`deck_optimization_mana_floor_live_test.dart:573-616,655-673`) relê o deck depois de assinatura ausente, **stale** (pré-checagem anterior ao HMAC) e violações de piso com token válido. Nenhum caso de tamper, expired, outro deck ou outro usuário passa pela rota. Esses só têm o unit da função e a ordem no fonte (`optimize_apply_authorization_test.dart:840-883`, teste de string). O live **nunca teve receipt** e, chamado pela suíte (`manaloom_e2e_suite.sh:600`), para em `POST /auth/register` (l.379). O código está certo: `validateOptimizationApplyAuthorization` em `decks/[id]/index.dart:324` antes do DELETE em l.387, e em `bulk:171` antes de l.230, tudo em `runTx`. |
| DCK-P1-04 | H1. Estado de validação persistido e consistente | PRONTO_E_PROVADO → PRONTO_SEM_PROVA | A persistência, os CHECKs e as transições só têm prova no live `deck_validation_state_live_test.dart:113-235`. O último receipt dele (2026-07-22) é `historical_evidence`, e as rotas exercitadas mudaram em 6 commits depois (`bd2c65a93` … `f6f791098`). O live exige política isolada com `account_registration`, `catalog_private`, `decks_private` e `deck_replace_all`, que ninguém gera para testes de deck. O que é determinístico cobre só vocabulário e exposição (`deck_validation_state_support_test.dart:6-63`). O resto é teste de string sobre SQL e fonte (`data_model_migration_test.dart:477-516,734-749`; `deck_validation_state_route_contract_test.dart:7-39`). |
| DCK-P1-04 | H2. Mudança de cartas/formato invalida | PRONTO_E_PROVADO → PRONTO_SEM_PROVA | Mesmo live de H1, com a mesma falta de receipt. Nenhum teste determinístico exercita o trigger. E o caso que a onda nomeia, a troca 1→1, não está no live: ele muda só quantidade (60→59) e formato. A medição dizia "troca de carta → draft", o que não confere. |
| DCK-P1-04 | H3. UI nunca diz "pronto" sem strict | PRONTO_E_PROVADO → PARCIAL | `deck_diagnostic_panel.dart:256-281` diz **"Base pronta para testar"** e "Deck jogável" a partir do `readiness` do `/analysis`, que não é strict e trata legalidade ausente como legal (`analysis/index.dart:285-287,801-808`). O painel não olha `validation_state` e aparece sempre que `ai_analyze_optimize_advisory` está ON (`deck_details_screen.dart:772-774`; `deck_details_overview_tab.dart:318-325`). O widget test `deck_diagnostic_panel_test.dart:1012-1176` **trava** esse texto para um deck com estado `unknown` (fixture l.33-60; default em `deck.dart:58`). Os testes citados pela medição provam só o resumo da aba de análise e o chip do hero. |
| DCK-P0-06 | F4. Some das superfícies públicas/sociais | PRONTO_SEM_PROVA → PARCIAL | `GET /reports/:id` é control-plane (`release_capability_policy.dart:584-585`) e não tem auth. Serve o snapshot completo do deck (`shareable_report_service.dart:84-100`), que nasce público e sem expiração (l.44-66) e sobrevive até ao hard delete de hoje (`migrate.dart:927`, `ON DELETE SET NULL`). É alcançável agora, com todas as capabilities OFF. |

### Status de tarefa revisado

| Tarefa | De → Para | Por quê |
| --- | --- | --- |
| DCK-P0-07 | metade → **mal-começada** | Nenhuma das asserções do aceite está satisfeita no `DeckProvider` (3 parciais, 3 inexistentes, com G6). DCK-P0-03 tem mais código pronto e a própria medição a classificou como "mal-começada". O molde nos 5 providers irmãos barateia o trabalho, que continua o menor do grupo, mas não é implementação desta tarefa. |

As outras sete mantêm o rótulo. DCK-P1-04 fica em "metade" pelo lado da implementação, com **zero provadas** hoje.

### O que foi atacado e ficou de pé

- **A1** (`release_capability_policy_test.dart:14-35` carrega o manifesto real; o conjunto de chaves inclui `deck_replace_all` em `release_capability_policy.dart:44`; `every(!allowed)`) e **A2** (l.195-197 afirma exatamente os três mapeamentos): provas unitárias de verdade.
- **A4**: por composição. O middleware real nega antes do handler com contexto cujo `read` lança (l.393-438), e o mapeamento de A2 está provado. `_middleware.dart:105-144` não tem ramo por capability. A ordem "antes do PG" é teste de string (l.371-391), mas o caso comportamental responde sem banco.
- **C1**: o round-trip HMAC (tamper, expiry, binding de deck e de bracket, delta real) é unitário e passa pela mesma `validateOptimizationApplyAuthorization` que as duas rotas chamam. Ressalva registrada: a emissão pela rota `/ai/optimize` só tem teste de ordem no fonte (`optimize_route_resilience_support_test.dart:166-181`).
- **A6**: default `false` conferido nos 5 caminhos de criação (`decks/index.dart:335`; `import/index.dart:297` e cópia da comunidade l.403 sem a coluna, portanto `DEFAULT FALSE`; Rebuild com `false` literal, `rebuild_guided_service.dart:500-506`; app `deck_list_screen.dart:114,458`). Continua sem teste.
- **B7**: confirmado que o único teste está obsoleto. O PUT do teste passa pela pré-checagem stale e morre em `validateOptimizationApplyAuthorization` (`authorization_required`, `optimization_apply_authorization_support.dart:46-50`), mapeado para 409 (`decks/[id]/index.dart:580-582`) antes do strict. O teste espera 400 em l.188.
- **D4**: `validateAndThrow(strict:false)` antes do `DELETE`, tudo em `pool.runTx` (`import/to-deck/index.dart:213-325`). Continua sem teste que releia depois de falha dentro da transação.
- **F1**: coluna e índice existem (`migrate.dart:82-93`), inertes.

### O que subiu

Nenhuma asserção subiu de estado. Procurei promoções onde a medição disse NAO_ENCONTRADO e confirmei a ausência de cada uma no código:

- revision em `decks`;
- If-Match em deck (o único do servidor é o opcional de `post-game-notes/[noteId].dart:53-69`);
- undo universal;
- `DeckReviewArtifact`;
- purge (`git grep`);
- lixeira e restore;
- materialização do Generate;
- geração no `DeckProvider`;
- guard de deck vazio público.

A favor do medidor, três pontos:

- a nova G6 já nasce com a parte "restart" provada para rascunhos (`deck_entry_draft_store_test.dart:8-38`, que a medição não citou);
- o alinhamento de seletores de H4 é menor do que ele disse: 13 filtros em 4 arquivos, não "20 consultas em 7 arquivos", porque 7 dos casos são ranking;
- a contagem "26 arquivos de dono sem `deleted_at`" e a lista de 8 cascatas conferem.

### Correções menores feitas no corpo

- Linhas de teste de A4 (371-391 e 393-438).
- Lista completa de capabilities OFF (faltavam `account_registration`, `catalog_private` e `ai_analyze_optimize_advisory`).
- "Renomear" não existe no app.
- O 404 das 6 ações sem gate chega cru à pessoa (`friendly_error_mapper.dart:261-322`).
- `deck_version_at` é `DateTime.now()` (`decks/[id]/index.dart:831`).
- O `mana_floor` nunca teve receipt.
- `BT-DB-002`/`BT-DB-003` são dependências reais de DDL, não declaradas.
- A retenção de prompts (`BT-PRIV-003`) de que P0-04 depende é P1.
- H7 tem 6 escritores de `card_legalities`, não 1.
- Duas superfícies públicas que filtram e não tinham sido citadas: `community_following_feed_service.dart:21` e `community/users/index.dart:116`.

### Números revisados

| Tarefa | Arquivos | Testes | Migração | Prova viva |
| --- | --- | --- | --- | --- |
| DCK-P0-00 | 8 → **9** (≈15 com PATCH/remoção incremental) | 5 → **6** | não | sim |
| DCK-P0-01 | 23 → **25** (UI do desfazer) | 15 → **16** | sim | sim |
| DCK-P0-02 | 9 (≈17 se entregar sozinho todos os fluxos) | 7 → **8** | não | **não → sim** (harness PG isolado) |
| DCK-P0-03 | 10 | 8 | não | sim |
| DCK-P0-04 | 13 | 8 | sim | sim |
| DCK-P0-06 | 38 | 10 → **11** | sim | sim |
| DCK-P0-07 | 6 | 5 → **6** | não | não |
| DCK-P1-04 | 8 → **13** | 5 → **8** | **não → provável** (trigger) | **não → sim** (receipt em harness isolado) |
| **Grupo** | ~115 → **~123** | ~63 → **~71** | 3 → 3 + 1 provável, todas pela fila `BT-DB-002` | — |

Contagem por asserção: **9/5/18/21 → 4/8/21/21** (54 asserções com G6).

### Veredito de otimismo: **otimista demais**, concentrado no eixo PROVADO

A medição acertou o grosso do IMPLEMENTADO: nenhuma tarefa "já atendida" ou "quase lá", inventários conferidos, contradições reais achadas (o editor que só adiciona, o live test que trava deck vazio público, o rollback obsoleto). Mas errou para cima exatamente no eixo que o dono pediu para separar.

- **Das 9 "provadas", 5 caíram (56%).** Três delas (H1, H2, C5) contradiziam a própria regra do documento, que exige receipt para prova live.
- **Faltaram duas superfícies ativas.** Uma é o painel que diz "Base pronta para testar" sem strict, com um teste travando isso. A outra é o relatório público, alcançável hoje, que sobrevive ao delete.
- **Rótulos otimistas.** DCK-P0-07 estava em "metade" com zero asserções satisfeitas. DCK-P0-02 e DCK-P1-04 diziam "sem prova viva" enquanto a própria coluna "o que falta" pedia live tests.

O efeito prático no plano: antes de qualquer tarefa do grupo ser dada como fechada, é preciso uma rodada barata e ainda não planejada. Ela consiste em escrever a política isolada de capabilities para os live tests de deck, re-rodá-los e registrar receipts. Sem isso, o grupo tem **4 asserções provadas de 54**, não 9 de 53.
