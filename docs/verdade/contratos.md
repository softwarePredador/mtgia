# Auditoria documental — grupo "Contratos atuais" — 2026-09-22

Repo: `/Users/desenvolvimentomobile/Documents/rafa/mtg/mtgia`, branch `codex/free-beta-release-candidate-2026-07-17`, HEAD `d15beb05b` (árvore suja: `server/routes/community/marketplace/index.dart`, `docs/generated/*`, `project_logic_manifest.json`, `docs/flows/`, `docs/design/`, `docs/PONTO_DE_RETOMADA_COORDENACAO_2026-09-21.md`, `server/test/community_marketplace_privacy_contract_test.dart`).
Método: leitura integral dos 8 documentos; cada afirmação verificável conferida no disco com `grep`/`sed`/`python3`/`git log`; nenhum teste, build, servidor ou emulador executado; nenhuma escrita no repo. Único comando não trivial: `./scripts/manaloom_ui_source_digest.sh` (só calcula SHA-256 em `/tmp`).

## 1. Tabela-resumo por documento

| Documento | Estado lifecycle | Último commit | Afirmações verificadas | Erradas/defasadas | Veredito | Ação |
| --- | --- | --- | --- | --- | --- | --- |
| `docs/MANALOOM_E2E_RELEASE_CONTRACT.md` | current_contract (canônico) | `f6f791098` 2026-09-18 | 46 | 2 (4 %) | **CORRIGIR** (leve) | corrigir a tabela de perfis (nomes que o runner emite) e trocar "o CI" por `manaloom_local_ci.sh release` |
| `docs/MANALOOM_UI_LIVE_EVIDENCE_CONTRACT.md` | current_contract (canônico) | `f6f791098` 2026-09-18 | 52 | 3 (6 %) — todas por omissão (DEFASADO) | **CORRIGIR** | acrescentar ChromeDriver pinado, `--capture-core-product`, `--index-p0-matrix`, scripts Binder Import / Deck Workshop / P0 à lista de executáveis |
| `docs/MANALOOM_CARD_ART_SOURCE_CACHE_AND_RIGHTS_CONTRACT.md` | current_contract (canônico) | `fd0397a5a` 2026-08-24 | 26 | 0 | **MANTER** | nenhuma |
| `docs/MANALOOM_COLLECTION_INGESTION_CONTRACT.md` | current_contract (canônico) | `e6737c53b` 2026-08-10 | 31 | 2 (6 %) | **CORRIGIR** | registrar que `/collection/import` depende de `collection_private` e que o scanner exige `ENABLE_SCANNER_RELEASE` **e** a capability `scanner` |
| `docs/MANALOOM_EXTERNAL_ENGINE_DELTA_SCHEDULE.md` | current_contract (canônico) | `d39fe56af` 2026-07-28 | 24 | 0 (1 NAO_VERIFICAVEL, 1 contradição de outro doc) | **MANTER** | nenhuma neste doc; o MAPA (C7) é que está defasado |
| `docs/DECK_QUALITY_MODEL.md` | current_contract (canônico desde `8e6a7e0ed`) | `eb531386f` 2026-09-18 | 58 | 5 (9 %) + 9 NAO_VERIFICAVEL (números de backup) | **CORRIGIR** | corrigir "não está em canonical_documents", "zero ocorrências em docs/generated", duas referências de linha, e resolver `NOT_RATIFIED` vs canônico |
| `docs/BREWTACT_DECKBUILDER_AI_CURRENT_FLOW_2026-08-12.md` | current_contract (canônico) | `b2d3fc04f` 2026-08-13 | 84 | 7 (8 %) | **CORRIGIR** | separar "implementado" de "alcançável" na matriz (C1), registrar a política de capability nas rotas, atualizar tasks já PASS |
| `docs/project_logic_contracts.json` | contrato-fonte do registry | `8e6a7e0ed` 2026-09-18 | 312 caminhos + 8 fluxos + 6 receipts + 14 public paths | 14 divergências (todas em `flows`/`public_api_paths`/`receipt_contracts`; **0 caminho morto**) | **CORRIGIR** | ver §2.8; é a correção de maior alavancagem porque o registry e `CURRENT_SYSTEM.md` derivam dele |

Nenhum documento do grupo merece MARCAR_HISTORICO ou REMOVER: todos são contratos vigentes com a maioria das afirmações confirmadas no código. O padrão de erro é o mesmo em quatro deles: **foram escritos antes (ou no mesmo dia) da política de capabilities `brewtact_free_beta_2026-08-13` (`b2d3fc04f`) e não a incorporaram.**

Sobre as divergências "já conhecidas" do briefing: (1) terceiro portão confirmado; (2) 46 GoRoute confirmado; (3) **o "16 folhas" NÃO está em `project_logic_contracts.json`** — está em `docs/flows/life_counter_post_game.md:69` e `:89`; são 14 (`ls app/lib/features/home/life_counter/life_counter_native_*_sheet.dart | wc -l` = 14; `git grep -oh "open-native-[a-z-]*" app/lib | sort -u` = 14 tipos + prefixo solto); (4) confirmado; (5) confirmado e ampliado; (6) confirmado (3/220 PASS, 0/29 allowed).

---

## 2. Divergências por documento

Formato: linha | afirmação | classificação | evidência | verdade atual | ação | texto corrigido.

### 2.1 `docs/MANALOOM_E2E_RELEASE_CONTRACT.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 34-41 | Tabela "Perfis canônicos" com 6 perfis: `deterministic-read-only`, `diagnostic-allow-partial`, `isolated-mutating`, `isolated-play-vs-ai`, `live-smoke`, `release-target` | ERRADO (imprecisão de vocabulário) | `scripts/manaloom_e2e_suite.sh:96-102` emite exatamente `live-smoke+isolated-mutating`, `live-smoke`, `isolated-mutating`, `deterministic-read-only` como `requested_profile`; `:19,:60-63` definem `execution_policy` ∈ {`strict-gate`, `diagnostic-allow-partial`}; `grep -rn "isolated-play-vs-ai\|release-target" scripts server` = vazio | `diagnostic-allow-partial` é `execution_policy`, não perfil; `isolated-play-vs-ai` e `release-target` são rótulos desta página, não identificadores do summary; o perfil combinado `live-smoke+isolated-mutating` existe e não está na tabela | corrigir | Acrescentar abaixo da tabela: "O runner grava em `summary.json` `requested_profile` ∈ {`deterministic-read-only`, `isolated-mutating`, `live-smoke`, `live-smoke+isolated-mutating`} e `execution_policy` ∈ {`strict-gate`, `diagnostic-allow-partial`}. As linhas `isolated-play-vs-ai` e `release-target` são etapas desta página, executadas por `scripts/manaloom_play_vs_ai_e2e.sh` e pelo checklist de release, e não aparecem como perfil no summary." |
| 81-82 | "o CI executa esse bootstrap antes do gate" | ERRADO (nome) | `.github/` não tem `workflows/` (`ls .github` = `AGENT_POLICY.md agents instructions`); `project_logic_contracts.json:752` "GitHub Actions is intentionally absent"; quem executa o bootstrap é `scripts/manaloom_local_ci.sh:202-206` (`run_battle_gate`, só no modo `release`) e `scripts/manaloom_battle_product_gate.sh:782` | Não existe CI remoto; o bootstrap roda em `manaloom_local_ci.sh release` e dentro do próprio gate de battle | corrigir | "…a partir do SHA de `XMAGE_COMMIT`; `scripts/manaloom_local_ci.sh release` e `scripts/manaloom_battle_product_gate.sh` executam esse bootstrap antes do gate. Não há CI remoto." |

Tudo o mais conferiu: tokens `MANALOOM_CONFIRM_LIVE_MUTATIONS`/`MANALOOM_CONFIRM_POSTGRES_WRITES` (30/31 usos em `scripts`, `server/bin`, `server/lib`); `PASS=0 FAIL=1 BLOCKED=2 PARTIAL=3` (`manaloom_e2e_suite.sh:43-44`); `gate_eligible` só em `strict-gate`+`PASS` (`:522`); `/tmp/manaloom_e2e_suite_reports/<run>/summary.{md,json}` (`:10-13`); `quality_gate.sh e2e` (`:338`), `quality_gate.ps1:212` e `manaloom_local_ci.sh e2e` (`:209-213`) sempre `--strict`; todos os 15 modos citados na "Matriz mínima" existem em `quality_gate.sh:459-537`; `bootstrap_pinned_xmage_maven.sh` existe; `--require-deployable` em `manaloom_deploy_battle_sidecars.sh:68-71` e `manaloom_battle_product_gate.sh:688-689`; `engine_patch_commit` publicado em 5 arquivos de `server/lib`; `MANALOOM_PLAY_VS_AI_BROWSER_QA` (`manaloom_play_vs_ai_e2e.sh:10-14`), `release_ready:false`, `strategy_superiority_proven:false` (`:475-476`), `overall_ui_proof_claimed:false` (`:1091,1292`), 9 checkpoints (`:1055,1107`); `MANALOOM_RUN_MUTATING_RESOLUTION_E2E`, `MANALOOM_RUN_FLUTTER_RUNTIME_E2E`, `MANALOOM_RUN_SERVER_LIVE_E2E`, `MANALOOM_RUN_LIVE_PRODUCT_E2E`, `MANALOOM_FLUTTER_RUNTIME_DEVICE`, `MANALOOM_FLUTTER_RUNTIME_API_BASE_URL` (`manaloom_e2e_suite.sh:85-91,237-256`); `MANALOOM_FLUTTER_BIN` (12 scripts); hostname de produção bloqueado (`server/test/legacy_live_e2e_guard.py:9,23`); etapa "Ramp classifiers and data-foundation safety contracts" (`:431`) com `semantic_layer_v2_backfill*_test.dart` (`:425`, 2 arquivos existem); `active_consumer`/`manifest_only`/`ungoverned` em `report_retention_audit.py`; `docs/execution/CURRENT_QUEUE.md:28` com `NOW = BT-SCP-001`; `docs/qa/execution/` existe; `qualification.status=pass` e `deployment_allowed=true` no evidence da transição ativa.

### 2.2 `docs/MANALOOM_UI_LIVE_EVIDENCE_CONTRACT.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 90-165 (Fluxo canônico) e 283-312 (Arquivos executáveis) | O fluxo de captura Web é `./scripts/manaloom_ui_live_evidence_gate.sh --capture-battle-live-web` e os `*_visual_qa.sh`; nada sobre resolução do ChromeDriver | DEFASADO desde `b397f477b` (2026-09-21 20:29) | `scripts/lib/manaloom_chromedriver.sh:14-17` pina `153.0.8010.52` `mac-arm64` com SHA-256 e cache em `~/Library/Caches/manaloom/chromedriver/`; `resolve_manaloom_chromedriver` devolve 2 (BLOCKED) sem override executável nem pin em cache; o PATH nunca é consultado; a lib é `source`d por 12 scripts (`grep -ln manaloom_chromedriver.sh scripts/*.sh`), inclusive o gate (`:15,:284`), `manaloom_ui_source_digest.sh` e `manaloom_web_image_memory_profile.sh`; `--capture-battle-live-web` exige major do driver == major do Chrome (`gate:295-298`) | Toda captura Web hoje resolve o driver por essa lib: `MANALOOM_CHROMEDRIVER_BIN` (override, precisa executar) → pin em cache → BLOCKED apontando para `scripts/manaloom_chromedriver_bootstrap.sh` | corrigir | Inserir na seção "Fluxo canônico", antes dos scripts `*_visual_qa.sh`: "Toda captura Web resolve o ChromeDriver por `scripts/lib/manaloom_chromedriver.sh`: `MANALOOM_CHROMEDRIVER_BIN` (override explícito, precisa ser executável) → pin `153.0.8010.52` em `~/Library/Caches/manaloom/chromedriver/` → `BLOCKED` apontando para `scripts/manaloom_chromedriver_bootstrap.sh`. O PATH nunca é consultado. O major do driver precisa ser igual ao do Chrome instalado; divergência falha fechada." E na lista de executáveis: "- ChromeDriver pinado: `scripts/lib/manaloom_chromedriver.sh`; bootstrap: `scripts/manaloom_chromedriver_bootstrap.sh`;" |
| 283-312 | Lista "Arquivos executáveis" (integração + captura de 7 packs, extração, gate, digest, platform view, evidência) | DEFASADO (incompleta desde 2026-07-27/07-29/07-31/08-10) | Política `app/test/ui/fixtures/ui_live_evidence_policy.json` declara 11 superfícies, incluindo `binder_import` (7 checkpoints × 3 perfis, `../scripts/manaloom_binder_import_visual_qa.sh`), `deck_workshop` (12 × 3, `../scripts/manaloom_deck_workshop_visual_qa.sh`), `authenticated_p0_matrix` (54/53/53/54 checkpoints; `../scripts/manaloom_p0_runtime_capture.sh`; `--index-p0-matrix`); o gate tem os modos `--capture-core-product` (`gate:712`, desde `b642b9870` 2026-07-29, saída `docs/qa/ui-live/current/core-product-android`) e `--index-p0-matrix` (`gate:715`, desde `25fd4ee58` 2026-07-27); `app/integration_test/{binder_import,deck_workshop}_visual_runtime_proof_test.dart`, `app_existing_user_visual_audit_test.dart`, `core_product_acceptance_runtime_test.dart`, `app/test_driver/runtime_screenshot_crop.dart` existem e entram no digest (`manaloom_ui_source_digest.sh:33-48`) | O contrato descreve 7 dos 11 packs que o próprio gate verifica; a matriz P0 (a maior, 214 capturas) e o core-product Android não têm linha na lista | corrigir | Acrescentar à lista: "- integração Binder Import: `app/integration_test/binder_import_visual_runtime_proof_test.dart`; captura: `scripts/manaloom_binder_import_visual_qa.sh`;\n- integração Deck Workshop: `app/integration_test/deck_workshop_visual_runtime_proof_test.dart`; captura: `scripts/manaloom_deck_workshop_visual_qa.sh`;\n- matriz P0 autenticada: `app/integration_test/app_existing_user_visual_audit_test.dart`; captura por perfil: `scripts/manaloom_p0_runtime_capture.sh`; indexação: `scripts/manaloom_ui_live_evidence_gate.sh --index-p0-matrix`; manifests em `docs/qa/ui-live/current/p0-matrix/`;\n- core product Android: `app/integration_test/core_product_acceptance_runtime_test.dart`; captura: `scripts/manaloom_ui_live_evidence_gate.sh --capture-core-product`;\n- crop de margem do host Web: `app/test_driver/runtime_screenshot_crop.dart` (marcador `SCREENSHOT_VIEWPORT_CROP`)." |
| 226-231 | "A política corrente exige a matriz P0 ampla nos perfis declarados pela política executável. Binder Import, Deck Workshop, … mantêm suas matrizes próprias." | DEFASADO (verdade, mas sem os nomes dos perfis) | Perfis da P0: `web_mobile_390x844`, `web_desktop_1440x900`, `web_wide_1920x1080`, `android_emulator_manaloom_api34` (policy `surfaces[9].required_profiles`); Binder Import: `web_binder_import_{mobile_390x844,desktop_1440x900,wide_1920x1080}`; Deck Workshop: `web_deck_workshop_{…}` | Os perfis existem e são exclusivos, mas só os packs 04-08 estão nomeados no contrato | corrigir | Após a frase, acrescentar: "Os perfis P0 são `web_mobile_390x844`, `web_desktop_1440x900`, `web_wide_1920x1080` e `android_emulator_manaloom_api34` (54/53/53/54 checkpoints); Binder Import usa `web_binder_import_*` (7 checkpoints × 3) e Deck Workshop `web_deck_workshop_*` (12 × 3)." |

Conferiu: `PASS_AUTOMATED`/`PASS_RUNTIME`/`PASS_VISUAL_REVIEWED`/`PASS` (policy `status_model`); `--check`, `--capture-play-vs-ai`, `--capture-battle-live-web` (`gate:700-711`); `MANALOOM_UI_PROOF_DEVICE`, `MANALOOM_UI_ANDROID_RUNTIME_KIND` (`gate:721`); 9 PNGs `web_play_vs_ai_1440x900` com os 9 nomes de checkpoint; 5/15 onboarding, 10/30 visual system, 22/66 critical overlays, 16/48 social trade, 10/30 battle learning, 5 battle live (policy `required_checkpoints` × `required_profiles`); `docs/qa/ui-live/current/{play-vs-ai-web-real,battle-live-web,battle-coach-android}/capture-manifest.json` e `visual-review.json` existem; `docs/qa/execution/2026-08-25/play-vs-ai-real-xmage-e2e.md` existe; `latest.json` registra `capture_manifests[].sha256`, `reviewed_profiles`, `reviewed_screenshot_count`; `ui-proof`, `ui-audit` (`quality_gate.sh:198,203`), `battle-lab` (`:305`) e `manaloom_local_ci.sh quick` (`:222`) chamam `--check`; digest cobre `app/lib`, `app/assets`, `app/web`, `res` Android, pubspecs, fixtures e harness (`manaloom_ui_source_digest.sh:24-49`); atestação ADB por `ro.kernel.qemu`/`ro.boot.qemu` (`gate:48-53`); platform view por `adb exec-out screencap -p`, assinatura PNG e orientação landscape (`manaloom_capture_android_platform_view.sh:15,65,76-84`); `docs/qa/ui-live/videos/` no `.gitignore:57`.

**Estado real hoje (não é erro do contrato, é fato para a tabela de verdade):** o digest corrente é `8bba809cb7617445ea8bde807643515ac716a08ef28a554164c3c17d0b090ba4`; `docs/qa/ui-live/latest.json` (último commit `f6f791098`, 2026-09-18) ainda aponta `source_digest=865e6041…`; dos 23 manifests que ele referencia, **22 foram recapturados em `8bba809c` mas com SHA-256 diferente do registrado, e `play-vs-ai-web-real` continua em `865e6041`** → `--check` falha fechado hoje (0 de 23 casam digest+hash). Dos 35 diretórios em `docs/qa/ui-live/current/`, 18 estão no digest corrente. Bate com `PONTO_DE_RETOMADA:19-28` e com o bloqueio `BT-UIEV-001` do MAPA §7.

### 2.3 `docs/MANALOOM_CARD_ART_SOURCE_CACHE_AND_RIGHTS_CONTRACT.md`

Sem divergência. Conferido: `AppImageCachePolicy` 96 entradas / 32 MiB / decode 1400 px (`app/lib/core/services/image_cache_policy.dart:13-17`); intervalo 125 ms e retries 500/1500 ms (`scryfall_image_request_policy.dart:13,53-54`); `User-Agent: ManaLoom/1.0` e `Accept: image/*` (`cached_card_image.dart:43-44`); `fit = BoxFit.contain` (`:66`); hosts `api.scryfall.com`/`cards.scryfall.io` (`:127,144`); estado `reference` (`card_artwork.dart:27,189-221`); `art_crop` = 0 ocorrências em `app/lib/features`; `Image.network`/`CachedNetworkImage(` = 0 em `app/lib/features`; guard `app/test/core/widgets/card_image_source_policy_guard_test.dart` existe; rota `/legal` (`main.dart:472`); `currentTermsVersion = '2026-08-05'` (`server/lib/legal_policy.dart:1`); aviso de conteúdo de fã com Wizards e Scryfall (`legal_screen.dart:150-153`, usa `ProductIdentity.displayName`, então já diz "BrewTact"). As cinco fontes oficiais (linhas 132-143) são NAO_VERIFICAVEL offline. Observação sem ação: o documento e o `User-Agent` dizem "ManaLoom" enquanto a marca pública é BrewTact (ADR 0010); código e contrato coincidem, então não é divergência.

### 2.4 `docs/MANALOOM_COLLECTION_INGESTION_CONTRACT.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 113-114 | "Quando `ENABLE_SCANNER_RELEASE=true`, o workspace pode abrir `CardScannerScreen` em `continuousBinderSession`." | DEFASADO desde `b2d3fc04f` (2026-08-13) | `app/lib/core/config/launch_features.dart:4-11`: "This flag is never an authorization decision. Runtime access also requires the server capability `scanner.allowed == true`"; `app/lib/main.dart:104-109` passa `scanner: LaunchFeatures.scannerSupported` para `ReleaseRouteBuildSupport`; `app/lib/core/config/release_capabilities.dart:392-393` faz `buildSupport.scanner && capabilities.isAllowed(ReleaseCapability.scanner)`; `server/config/release_capabilities.json`: `scanner` = `implemented_guarded`, `allowed:false` | O flag de build é suporte técnico; o CTA/rota só existe com o flag **e** a capability `scanner` ligada no servidor — hoje desligada | corrigir | "Quando o artefato foi compilado com `ENABLE_SCANNER_RELEASE=true` **e** a capability `scanner` de `server/config/release_capabilities.json` está `allowed`, o workspace pode abrir `CardScannerScreen` em `continuousBinderSession`. O flag de build nunca é autorização por si só (`app/lib/core/config/launch_features.dart`). Hoje a capability está `off`." |
| 17 | "O workspace persistente `/collection/import` é a superfície canônica." | DEFASADO (sem o portão) | Rota existe (`main.dart:758`); guard do app manda `/collection/*` para `/home` quando `collection_private` está off (`release_capabilities.dart:474,507`); servidor classifica `/binder/**` em `collection_private` (`release_capability_policy.dart:535`) → 404 `capability_unavailable`; `collection_private` = `implemented_p0_open`, `allowed:false` | A superfície é canônica mas inalcançável na beta atual | corrigir | Acrescentar: "A rota e os endpoints `/binder/import/*` ficam sob a capability `collection_private`; com ela `off` (estado atual da política `brewtact_free_beta_2026-08-13`) o app redireciona para `/home` e o servidor responde 404 `capability_unavailable`." |

Conferiu: `binderImportMaxItems = 100` (`server/lib/binder_import_contract.dart:13`); parser aceita `#` (`binder_import_models.dart:279`), `quantityPattern`/`printingHintPattern` (`:272-273`); `needsPrinting` (provider/screen/models); `POST /binder/import/preview` e `/apply` (`server/routes/binder/import/{preview,apply}/index.dart`); `collection_availability_snapshot` (`preview:100,225`; `apply:267`); `FOR UPDATE` (`apply:138,197`), `ON CONFLICT … DO NOTHING` (`apply:170-172`), `unchanged` (`:212`), `binder_import_inventory_changed` (`:249`); `historyLimit = 10` e `SharedPreferences` (`binder_import_draft_store.dart:22,25`); `continuousBinderSession: true` (`binder_import_screen.dart:211`); ADR 0008 existe.

### 2.5 `docs/MANALOOM_EXTERNAL_ENGINE_DELTA_SCHEDULE.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 43-47 | "a opção operacional é uma automação cron local do Codex … roda domingo às 09:17 … chama-se `ManaLoom • Deltas XMage/Forge`" | NAO_VERIFICAVEL (configuração externa ao repo), mas **compatível com a evidência**: `~/Library/Application Support/ManaLoom/external-engine-delta/` tem 9 relatórios (07-28, 08-02, 08-10, 08-17, 08-24, 08-25, 08-31, 09-13, 09-20), os de domingo às 12:18Z = 09:18 BRT; `latest.json` = `2026-09-20T12:18:42Z`, `status=review_required`, `mode=official_upstream_compare` | Algo roda semanalmente e escreve na superfície exclusiva; 6 de 9 relatórios são `skipped/dirty_worktree`; o último (domingo 2026-09-20) consultou o upstream com sucesso | nenhuma | — |
| 53-60 | "Um LaunchAgent continua disponível para checkouts fora das pastas protegidas" | CORRETO — e **contradiz o MAPA**: `docs/MAPA_OPERACIONAL_DO_PROJETO.md:176-197` e C7 (`:365`) dizem "cadência semanal declarada como instalada; não está" e "latest.json skipped · dirty_worktree" | `ls ~/Library/LaunchAgents/com.manaloom.external-engine-delta-weekly.plist` = não existe (este doc nunca afirmou que está instalado neste checkout; `:63` explica o bloqueio de TCC); `latest.json` hoje é `review_required`, não `skipped` | O doc está certo; o MAPA §4.3 está defasado desde 2026-09-20 | nenhuma neste doc (corrigir o MAPA §4.3/C7) | — |

Conferiu: scripts `manaloom_external_engine_delta_audit.sh`, `_weekly.sh`, `manaloom_install_external_engine_delta_schedule.sh` existem; `--local-only` (`weekly:18,28`); `REPORT_DIR` default `~/Library/Application Support/ManaLoom/external-engine-delta` (`weekly:6`); retenção 180 dias (`weekly:7,144`) só sobre `external-engine-delta-<UTC>-<pid>.json` (`weekly:142`); `latest.json` (`weekly:118`); `skipped`/`dirty_worktree` antes de rede (`weekly:197`); janela de 8 dias (`installer:153-154`); `--install|--check|--run-now|--uninstall` (`installer:214-226`); bloqueio `Documents|Desktop|Downloads` e override `MANALOOM_ALLOW_PROTECTED_LAUNCHD_ROOT=I_HAVE_GRANTED_FULL_DISK_ACCESS` (`installer:58-64`); label `com.manaloom.external-engine-delta-weekly` (`installer:6`); `Weekday 0 / Hour 9 / Minute 17` (`installer:96-98`); `test_external_engine_delta_schedule_contract.py` existe; modos `engine-capabilities` e `engine-transition` existem (`quality_gate.sh:521-526`); `EXTERNAL_ENGINE_PIN_TRANSITION_CONTRACT.json` com `deploy_requires_qualification_status: pass` (`:13`).

### 2.6 `docs/DECK_QUALITY_MODEL.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 355-357 | "Este documento ainda **não** está em `canonical_documents`… Registrá-lo é uma linha" | DEFASADO desde `8e6a7e0ed` (2026-09-18 14:22, 2 h depois de `eb531386f` que criou o doc) | `docs/project_logic_contracts.json:16` = `"docs/DECK_QUALITY_MODEL.md"`; `git log -S'docs/DECK_QUALITY_MODEL.md' -- docs/project_logic_contracts.json` → `8e6a7e0ed docs: register the operational map and deck-quality model as canonical` | Está em `canonical_documents`, logo em estado `current_contract` (`canonical_default_state`) | corrigir | "Este documento está em `canonical_documents` de `docs/project_logic_contracts.json` desde `8e6a7e0ed` (2026-09-18) e herda o estado `current_contract`. Ele descreve mecanismo; não define prioridade nem autoriza mutação." |
| 3 | Status `… · NOT_RATIFIED` | CONTRADIZ_OUTRO_DOC (`project_logic_contracts.json:16` + `documentation_lifecycle.canonical_default_state = current_contract`) | idem acima | O doc é contrato corrente pelo lifecycle e diz de si mesmo que não foi ratificado | corrigir (decisão do dono: ratificar ou tirar da lista) | Se ratificado: trocar `NOT_RATIFIED` por `CANONICAL_CURRENT_CONTRACT_2026-09-18`. Se não: remover a linha 16 do JSON e manter o status. |
| 12-14 | "`referenceProfile`, `usage_count`, `commander_reference_profiles` e `optimize_filler_loader_support` têm **zero ocorrências** em `docs/generated/` e em `docs/project_logic_contracts.json`" | ERRADO (nunca foi verdade para `docs/generated/` como um todo) | `docs/generated/DATABASE_ERD.md:393,488` (`number usage_count`) e `:572` (`commander_reference_profiles {`), commit `e6737c53b` de 2026-08-10, anterior ao doc; zero em `CURRENT_SYSTEM.md`, `openapi.generated.json` e no JSON (verificado) | O ERD gerado lista tabela e coluna (sem explicar o mecanismo); os demais arquivos têm zero | corrigir | "…têm zero ocorrências em `docs/generated/CURRENT_SYSTEM.md`, `docs/generated/openapi.generated.json` e `docs/project_logic_contracts.json`; `docs/generated/DATABASE_ERD.md` lista a tabela e a coluna como schema, sem explicar o que decidem." |
| 121 | "`API_CONTRACTS_AND_DATA_MAP.md:224` — lote 1 + lote 2 + Anchor 30 batches A-C persistidos (~42)" | ERRADO (referência de linha) | `server/doc/API_CONTRACTS_AND_DATA_MAP.md:224` é a linha de `POST /ai/generate`; a alegação de perfis aplicados está em `:372-395` ("Anchor 30 Batch A … exact Commander Reference Profiles were applied", "Batch B …") | A afirmação de conteúdo está no doc, em outra linha | corrigir | trocar `:224` por `:372-395` |
| 119 | "Built-ins no código 2 (Lorehold, Kaalia) — `commander_reference_profile_support.dart:47,57`" | ERRADO (referência de linha, leve) | constantes em `:6` (`loreholdReferenceCommanderName`) e `:10` (`kaaliaReferenceCommanderName`); predicados em `:47` (`isLoreholdCommanderReferenceCandidate`) e `:53` (`isKaaliaCommanderReferenceCandidate`); `:57` é `}` | 2 built-ins confirmados | corrigir | `:6,10` (nomes) e `:47,53` (candidatos) |
| 37-38, 167-229, 239-246, 264-266, 277-284, 296-311 | Números de banco: 33.274 linhas, `max(last_updated_at)=2026-03-13`, distribuição, 574 empatadas, 653 decks/composição, 29.593 nomes órfãos, 615 lendárias, datas de `meta_decks`/`edhrec_card_snapshots` | NAO_VERIFICAVEL sem restaurar o dump (o doc já os rotula como backup de 2026-08-03) | `backups/manaloom-postgres/manaloom-postgres-20260803T141713Z.dump` existe no disco; extração exige `pg_restore`/PostgreSQL, fora do escopo read-only desta rodada | Fica como está: o doc é honesto sobre a proveniência | nenhuma | — |

Conferiu: bifurcação em `server/routes/ai/generate/index.dart:136` (`loadUsableCommanderReferenceProfile`), prosa de archetype stats `:553-558`, prompt estático `:559`, reparo dentro de `if (referenceProfile != null)` `:1056`; `release_capability_policy.dart:425-430` (`/ai/generate` → `ai_generate_rebuild`); `* 0.72` em `commander_reference_card_stats_support.dart:767` dentro de `loadCompatibleCommanderReferenceArchetypeStats` (`:663-786`); migração `034 create_commander_reference_tables` em `migrate.dart:1059-1062`, ausente de `database_setup.sql`; `deck_count` gravado `0` (`profile_support.dart:640-647`); `role_targets` só prosa (`:543`); 10 JSONs + README "no database apply was run" (`docs/qa/commander_reference_profiles_secrets_of_strixhaven_2026-05-11/README.md:9`); `8cab6400b` (2026-05-30) removeu `anchor_30_queue.json` e lotes `edhtop16_promotable_batch_*`; CLI `commander_reference_profile.dart:76` e `commander_reference_profile_lorehold.dart`; `loadCompetitiveNonLandFillers` em `optimize_filler_loader_support.dart:1263` (a query citada, `ORDER BY sub.pop_score DESC … LIMIT 600`, está em `:87-112`, dentro do helper que ela chama — a referência 1263-1320 aponta para a assinatura, não para o SQL); `compareCandidates` `:516`; `extract_meta_insights.dart` lê `meta_decks` inteira (`:133-135`), acumula `usage_count + @usage` (`:838`), `TRUNCATE` só sob `--full` (`:25,:92`); `manaloom_ops_daemon.py:736` `_jobs_for_release_policy`, 16 jobs, único sem capability `hermes_cron_governor_report` (`:732`), 8 flags forçadas `"0"` (`:243-250`); `server/.env.example:63` = `MANALOOM_ENABLE_PRODUCT_LEARNING_WRITES=0`; `upsertCommanderLearnedDeck` definida em `commander_learned_deck_support.dart:785` e sem chamador; `commander_learned_deck.dart:19` lança `StateError` no `--apply`; `shouldWriteProductLearning` `e2e_validation_policy.dart:21-36`; apply summary `2026-06-29T20:41:39Z` com `mode: apply`, `db_mutations: true` (`server/test/artifacts/commander_learned_deck_import_lorehold_v615_mana_engine_v1/*_apply_summary.json`); `magicrules.txt` "effective as of June 19, 2026" (`:3`; último commit do arquivo 2026-07-16); `commander_game_changers.json` último commit 2026-07-14; MTGJSON `5.3.0+20260605` (`server/doc/SYNC_CARDS_FULL_FAST_2026-06-06.md:31`); `cron_snapshot_edhrec.sh` último commit 2026-06-02; 8 de 9 `server/bin/cron_*.sh` não registrados no daemon (só `cron_cleanup_optimize_telemetry.sh` está); `quality_gate.sh deck-quality` (`:500`) e fixture 36 Forest + 64 vanilla ≥ 84 (`server/test/ai/deck_quality_report_limits_test.dart:15-38`); commits `354983a1e` (2026-09-04) e `a2d044618` (2026-09-18) existem; `activeUsableCommanderReferenceProfilesSql` em `commander_reference_readiness_cli_support.dart:1`.

### 2.7 `docs/BREWTACT_DECKBUILDER_AI_CURRENT_FLOW_2026-08-12.md`

| Linha | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| 374-384 | Matriz "Funciona hoje": `sim` para criar/importar/editar, Analyze, Analyze IA, Generate, Optimize, Complete | CONTRADIZ_OUTRO_DOC (`docs/MAPA_OPERACIONAL_DO_PROJETO.md:106-108` e C1 `:359`) e o código | `server/config/release_capabilities.json`: 29/29 `allowed:false`; `server/routes/_middleware.dart:105-144` responde 404 `capability_unavailable` antes do handler; `release_capability_policy.dart:425-441` classifica `/ai/generate`, `/ai/rebuild`, `/ai/optimize`, `/decks/{id}/analysis`, `/decks/{id}/ai-analysis`; `decks_private` off cobre `/decks`; a palavra `capability` aparece 2 vezes no doc e `release_capabilit` 0 | Tudo isso está **implementado** e **inalcançável** hoje (404) — a coluna mede implementação | corrigir | Renomear a coluna para "Implementado" e acrescentar "Alcançável hoje (capability)": criar/importar/editar → `não · decks_private`; Analyze → `não · ai_analyze_optimize_advisory`; Analyze IA → `não · ai_analyze_optimize_advisory`; Generate → `não · ai_generate_rebuild`; Optimize/Complete → `não · ai_analyze_optimize_advisory`; Rebuild → `não · ai_generate_rebuild`; Learning → `não · learning_writes/learning_reads`; Battle → `não · battle_batch` **e** não compilado (`ENABLE_INTERACTIVE_BATTLE` default `false`, `launch_features.dart:31-33`; `main.dart:675,690,704,713`). |
| 298-299 | "A rota `/ai/commander-learning` responde `503` antes do banco enquanto desligada" | DEFASADO desde `b2d3fc04f` (mesmo commit da última edição do doc) | `release_capability_policy.dart:443-445`: `GET /ai/commander-learning` → `learning_reads` (off) → 404 no middleware raiz; o `503` (`server/routes/ai/commander-learning/index.dart:104`) só é alcançado com a capability ligada e os env `MANALOOM_ENABLE_*_READS` ausentes | Primeiro portão é 404 `capability_unavailable`; o 503 é o segundo | corrigir | "A rota `/ai/commander-learning` responde 404 `capability_unavailable` enquanto `learning_reads` estiver `off`; com a capability ligada e as duas variáveis ausentes ela responde `503` antes de tocar o banco." |
| 352-368 | Inventário de rotas: coluna "Postura até fechar tasks" diz "entra no registry/capability de `BT-AI-029`", "default-off … decidir em `BT-AI-029`" para `/ai/archetypes`, `/ai/explain`, `/decks/:id/recommendations`, `/ai/weakness-analysis`, `/ai/simulate-matchup`, `/decks/:id/simulate`, `/ai/ml-status` | DEFASADO desde `b2d3fc04f` | `release_capability_policy.dart:431-441`: `/ai/archetypes`, `/ai/explain`, `/decks/{id}/(ai-)analysis` → `ai_analyze_optimize_advisory`; `:446-453`: `/ai/ml-status`, `/ai/simulate-matchup`, `/ai/weakness-analysis`, `/ai/optimize/telemetry`, `/decks/{id}/(recommendations|simulate)` → `legacy_ai_routes` (`contained_legacy`, off) | O enforcement server-side que o doc pede a `BT-AI-029` já existe como capability; o que falta é a decisão adapter/410/remover | corrigir | Acrescentar coluna "Capability (hoje `off`)" com os valores acima e trocar "entra no registry/capability de `BT-AI-029`" por "já classificada em `ai_analyze_optimize_advisory`; `BT-AI-029` decide adapter/410/remover". Nota após a tabela: "As 7 rotas sem consumer Flutter estão sob `legacy_ai_routes` (`contained_legacy`, `allowed:false`)." |
| 498-499, 579 | "Gaps que bloqueiam … 1. `BT-GOV-001`, `BT-SCP-001`, `BT-DOC-004`, …"; "Critério … 1. `BT-DOC-004` provar IDs/dependências/lifecycle" | DEFASADO | `docs/generated/TASK_REGISTRY.json`: `BT-GOV-001` = `PASS`, `BT-DOC-004` = `PASS`, `BT-DOC-001` = `PASS`; `BT-SCP-001` = `IN_PROGRESS_CONTAINED` (é o `NOW`, `CURRENT_QUEUE.md:28`); `BT-GATE-001`/`002` = `IMPLEMENTED_LOCAL_PENDING_FULL_GATE` | Dois dos cinco IDs da onda 1 já fecharam | corrigir | "1. verdade/capabilities/DAG/gates: ~~`BT-GOV-001`~~ (PASS), `BT-SCP-001` (NOW), ~~`BT-DOC-004`~~ (PASS), `BT-GATE-001`, `BT-GATE-002`;" e no critério 1: "`BT-DOC-004` (PASS) provou IDs/dependências/lifecycle;" |
| 5 | Status `… · IMPLEMENTATION_PACKAGE_NOT_STARTED` | DEFASADO | Registry: 10 `IN_PROGRESS_CONTAINED` (entre eles `BT-DB-004`, `DCK-P0-05`, `BT-AI-020`, `BT-SCP-001`), 7 `IMPLEMENTED_LOCAL_PENDING_FULL_GATE` (entre eles `BT-AI-001`, `BT-GATE-001`, `BT-GATE-002`), 3 `PASS` | O pacote começou pelos P0 de governança/gates; o núcleo Deck/IA (`DCK-P0-00..04`) continua `TODO`/`BLOCKED_BY_P0` | corrigir | `SECOND_AUDIT_MAPPED · CONTAINMENT_PARTIAL · IMPLEMENTATION_PACKAGE_STARTED_GOVERNANCE_ONLY` + linha "Estado por task: ver `docs/generated/TASK_REGISTRY.json` (3 PASS, 10 IN_PROGRESS_CONTAINED, 7 IMPLEMENTED_LOCAL_PENDING_FULL_GATE em 2026-09-22)." |
| 434-436 | "`optimize_feedback_support.dart`: removido do caminho runtime" | DEFASADO (verdade mais forte) | `git log --diff-filter=D -- server/lib/ai/optimize_feedback_support.dart` → apagado em `b2d3fc04f`; `find server -name optimize_feedback_support.dart` = vazio | O arquivo não existe mais | corrigir | "`optimize_feedback_support.dart`: removido da árvore em `b2d3fc04f`; o receipt futuro deve nascer no apply confirmado…" |
| 302-303 | "O scan atual encontrou 2.809 candidatos, incluindo 1.921 duplicatas" | NAO_VERIFICAVEL (saída de execução não versionada; `grep -rn "2809\|2\.809" docs server/doc` só encontra este doc) | — | — | nenhuma (ou anexar o receipt) | — |

Conferiu: os 49 caminhos `server/…`, `app/…`, `docs/…` citados existem (0 ausentes); os 91 IDs de task citados existem no registry (0 ausentes); tombstones `optimizer_loop.sh` (`exit 2`, `:9`) e `update_schema.dart` (`exitCode = 2`, `:14`); `optimize_preview_received` (`deck_provider.dart:704`) e `deck_optimized` = 0; `can_save: false` (`generate/index.dart:1275,1307,1342`); `training_eligible`/`quarantined_source` (`pull_learning_events.py`); `--report-only` e `BLOCKED_BT_AI_014` (`manaloom_knowledge_import.sh:11,27`); `MANALOOM_ENABLE_PROMOTED_LEARNED_DECK_READS`/`…_COMMANDER_USAGE_CORPUS_READS` em `commander_learned_deck_support.dart`; `card_deck_profiles` = 0 em `server/lib`+`server/routes`; `battle_simulator.dart`, `deck_state_analysis.dart`, `optimize_state_support.dart` existem; gates `manaloom_deck_ai_learning_gate.sh --profile local|release-read-only` com `MANALOOM_DECK_AI_RELEASE_RECEIPT` (`:14-24`), `quality_gate.sh deck-ai-learning` (`:518`), `ai-bridge`, `deep-ai`, `pg-contract`, três `*_audit.py`, `manaloom_project_logic.sh --write/--check`; `PARTIAL=3`.

### 2.8 `docs/project_logic_contracts.json`

Conferência programática: **0 caminho morto** em `canonical_documents` (36), `document_overrides` (17), `prefix_rules` (9), `receipt_contracts` (producers/consumers), `flows[*].{implementation,tests,gates}` (8 fluxos), `traceability` (12 regras), `api_route_aliases`, `.tbls.yml`. As divergências são todas de **conteúdo declarado vs. comportamento real**:

| Chave | Afirmação | Classificação | Evidência | Verdade atual | Ação | Texto corrigido |
| --- | --- | --- | --- | --- | --- | --- |
| `flows[deck_lifecycle].entrypoints` | `"/import"`, `"/decks/generate"` | ERRADO | `grep "path: '/import'" app/lib/main.dart` = vazio (o servidor tem `server/routes/import/to-deck/index.dart`); `/decks/generate` é gated por `ai_generate_rebuild` (`release_capability_policy.dart:425-430`), capability do fluxo `deck_ai`; faltam `/decks/:id`, `/decks/:id/search`, `/decks/:id/scan` (rotas de edição em `main.dart`) | `/import` não é rota de app; `/decks/generate` pertence a `deck_ai` | corrigir | `"entrypoints": ["/decks", "/decks/import", "/decks/{id}", "/decks/{id}/search", "/decks/{id}/scan", "POST /import/to-deck"]` |
| `flows[card_collection].entrypoints` | `"/cards"`, `"/sets"` | ERRADO | `grep "path: '/sets'\|path: '/cards'" app/lib/main.dart` = vazio; rotas de app são `/cards/:cardId` e `/collection/sets` | mistura path de servidor com rota de app | corrigir | `"entrypoints": ["/collection", "/collection/sets", "/cards/{cardId}", "GET /cards", "GET /sets"]` |
| `flows[battle_replay].entrypoints` | `"/decks/{id}/battle-coach"`, `"/decks/{id}/battle-coach/{sessionId}"` | DEFASADO | `app/lib/main.dart:704-722`: ambas são `GoRoute` com `redirect:` para `play-vs-ai` | redirect depreciado, não entrypoint | corrigir | remover as duas e anotar em `sequence` ou num campo `"deprecated_redirects": ["/decks/{id}/battle-coach", "/decks/{id}/battle-coach/{sessionId}"]` |
| `flows[deck_ai].entrypoints` | `"/ai/commander-reference"` | ERRADO | `grep -rn "commander-reference" app/ --include='*.dart'` = vazio (confirmado em `docs/flows/deck_ai.md:177`, rechecado); rota de 1.225 linhas sem chamador Flutter | é implementação server-only, não entrada de jornada | corrigir | mover para `implementation` (já está) e retirar de `entrypoints` |
| `flows[deck_ai].implementation` | 18 arquivos, nenhum é `server/routes/decks/[id]/index.dart` | DEFASADO | o apply do Optimize passa por `PUT /decks/{id}` (`server/routes/decks/[id]/index.dart`) sob `deck_replace_all` (política `:15-45` lista a capability) | o passo final da jornada não está rastreado | corrigir | acrescentar `"server/routes/decks/[id]/index.dart"` a `implementation` |
| `flows[deck_ai].status` | `"experimental_guarded"` | CONTRADIZ_OUTRO_DOC (`server/config/release_capabilities.json`) | `ai_analyze_optimize_advisory` = `experimental_p0_open`; `ai_generate_rebuild` = `experimental_guarded` | um status para dois eixos diferentes | corrigir | `"status": {"analyze_optimize": "experimental_p0_open", "generate_rebuild": "experimental_guarded"}` ou dividir o fluxo |
| `flows[release_operations].entrypoints` | `"quality gates"`, `"release scripts"`, `"/health"`, `"/ready"` | ERRADO | dois rótulos não são caminhos; falta `/capabilities`, único endpoint público consumido pelo app antes do login (`app/lib/core/config/release_capabilities.dart:251`; `server/routes/capabilities/index.dart`) | — | corrigir | `"entrypoints": ["GET /", "GET /capabilities", "GET /health", "GET /health/live", "GET /health/ready", "GET /ready", "scripts/quality_gate.sh", "scripts/manaloom_local_ci.sh", "scripts/manaloom_build_beta_release.sh"]` |
| `flows[release_operations].implementation`/`tests` | 7 impl. / 5 testes, nenhum do portão de capability | DEFASADO desde `b2d3fc04f` | `server/routes/_middleware.dart:105-144`, `server/lib/release_capability_policy.dart`, `server/routes/capabilities/index.dart`, `app/lib/core/config/release_capabilities.dart` e `server/test/release_capability_policy_test.dart` (565 linhas) não aparecem em fluxo nenhum; **nenhum fluxo declara `server/routes/_middleware.dart`** | o portão fail-closed que define a beta não é `implementation` de nada | corrigir | acrescentar os 4 arquivos a `implementation` e `server/test/release_capability_policy_test.dart`, `app/test/core/config/release_capabilities_test.dart`, `app/test/core/config/release_capability_surface_contract_test.dart` a `tests` |
| `flows[release_operations].gates` | `["scripts/manaloom_release_ops_contract_test.sh", "scripts/manaloom_e2e_suite.sh"]` | ERRADO (cobertura) | `grep -n 'dart test' scripts/manaloom_release_ops_contract_test.sh` = vazio; ele roda só `release_sbom_scope_test.py` (`:211`); a lista `dart_tests` da suíte E2E (`:396-419`) não tem nenhum dos 4 `.dart`; `deploy_rollback_convergence_contract_test.dart` e `flutter_web_deploy_contract_test.dart` só rodam via `dart test` genérico em `quality_gate.sh quick/full` (`run_backend_full:65-77` usa `find test -name '*_test.dart'`) | os gates declarados executam 1 dos 5 testes declarados | corrigir | `"gates": ["scripts/quality_gate.sh full", "scripts/manaloom_release_ops_contract_test.sh", "scripts/manaloom_e2e_suite.sh"]` |
| `flows[release_operations].storage` | `["schema_migrations", "sync_log"]` | ERRADO (escopo) | `sync_log` só é escrito por `server/bin/sync_cards.dart:373` e `sync_staples.dart:440` — jobs de catálogo, não build/deploy/migração; faltam `sync_state` (`sync_cards.dart:301`) e `data_source_snapshots` (`sync_combos.dart:515`) se a intenção for cobrir os syncs | — | corrigir | `"storage": ["schema_migrations"]` (mover `sync_log`, `sync_state`, `data_source_snapshots` para o fluxo de ops/cron novo) |
| `flows[social_trade].gates`/`tests` | `tests[0] = server/test/e2e_trade_tests.py`; `gates = [scripts/manaloom_e2e_suite.sh]` | ERRADO (vínculo) | `e2e_trade_tests.py:8` exige `--api URL` (suíte legacy live, guardada por `legacy_live_e2e_guard.py`); nenhuma etapa de `manaloom_e2e_suite.sh` (labels em `:294-621`) o invoca; `community_engagement_contract_test.dart` roda só no `dart test` genérico de `quality_gate.sh full` | o gate declarado não executa o teste declarado | corrigir | `"tests"`: marcar `e2e_trade_tests.py` como `live_only`; `"gates": ["scripts/quality_gate.sh full", "scripts/manaloom_e2e_suite.sh"]` |
| `flows` (conjunto) | 8 fluxos | DEFASADO (incompleto) | `docs/flows/` documentou 11; sem contrato: `home_onboarding_notifications` (`/home`, `/onboarding/core-flow`, `/notifications`, FCM, canal Android `manaloom_notifications` em `MainActivity.kt:14,29-43`), `commercial_plans` (`/plans`, `/upgrade`, `/checkout`, `POST /billing/webhook`, `scripts/manaloom_commercial_quality_gate.sh`), `public_web` (`web-public/`, 8 páginas + `healthz`/`robots`/`sitemap`, consome `GET /reports/{id}`, deploy `scripts/manaloom_deploy_public_web.sh`) e o **terceiro portão** `server/bin/manaloom_ops_daemon.py` (16 jobs, `JOB_REQUIRED_CAPABILITIES:711-734`, `_jobs_for_release_policy:736`, health próprio em `MANALOOM_NATIVE_BATTLE_PORT`, deploy `scripts/manaloom_deploy_ops_image.sh`) | a superfície mais alcançável (site público) e o maior escritor de dado de produto (crons) não têm dono | corrigir | acrescentar 4 fluxos: `home_onboarding_notifications`, `commercial_plans`, `public_web`, `ops_scheduler` (ou seção de `release_operations`), e separar `binder_collection_scanner` de `card_collection` (capabilities `collection_private`/`scanner`/`binder_public` vs `catalog_private`) |
| `flows[release_operations].id` | `release_operations` | CONTRADIZ_OUTRO_DOC (`docs/flows/platform_release_ops.md`, `docs/flows/README.md`) | nome divergente para o mesmo fluxo | — | corrigir (um dos dois) | renomear `docs/flows/platform_release_ops.md` → `release_operations.md` (o id do contrato alimenta o registry e o generator; o doc de apoio é o que deve ceder) |
| `public_api_paths` | inclui `/sets`, `/billing/webhook`, `/auth/register`; não inclui `/capabilities` | ERRADO | `release_capability_policy.dart:527-528` `/sets` → `catalog_private` (off → 404); `:521` `POST /billing/webhook` → `billing_checkout` (off → 404); `:385-386` `/auth/register` → `account_registration` (off → 404; o app redireciona `/register` → `/login`); allowlist `:590-620` tem `GET /capabilities` e não tem os três | das 14 entradas, respondem hoje: `/`, `/ready`, `/health`, `/health/live`, `/health/ready`, `/reports/{id}` e os `POST /auth/*` do plano de controle (menos `register`) | corrigir | adicionar `"/capabilities"`; anotar `/sets`, `/billing/webhook`, `/auth/register` como `"public_when_capability_on": {...}` ou removê-los |
| `receipt_contracts[deck_ai_learning_gate_v2].consumers` | `["scripts/quality_gate.sh", "scripts/manaloom_local_ci.sh"]` | ERRADO (leve) | `grep -n "deck_ai\|learning" scripts/manaloom_local_ci.sh` = vazio; `local_ci full` → `melos run quality` (`melos.yaml:98`) = `project-logic && full && ui-audit && custom-lint && patrol-smoke && dependency_audit` — nunca `deck-ai-learning` | só `quality_gate.sh deck-ai-learning` consome | corrigir | `"consumers": ["scripts/quality_gate.sh deck-ai-learning"]` |
| `canonical_documents[6]` | `docs/DECK_QUALITY_MODEL.md` como canônico | CONTRADIZ_OUTRO_DOC (o próprio doc, `:3` `NOT_RATIFIED`, `:355-357`) | ver §2.6 | — | corrigir (decisão do dono) | ver §2.6 |
| `traceability[3].canonical_source`, `[4]` | `docs/hermes-analysis/DATA_MODEL_FINAL_VALIDATION_2026-06-15.md` | DEFASADO (leve) | arquivo existe, mas não está em `canonical_documents` e o prefixo `docs/hermes-analysis/` não tem regra de lifecycle (cai em `supporting_reference_non_authoritative`) | uma regra de rastreabilidade aponta para fonte não canônica | corrigir | adicionar o arquivo a `canonical_documents` ou trocar `canonical_source` por `docs/hermes-analysis/DATA_FIELD_ALIAS_CONTRACT_2026-06-30.md` |

Conferiu: `documentation_lifecycle` (estados, overrides e prefixos batem com o disco; `docs/adr/0004-xmage-human-spike-go.md` existe como duplicata marcada histórica); `receipt_contracts` — todos os producers existem; `manaloom_e2e_suite_v1.status_semantics` bate com `manaloom_e2e_suite.sh:43-44,522`; `ui_live_evidence_v1.allowed_statuses` bate com a policy; `play_vs_ai_e2e_report_v1.required_bindings` batem com os campos gravados em `manaloom_play_vs_ai_e2e.sh:475-476,1086-1110`; `release_identity_v1` — `manaloom_release_identity.sh` existe e é chamado por 4 scripts de build/deploy e por `manaloom_release_ops_contract_test.sh:144`; `api_route_aliases` `/community/decks/following` → `server/routes/community/decks/[id]/index.dart:17` (`if (id == 'following')`); `runtime_introspection.local_gates` (hooks `.githooks/pre-commit` → `local_ci quick`, `pre-push` → `local_ci full`; modos `quick|schema|full|e2e|release` em `local_ci:237-259`; `.tbls.yml` existe; "GitHub Actions is intentionally absent" confirmado); `known_limits` são declarações, sem contraprova.

---

## 3. Fatos confirmados (tabela de verdade — 2026-09-22)

Cada linha foi conferida na fonte primária nesta rodada.

**Governança e escopo**
1. `server/config/release_capabilities.json`: `policy_version = brewtact_free_beta_2026-08-13`, **29 capabilities, 0 `allowed`**, `live_verified_as_of = null`; estabelecida em `b2d3fc04f` (2026-08-13 17:05). Statuses de implementação presentes: `implemented_p0_open` (ex.: `catalog_private`, `decks_private`, `collection_private`), `experimental_p0_open` (`ai_analyze_optimize_advisory`), `experimental_guarded` (`ai_generate_rebuild`), `implemented_guarded` (`battle_batch`, `scanner`), `contained_legacy` (`learning_writes`), `not_implemented`.
2. **Três portões fail-closed** consomem o mesmo arquivo: `server/routes/_middleware.dart:105-144` (404 `capability_unavailable` / 404 `capability_route_unclassified` / 503 `capability_policy_invalid`), `app/lib/core/config/release_capabilities.dart:350-534` (`ReleaseCapabilityRouteGuard`, snapshot default `denied()`), e `server/bin/manaloom_ops_daemon.py:145-205,711-746` (16 jobs; só `hermes_cron_governor_report` roda com 29/29 off; 8 flags de apply forçadas `"0"` em `:243-250`).
3. `app/lib/main.dart`: **46 `GoRoute(`**. `/decks/{id}/battle-coach` e `/battle-coach/{sessionId}` são `redirect` (`:704-722`). Battle interativo não é compilado por padrão (`ENABLE_INTERACTIVE_BATTLE` default `false`, `launch_features.dart:31-33`); scanner idem (`ENABLE_SCANNER_RELEASE` default `false`, `:8-11`) e ambos ainda exigem a capability do servidor.
4. `docs/generated/TASK_REGISTRY.json`: **220 tasks** — `TODO` 86, `BLOCKED_BY_P0` 77, `DEFERRED_BY_SCOPE` 33, `IN_PROGRESS_CONTAINED` 10, `IMPLEMENTED_LOCAL_PENDING_FULL_GATE` 7, `WAITING_EXTERNAL` 4, **`PASS` 3 (`BT-GOV-001`, `BT-DOC-001`, `BT-DOC-004`)**. `BT-UIEV-001` e `BT-CI-001`, citados nos 6 últimos commits, **não existem no registry**.
5. `docs/execution/CURRENT_QUEUE.md:28`: `NOW = BT-SCP-001` (`IN_PROGRESS_CONTAINED`, `P0 CORE`).
6. `docs/project_logic_contracts.json`: 36 `canonical_documents`, 8 `flows`, 6 `receipt_contracts`, 12 regras de `traceability`, 14 `public_api_paths`; **todos os 312 caminhos declarados existem no disco**. `DECK_QUALITY_MODEL.md` e `MAPA_OPERACIONAL_DO_PROJETO.md` são canônicos desde `8e6a7e0ed` (2026-09-18 14:22).
7. Não existe `.github/workflows/`; os gates são `.githooks/pre-commit` → `scripts/manaloom_local_ci.sh quick` e `.githooks/pre-push` → `scripts/manaloom_local_ci.sh full` (`full` → `melos run quality` = `project-logic && full && ui-audit && custom-lint && patrol-smoke && dependency_audit`, mais `run_schema_gate`).
8. `scripts/quality_gate.sh` tem 27 modos: `quick full performance web-image-memory resolution ui-audit ui-proof web deps custom-lint patrol-smoke ai-eval deck-quality ai-bridge server-target report-retention pg-contract deep-ai deck-ai-learning battle battle-lab engine-capabilities engine-transition engine-delta project-logic e2e help` (`:459-537`).
9. "16 folhas nativas" do contador é erro de `docs/flows/life_counter_post_game.md:69,89`, **não** de `project_logic_contracts.json`; são **14** (`life_counter_native_*_sheet.dart` = 14; tipos `open-native-*` = 14).

**E2E e release**
10. `scripts/manaloom_e2e_suite.sh`: `requested_profile` ∈ {`deterministic-read-only`, `isolated-mutating`, `live-smoke`, `live-smoke+isolated-mutating`}; `execution_policy` ∈ {`strict-gate` (default), `diagnostic-allow-partial`}; exit `PASS=0 FAIL=1 BLOCKED=2 PARTIAL=3`; `gate_eligible` só em `strict-gate`+`PASS`; relatórios em `/tmp/manaloom_e2e_suite_reports/<run>/summary.{md,json}`; `quality_gate.sh e2e`, `manaloom_local_ci.sh e2e` e `quality_gate.ps1` sempre `--strict`.
11. Tokens de escrita: `MANALOOM_CONFIRM_LIVE_MUTATIONS=I_HAVE_EXPLICIT_APPROVAL` (30 usos) e `MANALOOM_CONFIRM_POSTGRES_WRITES=I_HAVE_EXPLICIT_APPROVAL` (31 usos) em `scripts`, `server/bin`, `server/lib`; `MANALOOM_CONFIRM_ISOLATED_RELEASE_CAPABILITIES` (6) para o override isolado de capabilities (`release_capability_policy.dart:346-375`, só arquivo em `/tmp`).
12. Pins: `XMAGE_COMMIT = 2c43ec8cdb5cd475d47e6b555a4077151f476a3b`, `XMAGE_PATCH_COMMIT = 991948742f840cd88493a4ea8cb3f4ed192e4742`, `FORGE_COMMIT = a62915f500c2411484689294659c6bb84ea215f8`. Transição ativa `34d81ea → 2c43ec8` (2026-07-28) com `qualification.status = pass`, `deployment_allowed = true` (`docs/qa/evidence/XMAGE_PIN_TRANSITION_34d81ea_2c43ec8.json`).
13. Auditoria de delta: 9 relatórios em `~/Library/Application Support/ManaLoom/external-engine-delta/` (2026-07-28 → 2026-09-20); 6 `skipped/dirty_worktree`; **o mais recente (domingo 2026-09-20 12:18Z) é `review_required` com comparação upstream real** — o MAPA §4.3/C7 ("latest.json skipped") está defasado. LaunchAgent `com.manaloom.external-engine-delta-weekly` **não** instalado (checkout sob `~/Documents`, bloqueado por TCC).

**Evidência de UI**
14. Digest de UI corrente: `8bba809cb7617445ea8bde807643515ac716a08ef28a554164c3c17d0b090ba4`. `docs/qa/ui-live/latest.json` (último commit `f6f791098`, 2026-09-18) está em `865e6041…` com `status: PASS` e `overall_release: NO_GO_CAPABILITIES_OFF`; **0 dos 23 manifests referenciados casam digest+hash** (22 recapturados em `8bba809c` com hash novo, `play-vs-ai-web-real` ainda em `865e6041`). `manaloom_ui_live_evidence_gate.sh --check` falha fechado hoje; por isso `quick` e `full` falham e nenhum commit passa sem `--no-verify`. 35 diretórios sob `docs/qa/ui-live/current/`, 18 no digest corrente.
15. ChromeDriver: pin único `153.0.8010.52 mac-arm64` em `scripts/lib/manaloom_chromedriver.sh` (desde `b397f477b`, 2026-09-21), `source`d por 12 scripts; override `MANALOOM_CHROMEDRIVER_BIN`; cache `~/Library/Caches/manaloom/chromedriver/`; PATH nunca usado; `~/.manaloom/toolchains/chromedriver-150.0.7871.124` é resíduo sem consumidor.
16. Política executável `app/test/ui/fixtures/ui_live_evidence_policy.json`: 11 superfícies; checkpoints × perfis: play_vs_ai 9×1, battle_live 5 (interno), binder_import 7×3, deck_workshop 12×3, battle_learning 10×3 (histórico), social_trade 16×3, onboarding_intent 5×3, visual_system_workspace 10×3, critical_overlays_states 22×3, authenticated_p0_matrix 54/53/53/54, core_product Android; `ios_voiceover = DEFERRED_BY_SCOPE`; `android_talkback = pending_physical_human_review`.
17. Flutter pinado `~/.manaloom/toolchains/flutter-3.44.6` (usado por todos os `*_visual_qa.sh` e pelo gate).

**Catálogo, arte, coleção**
18. Arte de carta: cache vivo 96 entradas / 32 MiB / decode 1400 px; gate Scryfall 125 ms, retries 500 ms e 1500 ms; `User-Agent: ManaLoom/1.0`; `BoxFit.contain`; zero `art_crop` e zero `Image.network`/`CachedNetworkImage(` em `app/lib/features`; `currentTermsVersion = '2026-08-05'`; aviso legal em `/legal` usa `ProductIdentity.displayName`.
19. Importação de coleção: `binderImportMaxItems = 100`; histórico local 10 em `SharedPreferences`; apply com `FOR UPDATE` + `ON CONFLICT … DO NOTHING` + `binder_import_inventory_changed`; `/collection/import` e `/binder/**` sob `collection_private` (off); scanner exige `ENABLE_SCANNER_RELEASE` **e** capability `scanner` (off).

**Deck/IA**
20. `POST /ai/generate` bifurca em `loadUsableCommanderReferenceProfile` (`generate/index.dart:136`); reparo estrutural só dentro de `if (referenceProfile != null)` (`:1056`); pool de candidatos `ORDER BY pop_score DESC … LIMIT 600` sem o comandante na query (`optimize_filler_loader_support.dart:87-112`, chamado por `loadCompetitiveNonLandFillers:1263`); `usage_count` acumula sem decaimento (`extract_meta_insights.dart:838`), `TRUNCATE` só com `--full` (`:92`); 2 perfis built-in (Lorehold, Kaalia); perfis nascem só por CLI (`commander_reference_profile.dart:76`, `_lorehold.dart`); `upsertCommanderLearnedDeck` (`commander_learned_deck_support.dart:785`) sem chamador; última escrita real de learned deck comprovada: `2026-06-29T20:41:39Z` (`server/test/artifacts/…/lorehold_candidate_607_v615_mana_engine_v1_apply_summary.json`, `mode: apply`, `db_mutations: true`).
21. Classificação das rotas de IA (`release_capability_policy.dart:425-453`): `/ai/generate`, `/ai/generate/jobs/*`, `/ai/rebuild`, `/ai/commander-reference` → `ai_generate_rebuild`; `/ai/optimize`, `/ai/optimize/jobs/*`, `/ai/archetypes`, `/ai/explain`, `/decks/{id}/(ai-)analysis`, `/decks/{id}/optimizations*` → `ai_analyze_optimize_advisory`; `GET /ai/commander-learning` → `learning_reads`, `POST` → `learning_writes`; `/ai/ml-status`, `/ai/simulate-matchup`, `/ai/weakness-analysis`, `/ai/optimize/telemetry*`, `/decks/{id}/(recommendations|simulate)` → `legacy_ai_routes`. Todas `off` → 404.
22. Frescor: `server/magicrules.txt` "effective as of June 19, 2026" (commit 2026-07-16); `server/config/commander_game_changers.json` commit 2026-07-14; MTGJSON `5.3.0+20260605`; 8 de 9 `server/bin/cron_*.sh` não registrados em `manaloom_ops_daemon.py` (só `cron_cleanup_optimize_telemetry.sh`); `server/.env.example:63` `MANALOOM_ENABLE_PRODUCT_LEARNING_WRITES=0`; backup local `backups/manaloom-postgres/manaloom-postgres-20260803T141713Z.dump` existe (números de banco do `DECK_QUALITY_MODEL` só se verificam restaurando-o).

**Datas dos contratos (para o cabeçalho de lifecycle)**
23. Últimas edições: E2E e UI contracts `f6f791098` (2026-09-18); Card Art `fd0397a5a` (2026-08-24); Collection Ingestion `e6737c53b` (2026-08-10); Delta Schedule `d39fe56af` (2026-07-28); Deck Quality `eb531386f` (2026-09-18); Deckbuilder Flow `b2d3fc04f` (2026-08-13, mesmo commit da política de capabilities); `project_logic_contracts.json` `8e6a7e0ed` (2026-09-18).
