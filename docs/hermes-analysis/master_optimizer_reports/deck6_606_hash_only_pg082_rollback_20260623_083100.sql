\pset pager off

BEGIN;

DELETE FROM card_battle_rules
WHERE normalized_name IN (
  'library of leng',
  'scroll rack',
  'unexpected windfall',
  'valakut awakening // valakut stoneforge',
  'wayfarer''s bauble'
);

INSERT INTO card_battle_rules (
  normalized_name,
  card_id,
  card_name,
  effect_json,
  deck_role_json,
  source,
  confidence,
  review_status,
  rule_version,
  oracle_hash,
  notes,
  reviewed_by,
  reviewed_at,
  created_at,
  updated_at,
  last_seen_at,
  logical_rule_key,
  execution_status
)
SELECT
  normalized_name,
  card_id,
  card_name,
  effect_json,
  deck_role_json,
  source,
  confidence,
  review_status,
  rule_version,
  oracle_hash,
  notes,
  reviewed_by,
  reviewed_at,
  created_at,
  updated_at,
  last_seen_at,
  logical_rule_key,
  execution_status
FROM manaloom_deploy_audit.pg082_deck6_606_hash_only_20260623_083100;

COMMIT;
