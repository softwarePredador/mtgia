# XMage Definitive Flow Pilot: Crystal Vein

Date: 2026-06-29

## Scope

Pilotar o fluxo definitivo em uma carta real da familia `ramp_permanent`, usando o XMage local em `/Users/desenvolvimentomobile/Downloads/mage-master`, sem promover para PostgreSQL e sem sincronizar Hermes.

Carta escolhida: `Crystal Vein`, porque o XMage expõe duas habilidades distintas:

- `{T}: Add {C}.`
- `{T}, Sacrifice Crystal Vein: Add {C}{C}.`

Fonte XMage verificada:

- `/Users/desenvolvimentomobile/Downloads/mage-master/Mage.Sets/src/mage/cards/c/CrystalVein.java`

## Mapper Result

Antes deste piloto, `Crystal Vein` caia no fallback:

- `xmage_land_mana_source_variant_review_v1`
- `mana_produced=2`
- `activation_requires_sacrifice=true`

Isso era rapido, mas perigoso para runtime: podia representar a terra como se sempre gerasse dois manas.

Depois do piloto, o mapper separa os modos:

```json
{
  "effect": "ramp_permanent",
  "battle_model_scope": "colorless_land_tap_or_tap_sacrifice_two_colorless_mode_v1",
  "mana_produced": 1,
  "default_mana_produced": 1,
  "produces": "C",
  "has_sacrifice_mana_mode": true,
  "sacrifice_mana_produced": 2,
  "sacrifice_mana_mode_status": "runtime_required",
  "alternate_mana_modes": [
    {
      "mode": "tap_sacrifice_for_two_colorless",
      "produces": "C",
      "mana_produced": 2,
      "activation_requires_tap": true,
      "activation_requires_sacrifice": true,
      "status": "runtime_required"
    }
  ],
  "oracle_runtime_scope": "land_alternate_sacrifice_mana_mode_runtime_required_v1"
}
```

## Pipeline Evidence

Command:

```bash
/usr/bin/time -p python3 docs/hermes-analysis/manaloom-knowledge/scripts/xmage_current_replay_batch_pipeline.py \
  --xmage-root /Users/desenvolvimentomobile/Downloads/mage-master \
  --skip-materialize \
  --output-prefix /tmp/manaloom_definitive_flow_pilot_after_crystal_vein_20260629_final
```

Output summary:

- Total current replay/deck cards scanned: 541
- Actionable cards: 139
- Exact XMage-backed structured/review candidates: 76
- Mapper-required cards: 63
- `ramp_permanent`: 29
- `proposal_status_counts`: `mapper_metadata_or_test_scenario_required=63`, `runtime_family_implementation_required=1`, `split_family_scope_review_required=75`
- Wall time: `real 0.78s`
- Mean scan/classification time: about `0.0014s/card` across all scanned cards, or `0.0056s/actionable card`

`Crystal Vein` final lane:

- `validity_status=ready_for_structured_xmage_pull_review_required`
- `family_id=ramp_permanent`
- `promotion_lane=split_family_scope_review_required`
- `proposal_status=split_family_scope_review_required`
- `safe_for_batch_pg_package=false`
- `execution_status=review_only`

Suggested focused scenarios now generated by the pipeline:

- `tap land for one colorless mana without sacrificing it`
- `tap and sacrifice land for two colorless mana when the extra mana unlocks a cast`

## Runtime Evidence

Added runtime guard:

```bash
python3 -m unittest test_reviewed_battle_card_rules.ReviewedBattleCardRulesTests.test_colorless_land_sacrifice_mana_mode_does_not_overproduce_before_runtime_executor
```

Result:

- `Ran 1 test ... OK`

The guard proves the current battle runtime does not inflate `Crystal Vein` to two free mana while the sacrifice-mode executor is still marked `runtime_required`.

## Focused Test Evidence

Commands:

```bash
python3 -m unittest test_xmage_to_manaloom_effect_hints.py
python3 -m unittest test_xmage_semantic_family_batch_pipeline.py
```

Results:

- Mapper tests: `Ran 254 tests ... OK`
- Family/generator tests: `Ran 239 tests ... OK`

## Conclusion

This is the desired shape of the definitive flow:

1. XMage exact source is read locally.
2. Mapper extracts a specific ManaLoom semantic scope instead of a broad fallback.
3. Dangerous runtime assumptions are not promoted.
4. The family lane remains blocked from PostgreSQL batch promotion until runtime support exists.
5. The generated test scenarios identify exactly what must be implemented next.

No PostgreSQL writes were performed. No Hermes sync was performed.
