# ManaLoom Hermes Operating Context

## Identidade
- Repo: /opt/data/workspace/mtgia
- Branch: codex/hermes-analysis-docs
- Produto: Plataforma Commander-first para Magic: The Gathering
- Stack: Flutter (app/) + Dart Frog (server/) + PostgreSQL

## Regras de Operação

### Escopo de Escrita
- `docs/hermes-analysis/**` e `.hermes.md` — uso livre
- `server/lib/`, `app/lib/`, `routes/` — permitido quando explicitamente solicitado
- Para escrever fora de `docs/hermes-analysis/`: usar `terminal()` com `cat >` (write_file é restrito)
- **SEMPRE compilar após mudanças Dart:** `dart analyze <path>` antes de commitar
- **SEMPRE commitar após cada mudança lógica** — não acumular

### Segurança
- NUNCA versionar secrets — GitHub Push Protection ativo
- NUNCA commitar em `master`
- Credenciais apenas em memória local (não versionar)

### Canonical Reading Order
1. `docs/CONTEXTO_PRODUTO_ATUAL.md`
2. `docs/hermes-analysis/PROJECT_MEMORY.md`
3. `docs/hermes-analysis/TECHNICAL_MAP.md`
4. `git log --oneline -40`
5. **Ler TODOS os arquivos que serão modificados ANTES de começar**

### Regra de Ouro
**NUNCA assumir que conheço o código.** Sempre ler o arquivo completo antes de modificar. Sempre compilar antes de commitar.

### Validação
```bash
dart analyze lib/ai/<arquivo>.dart  # Obrigatório após cada mudança
cd server && dart test               # Testes do backend
```
