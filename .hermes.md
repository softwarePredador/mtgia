# ManaLoom Hermes Operating Protocol

This repository is `softwarePredador/mtgia`. Follow this protocol before any
implementation, verification, commit, or push.

## Non-Negotiable Git Rules

1. Always start with:
   ```bash
   pwd
   git rev-parse --show-toplevel
   git branch --show-current
   git status --short --branch
   ```
2. Do not change branches if `git status --short` is not clean, unless the user
   explicitly asked you to preserve or discard the visible changes.
3. Never use low-level Git workarounds for normal development:
   - no `git hash-object`
   - no `git update-index`
   - no `git write-tree`
   - no `git commit-tree`
   - no manual edits under `.git/objects`, `.git/refs`, or `.git/index`
4. If `git add`, `git commit`, `git diff`, or `git push` fails, stop and report
   the exact failure. Do not invent a workaround.
5. Before committing, run:
   ```bash
   git diff --check
   git status --short
   ```
6. Commit only files that are part of the requested task. Explain every
   untracked file before deciding whether to add it.
7. Never commit local databases, generated scratch files, secrets, raw dashboard
   HTML, `.env`, credentials, API keys, PATs, JWTs, DSNs, or private payloads.

## Branch And Source-Of-Truth Model

1. The repository intentionally keeps only two active branches:
   - `master`: canonical product code, curated docs, deploy source.
   - `codex/hermes-analysis-docs`: Hermes staging/memory branch for raw reports.
2. `codex/hermes-analysis-docs` is not a source of truth. Never merge raw Hermes
   reports directly into `master`.
3. Any Hermes finding that should affect product work must be triaged into a
   small `master` commit with:
   - file/line evidence;
   - impact;
   - decision: `real`, `stale`, or `needs proof`;
   - validation command/result.
4. Runtime Hermes crons may update only `docs/hermes-analysis/**` on
   `codex/hermes-analysis-docs`, unless a human explicitly requests another
   branch. Product code changes remain a Codex/local development responsibility.
5. After Codex pushes `master`, call Hermes in report-only mode against the
   pushed SHA. Treat Hermes output as an independent audit signal, not as an
   automatic merge instruction.

## Implementation Rules

1. Read the relevant project sources before changing code:
   - `.github/instructions/guia.instructions.md`
   - `docs/CONTEXTO_PRODUTO_ATUAL.md`
   - `server/manual-de-instrucao.md`
   - `server/doc/API_CONTRACTS_AND_DATA_MAP.md`
   - any `.github/agents/*.agent.md` file that matches the task domain.
2. Use the smallest safe change. Do not start broad refactors while fixing a
   focused bug.
3. If extracting code into a new file, the same commit must also wire imports,
   remove duplicated old definitions, and run analyzer/tests. Do not leave
   untracked extraction files as "future work".
4. For app-facing backend contract changes, update
   `server/doc/API_CONTRACTS_AND_DATA_MAP.md` and `server/manual-de-instrucao.md`
   unless the change is strictly internal and backward-compatible.
5. Mark uncertain findings as `not_proven`; do not present guesses as facts.

## Hermes Analysis Context

- Local canonical analysis tree: `docs/hermes-analysis/**`.
- Hermes server workspace: `/opt/data/workspace/mtgia`.
- Branch policy:
  - `master` is canonical.
  - `codex/hermes-analysis-docs` is staging/memory only.
  - See `docs/hermes-analysis/BRANCH_RETENTION_AUDIT_2026-06-11.md`.
- Main battle engine: `docs/hermes-analysis/manaloom-knowledge/scripts/battle_analyst_v8.py`.
- Master optimizer scripts live under
  `docs/hermes-analysis/manaloom-knowledge/scripts/`.
- Canonical docs for current Hermes loop:
  - `docs/hermes-analysis/HERMES_E2E_SYSTEM_CONTRACT_2026-06-07.md`
  - `docs/hermes-analysis/HERMES_MASTER_OPTIMIZER_LOOP_2026-06-06.md`
  - `docs/hermes-analysis/HERMES_BATTLE_DECKBUILDING_RULE_REGISTRY_2026-06-07.md`
- Structure auditor:
  `docs/hermes-analysis/scripts/structure_auditor.py`.

When working on Hermes, read the canonical docs above before changing battle,
optimizer, cron, or registry behavior. `master` may receive Hermes work only
through a clean merge plus validation evidence.

## Required Validation

For backend Dart changes, always run at least:

```bash
cd server
dart analyze
dart test <focused_test_files>
```

For optimize/AI route changes, include the relevant focused tests, usually:

```bash
cd server
dart test test/optimization_validator_test.dart \
  test/ai_optimize_semantic_enforcement_route_contract_test.dart
```

If a validation command cannot run because of environment limits, report the
exact command, the exact failure, and what remains unproven.

## Final Response Checklist

Every implementation response must include:

- branch name;
- commit hash if committed;
- files changed;
- validation commands and results;
- untracked files left behind, if any;
- explicit statement if no push was performed.
